# lim-it — 개발 매거진

**게임 devlog 전용이 아닙니다.** 공부한 것, 만든 것, 그냥 일기까지 다 들어옵니다.
지금은 게임 개발에 관심이 쏠려 있어서 그쪽 글이 늘고 있을 뿐입니다.

글의 기본은 **왜 그렇게 했는지**를 남기는 것입니다. Astro + MDX,
글 하나가 파일 하나이고, 커밋하면 Vercel이 배포합니다.

> `CLAUDE.md` 는 이 파일을 가리키는 심볼릭 링크입니다. 규약은 여기 한 곳에만 씁니다.

⚠ **블로그가 아니라 매거진으로 굴러갑니다.** 2026-08-13 에 카테고리와 태그를
없애고 **저자**로 합쳤습니다. 글 하나는 저자 한 명에게 속하고, 그게 유일한
분류입니다. 저자는 전부 한 사람이 쓰는 이름이지만 **사이트 어디에도 그렇게 적지
않습니다** — 정한 것이니 "같은 사람"이라는 문구를 넣지 마세요 (§3).

⚠ **"이 블로그는 게임 devlog다" 라고 전제하지 마세요.** 2026-08-12 에
티스토리(`hyuk-todayfeelsogood.tistory.com`)의 글 116개를 옮겨왔고, 지금
전체 글 124편 중 113편이 학습 기록(study Lim)입니다.

---

## 0. 작업 규칙 — 손대기 전에 읽으세요

**이 블로그를 고칠 때는 항상 이 파일부터 읽고 시작합니다.** 매번 같은 맥락에서
같은 판단을 하기 위해서입니다. 세션이 바뀌어도 결정의 근거는 여기 남아 있습니다.

**작업이 끝나면 — 코드를 고쳤을 때만 — §8 에 한 줄 남깁니다.**

- 남기는 건 **뭐가 불편해서 뭘 했는가** 입니다. "이랬는데 저래서 이렇게 바꿨습니다" 정도면 충분합니다.
- **코드 설명은 쓰지 않습니다.** 함수 이름, 인자, 구현 방식은 파일을 열면 보입니다. 여기 옮겨 적으면 코드가 바뀔 때 같이 썩습니다.
- 대신 **코드만 봐서는 모르는 것**을 씁니다 — 왜 이 방법이었는지, 뭘 안 하기로 했는지, 어디를 건드리면 안 되는지.
- 규약이 새로 생겼으면 §8 이 아니라 **해당 절(§1~§7)에 직접** 씁니다. §8 은 기록이지 규약이 아닙니다.

**글을 쓴 것은 §8 에 적지 않습니다.** 글은 그 자체로 남습니다. 말투(§1)와
쓰는 방식(§2)만 지키면 됩니다.

**끝나면 커밋하고 push 합니다.** 혼자 하는 저장소라 브랜치를 따로 파지 않고
`main` 에 바로 올립니다. `npm run post` 를 쓰면 빌드가 깨졌을 때 push 를 막아 줍니다.
커밋 메시지는 **왜 고쳤는지가 보이게** 씁니다 — "BaseHead 수정"이 아니라
"fix: 공유 카드에 이미지가 안 뜨던 것".

---

## 1. 말투 — 제일 자주 틀리는 부분

**참고 기준:** 티스토리에서 옮겨온 글들 (`src/content/posts/` 의 `# 티스토리에서 옮겨왔습니다:` 주석이 붙은 것들). 원본은 https://hyuk-todayfeelsogood.tistory.com/

글은 전부 **-습니다체 존댓말**로 씁니다. 기술 학습 일지 같은 톤입니다.

| | 이렇게 | 이렇게 말고 |
|---|---|---|
| 종결어미 | "~했습니다""~입니다""~됩니다" | "~했다""~이다""~한다" |
| 자기지칭 | **저** | 나, 내가 |
| 독자 | 가끔 질문을 던집니다 —"카드 두 장인 걸까요?" | "~해보자"같은 청유·명령 |
| 이모지 | 거의 안 씁니다 | 문단마다 이모지 |

- 부드럽게 풀어도 됩니다: "~하더라고요""~거든요""~같습니다"
- **코드 주석은 예외입니다.** 원본 코드를 그대로 인용할 때는 손대지 않습니다.

### 저자마다 다른 것과 같은 것

**위의 표는 저자가 누구든 똑같습니다.** 종결어미, 자기지칭, 이모지, 꺾쇠 금지,
한 문장 한 단락 — 전부 공통입니다. 매거진은 저자가 달라도 지면 톤은 하나입니다.

**저자마다 다른 것은 `src/consts.ts` 의 `AUTHORS[].voice` 에 있습니다.**
문장 길이, 결론을 내리는지, 숫자를 얼마나 쓰는지 같은 것들입니다.

⚠ **글을 쓰기 전에 그 저자의 `voice` 를 읽으세요.** 종결어미까지 저자마다
다르게 만들지 마세요 — 같은 지면에서 나온 글로 안 읽힙니다.

### 인용 부호는 큰따옴표만 씁니다

⚠ **꺾쇠 `「」` 를 쓰지 마세요.** 강조하거나 말을 따올 때는 `""` 를 씁니다.

```
✗  죽지 않는 게임에서는 「무엇을 뺏을지」가 곧 난이도입니다
✓  죽지 않는 게임에서는 "무엇을 뺏을지?"가 곧 난이도입니다
```

물음이면 물음표까지 안에 넣습니다. 따옴표를 안 써도 읽히면 그냥 빼는 쪽이 낫습니다.

### 직관적인 단어를 씁니다

비유로 돌려 말하지 말고 **화면에 보이는 그대로** 씁니다. 읽는 사람이 한 번 더 해석해야 하는 말은 소제목에서 특히 나쁩니다.

```
✗  깃발에 주인이 생겼습니다
✓  깃발에 색상을 추가했습니다
```

같은 이유로 "프런티어"처럼 코드에서만 쓰는 말도 본문에서는 풉니다 — "이미 연 칸 옆".

기준 샘플은 `src/content/posts/status-effects-redefined.md` 입니다. **새 글을 쓰기 전에 그것부터 읽으세요.**

### ⚠ 사람이 쓰는 말투는 이것보다 헐겁습니다

2026-08-19 에 [`blog-rebuild-notes.md`](src/content/posts/blog-rebuild-notes.md) 를
`/admin` 에서 직접 고치셨습니다. **그 글이 진짜 말투 표본입니다** — 위 규칙(-습니다체·
"저"·꺾쇠 금지)은 그대로인데, AI가 쓴 문장이 지나치게 반듯해서 손을 본 것입니다.

무엇이 바뀌었는지가 곧 규칙입니다.

| | AI 초안 | 사람이 고친 것 |
|---|---|---|
| 제목 | `티스토리 116개를 옮기고 블로그를 고친 이틀` | `블로그 수정` |
| 소개문 | `정적 사이트에 비밀글을 붙이고 폰에서 글을 쓰게 만들면서 내린 판단들` | `블로그 수정하면서 생긴 것.` |
| 첫 문단 | 바로 기술 이야기 | **왜 다시 만들고 싶었는지** (morethan-log·노션을 안 쓰게 된 이야기) |
| 소제목 | `## 이미지 404장을 전부 받아왔습니다` | 지우고 `---` 로 |
| 단정 | `**그건 비밀글이 아닙니다.**` | 지움 |

- **앞뒤를 사람 말로 엽니다.** 기술 문단은 그대로 두되, 도입과 마무리에 왜 하고 싶었는지·하고 나서 어땠는지를 답니다.
- **말끝을 흐려도 됩니다** — "~네요", "~싶기도 하고..", "그래도 .. 하나하나 고쳐볼 생각입니다! :)"
- **말줄임표와 감탄사를 씁니다** — `...`, `ㅎㄷㄷ`, `허헣`, 취소선(`~~ ~~`), `:)`. 그림 이모지는 여전히 안 씁니다.
- **자기 자랑·자책을 조금 섞습니다** — "꽤나 많이 썻네요... 나 좀 열심히 했을지도?"
- **AI를 썼다는 것을 숨기지 않습니다** — "AI에게 정리 해달라고 했고 그걸 제가 좀 고쳤습니다".
- **결론을 굵게 못박지 않습니다.** 판단은 적되 선언은 덜어냅니다.
- 소제목은 `##` 도 쓰고 `---` 로만 나누기도 합니다. 덩어리만 갈리면 됩니다.

⚠ **기술 설명 문단까지 구어로 풀지는 마세요.** 사람이 고친 글에서도 숫자와 근거가 있는
문단은 거의 그대로 남았습니다. **바꾸는 건 이음새와 앞뒤**입니다.

---

## 2. 글 쓰는 방식

**이렇게 씁니다**

- **문제부터 씁니다.** 뭐가 불편했고 왜 손대야 했는지가 첫 문단에 있어야 합니다.
- **왜 그 선택인지 씁니다.** 무엇을 했는지는 커밋 로그에도 있습니다. 블로그에 쓸 값어치가 있는 건 판단의 이유입니다.
- **틀렸던 것도 씁니다.** 잘못 짰다가 고친 것, 예상 못 한 부작용, 뒤늦게 발견한 버그. 이게 제일 읽을 만합니다.
- **구체적인 숫자와 이름을 씁니다.** "밸런스를 조정했습니다"가 아니라 "전사 20에서 50으로, 10시드 × 1200런, 클리어율 33.0%".
- **끝에 남은 걸 한두 개 씁니다.** 다음에 읽을 사람(=본인)에게 쓸모 있는 문장으로.

**이렇게 안 씁니다**

- 커밋 메시지를 그대로 옮기지 않습니다. 변경사항 나열은 changelog지 글이 아닙니다.
- 확인 안 한 코드를 넣지 않습니다. 파일을 안 읽었으면 스니펫을 쓰지 않습니다.
- 과장하지 않습니다. "혁신적인""완벽하게 해결" 같은 말은 쓰지 않습니다.
- 튜토리얼처럼 쓰지 않습니다. 남을 가르치는 글이 아니라 **내가 뭘 했는지 남기는 글**입니다.
- 커밋에 없는 동기를 추측해서 채우지 않습니다. 모르면 안 씁니다.

### 단락과 사진

읽는 사람이 스크롤로 지치지 않게 하는 쪽이 우선입니다.

**기본은 한 문장에 한 단락입니다.** 마침표를 찍었으면 빈 줄을 넣고 다음 줄로 갑니다. 짧아서 혼자 두기 애매한 문장만 앞뒤와 붙입니다.

```
✗  스피드 레이스는 같은 시드로 만든 각자의 보드를 각자 풉니다. 상대가 어디를
   열었는지는 안 보이고, 진행바 숫자만 보입니다. 그런데 만들고 나니 아쉬웠습니다.

✓  스피드 레이스는 같은 시드로 만든 각자의 보드를 각자 풉니다.

   상대가 어디를 열었는지는 안 보이고, 진행바 숫자만 보입니다.

   그런데 만들고 나니 아쉬웠습니다.
```

⚠ **줄바꿈은 반드시 빈 줄이어야 합니다.** `remark-breaks` 를 안 쓰기 때문에 한 번 개행한 것은 화면에서 한 문단으로 이어 붙습니다. 이걸 바꾸려면 티스토리에서 옮겨온 116개가 같이 영향을 받으니 건드리지 마세요.

- **한 단락은 서너 줄을 넘기지 않습니다.** 화면에서 글자 벽이 되면 읽다가 놓칩니다.
- **소제목(`##`)으로 덩어리를 나눕니다.** 긴 글이면 특히. 나중에 본인이 다시 찾아 읽을 때 목차가 됩니다.
- **화면·에러·결과는 글로 설명하지 말고 사진을 붙입니다.** "이런 화면이 나왔습니다" 다음에 이미지 한 장이 문단 셋보다 낫습니다.
- 이미지는 `public/images/<slug>/` 에 두고 `![설명](/images/<slug>/파일명.png)` 로 씁니다. 티스토리에서 옮겨온 404장이 이 규칙을 따르고 있습니다.
- **alt 는 채웁니다.** 이미지가 안 뜰 때 남는 유일한 단서입니다.
- 코드가 길면 스니펫으로 자릅니다. 파일 전체를 붙여넣지 않습니다.

### 마크다운 함정 하나

한국어에서 `**"따옴표"**여야` 처럼 **닫는 `**` 앞뒤가 문장부호와 한글**이면 강조가 안 먹고 별표가 그대로 나옵니다 (CommonMark 규칙). 닫는 `**` 앞은 한글이나 숫자로 끝내세요.

```
✗  **"이번 라운드에 뭘 하면 안 되는가"**여야 합니다
✓  **이번 라운드에 뭘 하면 안 되는가**여야 합니다
```

---

## 3. frontmatter 규약

```yaml
---
title: 제목 (가장 중요한 정보 하나만)
description: 한 문장 요약   # 선택
pubDate: 2026-08-07
author: developer      # AUTHORS 와 일치
project: gridbrawl     # 선택. 만든 것 글에만
draft: false
---
```

**제목은 그 글이 뭔지 바로 보여야 합니다.** 본문과 달리 존댓말로 맞출 필요가 없습니다 — 명사로 끊는 편이 짧고 잘 읽힙니다.

| 이렇게 | 이렇게 말고 |
|---|---|
| `보물찾기 모드 추가` | `보물찾기는 죽는 대신 길을 잃습니다` |
| `타임어택 대결 모드 추가` | `같은 판을 둘이 동시에 푸는 레이스를 붙였습니다` |
| `공유할 수 있는 시드 추가` | `지뢰찾기 판 하나를 숫자 하나로 줄였습니다` |

오른쪽은 읽고 나면 멋있는데, 목록에서 처음 보면 무슨 글인지 모릅니다. **제목에서 비유·반전·요약을 하지 마세요.**

**`description` 은 딱 한 문장입니다.** 마침표를 찍었으면 거기서 끝냅니다 — 뒤에 문장을 더 붙이지 않습니다.

```
✓  51 x 51 한가운데 보물을 두고 누가 더 빨리 찾는 게임
✗  51×51 한가운데 보물을 두고 꼭짓점에서 길을 뚫습니다. 죽지 않는 지뢰찾기를 만들려다 …
```

**제목이 이미 다 말하고 있으면 `description` 을 아예 빼세요.** 같은 말을 늘려 쓴 줄이 하나 더 붙을 뿐입니다.

- **`description` 은 선택입니다.** 없으면 목록 카드에서 그 줄이 통째로 빠집니다. 티스토리에서 옮겨온 116개에는 거의 없습니다 — 억지로 채우지 마세요. 검색엔진에 나갈 meta description 은 없을 때 [`src/lib/excerpt.ts`](src/lib/excerpt.ts) 가 본문 앞부분에서 알아서 뽑습니다 (화면에는 안 보입니다). **SEO 때문에 description 을 채울 필요는 없습니다.**
- `author` 는 `src/consts.ts` 의 `AUTHORS[].id` 와 **반드시** 일치해야 합니다. 안 맞으면 빌드가 깨집니다 (의도한 것 — 오타를 배포 전에 잡습니다).
- `project` 는 `PROJECTS[].id` 와 일치해야 합니다. **저자와 별개입니다** — devlog 글은 보통 `author: developer` + `project: gridbrawl` 둘 다 붙습니다.
- **주소는 `slug` 가 정합니다.** Astro 의 glob 로더는 frontmatter 의 `slug` 를 글의 id 로 씁니다 — `slug: blog-design` 이면 파일명이 `blog-magazine-remake.md` 여도 주소는 `/posts/blog-design` 입니다. 영문 소문자 + 하이픈.
  ⚠ **`slug` 를 고치면 주소가 바뀝니다.** 옛 주소는 그 자리에서 404 가 됩니다 (2026-08-19 에 두 편이 그렇게 옮겨졌습니다). 본문의 사진은 `/images/…` 절대 주소라 그대로 뜹니다.
  ⚠ **글 전부에 `slug` 를 채워 뒀습니다** (2026-08-19 · 값은 파일명과 같습니다). `/admin` 의 "주소" 칸이 필수라, 비어 있으면 열어서 고칠 때 저장이 막힙니다.
- `secret: true` 면 **비밀글**입니다 (§6-3).
- `slug` 는 새 글의 **파일명**도 정합니다 (`config.yml` 의 `slug: '{{fields.slug}}'`). 사진이 올라가는 폴더 이름(`public/images/<주소>/`)도 같은 값입니다.
- **빈 문자열은 "값 없음"으로 봅니다.** `author: ''` 는 통과합니다 (`content.config.ts` 의 `blankAsUndefined`). `/admin` 에서 선택을 골랐다 지우면 키가 사라지지 않고 빈 문자열로 남는데, 그것 때문에 빌드가 깨지면 폰에서는 원인을 알 방법이 없습니다. **오타는 여전히 걸립니다** — `author: studnet` 은 그대로 빌드가 깨집니다.

### 저자 — 이 사이트의 유일한 분류

⚠ **카테고리와 태그는 없습니다.** 2026-08-13 에 둘 다 없애고 저자로 합쳤습니다.
`/categories/*` 와 `/tags/*` 주소도 같이 사라졌고, **되살리지 않습니다.**

**글 하나는 저자 한 명에게 속하고, 그게 전부입니다.**

| | 저자 |
|---|---|
| 개수 | 글마다 **하나** |
| 구조 | 평평함 (트리 없음) |
| 용도 | 누가 쓴 글인가 |
| 화면 | 왼쪽 사이드바 `Editors`, `/authors`, `/authors/<id>` |

지금 있는 저자 (`src/consts.ts` 의 `AUTHORS`):

| id | 이름 | 무엇을 쓰나 | 색 |
|---|---|---|---|
| `student` | study Lim | 배운 것 — 수업 필기, 공부 정리 | 파랑 |
| `developer` | 임데브 | 만든 것 — devlog, 이 블로그를 고친 기록 | 빨강 |

**저자는 화면 요소가 아니라 글의 규약입니다.** 그래서 `AUTHORS` 항목 하나가
보이는 것(`name`·`tagline`·`bio`·`color`)과 **안 보이는 것(`voice`)** 을 같이
들고 있습니다. `voice` 는 그 저자로 글을 쓸 때 지키는 말투이고, 화면에는 안
나옵니다. 두 개를 다른 파일에 두면 소개는 바꿨는데 말투는 그대로인 일이 생깁니다.

저자를 추가할 때 고칠 곳은 **세 군데**입니다.

1. `src/consts.ts` 의 `AUTHORS` — `voice` 와 `initial` 까지 채웁니다
2. `src/styles/global.css` 의 `--who-<id>` — **다크·라이트 두 줄** (§4 참고)
3. `public/admin/config.yml` 의 `author` select

### 저자 얼굴

**사진이 아직 없습니다.** 그래서 `initial` 한 글자를 저자 색 원에 앉혀 씁니다
([`Avatar.astro`](src/components/Avatar.astro)). 빈 원을 두면 준비 중인 것처럼
보여서 채워 둔 것이고, 사진이 생기면 이렇게 갈아끼웁니다.

1. `public/authors/` 에 파일을 넣습니다
2. `AUTHORS[].avatar` 에 `/authors/<파일명>` 을 적습니다

- **사진은 흑백으로 깔립니다** (`filter: grayscale(1)`). 색이 제각각인 사진이 나란히 서면 저자 색보다 사진이 먼저 읽혀서 분류가 흐려집니다. 컬러로 두려면 `Avatar.astro` 의 그 한 줄을 빼세요.
- ⚠ **아바타를 SVG 파일로 뽑지 마세요.** 색이 `--who-*` 라 테마마다 달라야 하는데, 파일로 두면 한쪽 테마에서 대비가 떨어집니다. 그래서 컴포넌트에서 그립니다.
- 원 배경은 저자 색 16% 를 카드색에 섞은 것입니다. 글자가 큰 편이라 대비 3:1 만 넘으면 되는데, 지금은 다크 4.0:1 · 라이트 5.0:1 입니다.

⚠ **글이 0개인 저자는 사이드바에서 숨습니다.** `/authors` 목록에는 나옵니다 —
사이드바는 고르는 곳이고 `/authors` 는 소개하는 곳이라 규칙이 다릅니다.

---

## 4. 디자인 시스템

### 출처

색·radius·아이콘 전부 **limSystem** 에서 가져왔습니다.

```
색/radius   원본: ~/Develop/React/limSystem/src/util/theme.ts
            사본: src/styles/tokens.css           ← 손으로 옮김
아이콘      원본: ~/Develop/React/limSystem/src/assets/icons/*.svg
            사본: src/assets/icons/*.svg           ← npm run icons
            추가: src/assets/icons-extra/*.svg     ← limSystem 에 없어서 손으로 넣음
```

limSystem 은 별도 Next.js 앱(`private: true`)이라 패키지로 못 가져옵니다. **컴포넌트(React)는 Astro 에서 못 씁니다** — 가져올 수 있는 건 값과 SVG 뿐입니다.

**색은 손으로 옮긴 것이라 원본이 바뀌면 `tokens.css` 도 같이 고쳐야 합니다.** 아이콘은 `npm run icons` 로 다시 긁어오면 됩니다.

### 아이콘 — 이모지 대신 씁니다

목록 머리글·사이드바 제목·버튼에는 **limSystem SVG** 를 씁니다. 이모지는 OS마다 그림이 달라서 다크 테마에서 혼자 튀거든요.

```astro
---
import Icon from '../components/Icon.astro'
---
<span class="title-ico"><Icon name="notebook" size={17} /> All Posts</span>
```

- 이름은 파일명 그대로입니다. 목록은 `src/lib/icon-names.ts` (자동 생성).
- 없는 이름을 쓰면 **빌드가 깨집니다.** 의도한 것입니다 — 오타를 배포 전에 잡습니다.
- 원본 SVG 는 색이 `#3C3E44` 로 박혀 있는데 `Icon.astro` 가 `currentColor` 로 바꿔 줍니다. 그래서 **부모의 `color` 를 따라갑니다** — 아이콘에 색을 직접 주지 말고 부모에 의미 토큰을 주세요.
- 아이콘 + 글자를 한 줄로 놓을 때는 `.title-ico`(global.css)나 `.side-title` 을 씁니다.

지금 쓰는 것들:

| 자리 | 아이콘 | 어디서 |
|---|---|---|
| Tags | `label` | limSystem |
| Live Service | `book-open` | icons-extra |
| Posts / Devlog | `notebook` | limSystem |
| Profile | `smile` | icons-extra |
| Contact | `chat-circle-dots` | icons-extra |
| 테마 토글 | `moon` / `sun` | icons-extra |
| 웹에서 플레이 | `play-circle` | limSystem |
| App Store 에서 받기 | `download` | limSystem |

### 아이콘이 limSystem 에 없을 때

limSystem 에는 **해·달·말풍선·펼친 책·웃는 얼굴·게임패드·메일·GitHub** 이 없습니다. 두 가지 방법이 있습니다.

1. **뜻이 통하는 다른 아이콘을 고른다** — 있는 걸로 되면 이쪽이 낫습니다.
2. **`src/assets/icons-extra/` 에 넣는다** — 같은 팩(24×24 · `fill="#3C3E44"`)에서 가져온 SVG만 넣으세요. 선 굵기가 다르면 나란히 놨을 때 바로 티가 납니다.

⚠ **`npm run icons` 는 `src/assets/icons/` 를 통째로 지웠다 다시 씁니다.** 손으로 넣은 아이콘을 거기 두면 다음 sync 때 사라집니다. 반드시 `icons-extra/` 에 두세요. 이름이 양쪽에 겹치면 sync 스크립트가 오류로 멈춥니다.

⚠ **파일명은 kebab-case 로 바꿔서 넣습니다.** limSystem 규약이라 `Book_Open.svg` → `book-open.svg` 처럼 맞춥니다.

### 토큰 사용 규칙

**컴포넌트에서 `--lim-*` 원시 값을 직접 쓰지 마세요.** 의미 토큰만 씁니다.

| 의미 토큰 | 용도 |
|---|---|
| `--bg` | 페이지 배경 |
| `--card` | 카드 배경 |
| `--surface` | 알약·인라인코드 배경, hover |
| `--surface-2` | 인용문·표 헤더 배경 |
| `--border` | 선, 구분선 |
| `--text` | 본문 글자 |
| `--text-dim` | 보조 글자 |
| `--text-faint` | 날짜·개수 같은 흐린 글자 |
| `--accent` | 강조 (인용문 왼쪽 선) |
| `--accent-text` | 본문 링크 |
| `--pill-fg` | 색 있는 알약 위의 글자 |
| `--who-<저자id>` | 저자 이름 글자색 |

radius 도 마찬가지입니다. `--radius-sm/md/lg/full` 은 limSystem 원본이고, `--radius-card`(16px)는 morethan-log 풍 카드를 위해 **블로그에서 추가한 확장값**입니다.

### 강조색과 프로젝트 색 — Neon Jungle

**전부 limSystem 400 단계에서 골랐습니다.** 별도 팔레트가 아니라 limSystem 안의 조합이라, 원본이 바뀌면 같이 따라갑니다.

| hue | 토큰 | limSystem | 프로젝트 | 다크 대비 |
|---|---|---|---|---|
| 0° | `--nj-red` | RED_400 | — (비어 있음) | 6.3:1 |
| 33° | `--nj-amber` | ORANGE_300 | — (비어 있음) | 10.1:1 |
| 34° | `--nj-orange` | ORANGE_400 | **BeepTimer** | 7.7:1 |
| 121° | `--nj-green` | GREEN_400 | **MineApp** | 7.8:1 |
| 181° | `--nj-teal` | TEAL_400 | **GridBrawl** | 10.7:1 |
| 209° | `--nj-blue` | BLUE_400 | — (비어 있음) | 6.2:1 |

토큰 여섯은 그대로 두되 **지금 쓰는 건 셋뿐입니다.** 프로젝트를 늘리면 빈 칸에서 골라 쓰세요.

**강조색은 청록입니다.**

| 토큰 | 값 | 쓰임 | 대비 |
|---|---|---|---|
| `--nj-accent` | TEAL_500 | 다크 인용문 선 | 8.4:1 |
| `--nj-accent-text` | TEAL_300 | 다크 링크 | 12.6:1 |
| `--nj-accent-deep` | TEAL_700 | 라이트 공용 | 4.6:1 (흰 배경) |

**청록은 강조색 계열이라 프로젝트에는 GridBrawl 하나만 씁니다.** 대표 프로젝트가 브랜드 색을 공유하는 건 의도한 것이고, 여기에 하나 더 넣으면 어느 게 강조인지 안 읽힙니다.

⚠ **`--nj-orange`(34°)와 `--nj-amber`(33°)는 색상이 1° 차이입니다.** limSystem 에 보라·분홍 계열이 없어서 6개를 전부 다른 계열로 못 뽑았습니다. 명도로 갈리니(7.7:1 vs 10.1:1) **둘을 목록에서 나란히 두지 마세요.** (지금은 amber 를 안 쓰고 있어서 문제가 없습니다.)

프로젝트가 더 늘면 limSystem 의 300/500 단계에서 뽑되, **기존 색과 hue 가 30° 이상 떨어지거나 명도가 확실히 다른 것**을 고르세요.

### 색을 바꿀 때 반드시 확인할 것

파스텔은 배경에 따라 대비가 확 달라집니다. **눈대중하지 말고 계산하세요.**

- **본문 링크(`--accent-text`)는 4.5:1 이상.** 밝은 청록은 흰 배경에서 2.1:1 밖에 안 나와서, 라이트 테마는 진한 쪽(`--nj-accent-deep`)을 따로 씁니다.
- **색 알약 위 글자(`--pill-fg`)는 두 테마 모두 어두운 색.** 알약 배경이 양쪽 다 밝은 파스텔이라, 흰 글자면 1.5~3.0:1 로 떨어집니다.
- **`.live` 배지는 `--card` 를 글자색으로 씁니다.** `--pill-fg` 를 쓰면 라이트 테마에서 어두운 글자 + 어두운 배경이 됩니다.

새 색이 필요하면 `tokens.css` 에 추가하고, `global.css` 에서 의미 토큰으로 연결하세요.

### 저자 색 (`--who-*`) — 프로젝트 색과 쓰임이 다릅니다

⚠ **프로젝트 색은 알약 배경이고, 저자 색은 글자색입니다.** 그래서 `--nj-*`
(400 단계 파스텔)를 저자에 그대로 쓰면 **라이트 테마에서 안 읽힙니다** —
흰 배경에 blue_400 은 2.8:1 밖에 안 나옵니다.

강조색과 같은 방식으로 테마마다 다른 값을 씁니다 (`global.css`).

| 저자 | 다크 | 라이트 | 대비 (다크 / 라이트) |
|---|---|---|---|
| `--who-student` | BLUE_400 | BLUE_700 | 6.5:1 / 8.2:1 |
| `--who-developer` | RED_400 | RED_700 | 6.6:1 / 5.9:1 |

`consts.ts` 의 `AUTHORS[].color` 는 이 이름을 가리킵니다. **원시 `--nj-*` 를
직접 넣지 마세요.** 저자를 늘리면 `global.css` 의 두 블록에 한 줄씩 추가하고
대비를 계산해 보세요 (눈대중 금지 — 400 단계는 라이트에서 거의 항상 떨어집니다).

### 헤더와 메뉴

**이름 하나 + 햄버거 하나입니다** (`Header.astro`). 예전에는 오른쪽에
Editors·Posts·Live Service 가 늘어서 있었는데, 거기에 검색과 테마까지
붙이면 다섯 개가 되어 폰에서 화면 끝에 닿습니다. 그래서 접었습니다.

| | 폰 | 큰 화면 |
|---|---|---|
| 메뉴 판 | 화면 전체 | 오른쪽 절반 (최소 24rem) |

- 판 안에 **검색칸 · EDITORS · POSTS · LIVE SERVICE · 아래 링크**가 다 있습니다.
- 오른쪽 위는 **해/달 → 닫기** 순서입니다.
- ⚠ **판을 열 때 검색칸에 포커스를 주지 마세요.** 폰에서 키보드가 같이 올라와 메뉴의 절반을 덮습니다. 메뉴를 열었다고 검색을 하려는 것은 아닙니다 — 키보드는 검색칸을 눌렀을 때만 올라옵니다. 대신 판 자체(`tabindex="-1"`)로 포커스를 옮겨서, 키보드로 다니는 사람이 판 안에서 이어지게 합니다.
- ⚠ **뒤 지면 잠그기는 폰과 데스크톱이 다릅니다.** iOS 사파리는 `overflow: hidden` 으로 터치 스크롤이 안 막힙니다 — 판을 열고 쓸어내리면 뒤가 그대로 움직입니다. 폰에서는 `body` 를 `position: fixed` 로 못박고 지금 스크롤 위치만큼 위로 올려 둡니다 (닫을 때 그 자리로 되돌립니다). **데스크톱에는 이 방법을 쓰면 안 됩니다** — `body` 가 흐름에서 빠지면서 `sticky` 헤더가 화면 밖으로 나가고, 판이 절반만 덮는 데스크톱에서는 그게 그대로 보입니다.
- ⚠ **읽기 진행 막대는 헤더에만 둡니다.** 페이지에서 한 번 더 그리면 겹칩니다.
- ⚠ **테마 토글은 한 페이지에 하나만.** `#theme-toggle` 을 id 로 찾기 때문에 둘을 놓으면 뒤엣것이 안 먹습니다.

### 검색

정적 사이트라 서버가 없습니다. 빌드할 때 색인을 내보내고
(`src/pages/search.json.ts` → `/search.json`, 83KB) 브라우저가 받아서
걸러냅니다 (`/search`).

- **본문 전체는 안 넣습니다.** 제목·소개문·본문 앞 240자까지입니다. 전체를 넣으면 몇 MB가 되고 검색창을 열 때마다 그걸 받습니다.
- ⚠ **비밀글은 본문에서 아무것도 뽑지 않습니다** (§6-3 과 같은 이유). 제목만 나오고 자물쇠가 붙습니다.
- 형태소 분석 없이 **글자 포함**으로 찾습니다 — "지뢰"로 "지뢰찾기"가 걸립니다.
- 주소의 `?q=` 로 결과를 그대로 주고받을 수 있습니다.

### 홈 지면

홈은 사이드바가 없습니다. 화면 폭을 다 쓰고 네 덩어리로 갑니다.

```
[ 첫 꼭지 ]   폰: 사진 위에 제목 / 큰 화면: 사진 왼쪽 + 제목 오른쪽
[ 저자 묶음 ] study Lim · 개발자 — 표지가 스크롤 중 위에 붙습니다
[ 최근 글 ]   썸네일 + 제목 줄
[ 아래 띠 ]   Editors · Live Service · Contact
```

- 카드 사진은 **폰 3:2 · 큰 화면 4:3** 입니다. 세로(2:3·4:5)로 두니 한 장이 화면의 3분의 1을 먹었습니다.
- 제목은 **두 줄에서 자릅니다.** 길이가 제각각이면 카드 아래가 들쭉날쭉합니다.
- 사진은 **위쪽 기준으로 자릅니다.** 코드·앱 캡처는 위에 정보가 몰려 있습니다.
- ⚠ **첫 꼭지의 글자는 `sticky` 로 붙여 둡니다.** 안 붙이면 사진은 아직 화면에 있는데 글자만 위로 빠져나가서 사진만 덩그러니 남는 구간이 생깁니다. 붙는 위치(`50vh - 9rem`)는 제목 두 줄 기준이라 **세 줄을 넘어가면 값을 키워야** 합니다.
- 스크롤 효과 셋: 커버 떠오르기 · 저자 표지 상단 고정 · 첫 꼭지 패럴랙스(큰 화면만). ⚠ **숨기는 클래스는 스크립트가 붙입니다** — CSS 로 처음부터 숨기면 자바스크립트가 죽었을 때 지면이 통째로 안 보입니다. `prefers-reduced-motion` 도 확인합니다.

### 레이아웃 — 홈 말고 나머지

`/posts`·`/authors`·`/projects` 는 아직 3열입니다.

```
[ (빈 칸) ]   [ 지면 ]   [ Editors / Live Service / Contact ]
   span 2      span 7                 span 3
```

- 12열 그리드, `gap: 1.5rem`, 컨테이너 `max-width: 1120px`
- 1024px 미만에서는 사이드바가 숨고 **저자 목록만 가로 스크롤 한 줄**로 본문 위에 남습니다 (`.tags-mobile`)
- 헤더는 `height: 3rem` sticky

⚠ **왼쪽 열은 일부러 비워 둔 것입니다.** Editors 를 오른쪽으로 옮기면서 비었는데,
열을 지우면 지면이 화면 왼쪽에 붙습니다. 자리만 잡는 빈 `<aside>` 로 남겨 두세요.

⚠ **저자 목록이 두 군데에 있습니다.** 오른쪽 열(`.rt-authors`)은 넓은 화면 전용이고,
폰에서는 본문 위 한 줄(`.tags-mobile`)이 그 역할을 합니다. 폰에서 오른쪽 열은 글
목록 **아래**라 거기 있는 저자 목록은 눌리지 않습니다 — 그래서 한쪽씩만 켭니다.

⚠ **`SiteFooter` 는 홈에서만 씁니다.** 나머지 페이지에는 같은 내용이 오른쪽
열에 이미 있어서, 붙이면 두 번 나옵니다.

### 지면 — 카드가 아니라 줄입니다

⚠ **글 목록에 상자(카드)를 두르지 마세요.** 매거진들(the-edit·longblack·
magazine-b·folin)을 보고 정한 것입니다. 상자를 겹겹이 쌓는 것보다 **큰 제목 +
여백 + 얇은 구분선** 쪽이 글이 100개 넘을 때 덜 지칩니다.

- **꼭지 크기는 전부 같습니다.** 첫 꼭지만 키우는 것(`lead`)을 넣었다 걷어냈습니다 — 매거진 관례지만 목록에서 그 한 줄만 튀어 보였습니다. 다시 넣지 마세요.
- **한 줄에 색은 저자 이름 하나뿐입니다.** 프로젝트 이름은 글자로만 둡니다 — 색이 둘이면 어느 쪽이 분류인지 안 읽힙니다
- 머리글은 아이콘 대신 **letter-spacing 준 대문자 작은 글자**(`LATEST`, `EDITORS`)

### 커버 사진

`heroImage` 를 지정한 글은 **0편**입니다. 그래서 한동안 "커버가 없다"고 보고
타이포그래피로만 지면을 만들었는데, 세어 보니 **124편 중 91편(73%)이 본문에
사진을 쓰고 있었습니다** (티스토리에서 받아온 420장).

그래서 [`src/lib/cover.ts`](src/lib/cover.ts) 가 순서대로 찾습니다.

1. `heroImage` — 손으로 고른 것
2. 본문에 나오는 첫 사진
3. 없음 — 그리는 쪽에서 **저자 색 판**으로 채웁니다

- ⚠ **기본 커버를 그림 파일로 만들지 마세요.** 한 장을 다크·라이트 양쪽에 쓰면 한쪽에서 반드시 튑니다 (어두운 판이 흰 배경에서 검은 사각형이 됐습니다). CSS 로 그리면 테마별로 밝기를 맞출 수 있고, 저자마다 색이 달라 33편이 다 같아 보이지도 않습니다.
- ⚠ **그 판에 글자를 넣지 마세요.** 커버 위에 제목을 얹는 지면에서 겹칩니다 (한 번 넣었다가 뺐습니다).
- ⚠ **비밀글은 본문을 뒤지지 않습니다.** 잠근 본문에서 뽑은 사진을 목록에 띄우면 잠근 의미가 없습니다 (§6-3 과 같은 이유).
- ⚠ **바깥 주소(`http…`)는 커버로 안 씁니다.** 티스토리 CDN 주소는 만료되면 404 입니다 (§5-2).
- 본문 사진은 `public/` 에 있는 원본이 그대로 나갑니다. **크기별로 뽑지 않습니다** — 목록에 사진을 여러 장 띄우기로 정하면 그때 Astro 자산으로 옮겨야 합니다.

### 글이 많아서 생긴 규칙

글이 124편이라 목록을 그냥 다 그리면 페이지가 감당이 안 됩니다.

| 화면 | 무엇을 |
|---|---|
| `/` | **최근 20편**만. 첫 꼭지는 크게, 끝에 "전부 보기" |
| `/posts` | **서고.** 지면이 아니라 한 줄씩, 연도로 묶어서 |
| `/authors` | 저자 소개. 얼굴 + 이름 + 한 줄 (글 0편인 저자도 나옵니다) |
| `/authors/<id>` | 그 저자의 글 전부 |

`/` 의 `RECENT` 상수를 건드릴 때는 서고가 있다는 걸 잊지 마세요 — 첫 화면을 늘리는 것보다 저자로 좁히는 쪽이 낫습니다.

### 폰트

**Pretendard Variable** 한 벌만 씁니다 (`src/assets/fonts/PretendardVariable.woff2`, 100~900).

라이선스는 **SIL OFL 1.1** 이고 `src/assets/fonts/Pretendard-LICENSE.txt` 에 동봉돼 있습니다. **이 파일을 지우지 마세요.** OFL은 폰트를 재배포할 때 라이선스 사본을 같이 넣도록 요구합니다.

---

## 5. 게임 저장소에서 devlog 뽑기 (게임 글을 쓸 때만)

게임 소스는 이 저장소에 없습니다. 전부 별도 비공개 저장소이고, 로컬에서는 형제 디렉토리에 있습니다.

```
~/Develop/Claude/
  ├── blog/            ← 여기
  ├── GridBrawl/       ← 사이트에 올라가 있음 (진행 중)
  ├── MineApp/         ← 사이트에 올라가 있음 (App Store 출시)
  ├── BeepTimer/       ← 사이트에 올라가 있음 (App Store 출시)
  ├── CoupleApp/       ← 안 올림. git 저장소도 아님
  ├── MiniGame_Speeder/  ← 안 올림
  └── TossTreasureHunt/  ← 안 올림
```

⚠ **만든 걸 전부 사이트에 올리지 않습니다.** 지금 보여줄 수 있는 셋(GridBrawl · MineApp · BeepTimer)만 `PROJECTS` 에 있습니다. 나머지 저장소의 글을 쓰려면 먼저 프로젝트를 추가해야 합니다 (§6).

글을 쓸 때는 해당 게임 저장소를 세션에 추가합니다.

```
/add-dir ~/Develop/claude/GridBrawl
```

### 절차

1. **커밋을 읽습니다.** 기억이 아니라 diff에서 출발합니다.
   ```bash
   git -C ~/Develop/claude/GridBrawl log --format='%ad | %s' --date=short -20
   git -C ~/Develop/claude/GridBrawl show <hash>
   ```
2. **글감이 되는 커밋을 하나 고릅니다.** 여러 커밋을 한 글에 욱여넣지 않습니다. "왜"가 있는 커밋 하나가 글 하나입니다.
3. **코드를 실제로 읽습니다.** 스니펫을 넣을 거면 파일을 열어서 확인합니다. **절대 지어내지 않습니다.**
4. `src/content/posts/<slug>.md` 로 씁니다. `author: developer` · `project: <id>`.
5. 사람이 읽고 고칩니다. 초안을 그대로 올리지 않습니다.

---

## 5-2. 티스토리에서 옮겨온 글

2026-08-12 에 `hyuk-todayfeelsogood.tistory.com` 의 글 **116개**를 옮겨왔습니다
(2022-03 ~ 2026-01). 파일 맨 위에 원본 주소가 주석으로 붙어 있습니다.

```
# 티스토리에서 옮겨왔습니다: https://hyuk-todayfeelsogood.tistory.com/122
```

- 옮긴 도구는 [`scripts/import-tistory.mjs`](scripts/import-tistory.mjs) 입니다. 한 번 쓰고 끝이지만, 다시 긁어야 할 때를 위해 남겨뒀습니다.
- 파일명은 **`<옛 카테고리 잎>-<티스토리 글번호>`** 입니다 (`flutter-122`, `swiftui-68`, `sdk-90`).
- 이미지 404장은 `public/images/<slug>/` 에 **받아서** 넣었습니다 (48MB).
  ⚠ **티스토리 CDN 주소를 그대로 쓰면 안 됩니다.** 서명된 URL 이라 `expires` 가 지나면 전부 404 가 되고, 서명을 뗀 주소도 404 입니다. 그래서 받아온 것입니다.
- `description` 은 대부분 비어 있습니다. 원본에 없었기 때문입니다 — 지어내지 말고, 손볼 때 채우세요.
- 티스토리에서 카테고리가 안 달려 있던 글 5개는 제목을 보고 정했습니다. 근거가 약한 곳이라 `import-tistory.mjs` 의 `UNCATEGORIZED` 표에 모아뒀습니다.

---

## 6. 프로젝트 추가

**고칠 곳은 두 군데입니다.**

1. `src/consts.ts` 의 `PROJECTS` 배열
2. `public/admin/config.yml` 의 `project` select — 같은 `id` 를 넣습니다

`PROJECTS` 항목 하나가 페이지 두 개를 만듭니다.

| 경로 | 무엇 | 쓰는 필드 |
|---|---|---|
| `/projects/<id>` | 그 프로젝트의 devlog 목록 | `name` `tagline` `platform` `stack` `color` `active` |
| `/projects/<id>/about` | **소개 페이지** — 받는 곳·플레이하는 곳 | 위 + `summary` `features` `links` |

devlog 페이지 **상단 카드를 누르면 소개 페이지로 갑니다.** 그래서 `summary`·`features`·`links` 를 비워두면 소개 페이지가 껍데기가 됩니다 — 새 프로젝트를 넣을 때 같이 채우세요.

- `summary` — 두세 문장. 뭘 하는 물건인지 + 왜 그렇게 만들었는지.
- `features` — 4~5줄. **구체적으로** 씁니다. "멀티플레이 지원"이 아니라 "방 코드 6자리로 친구를 부르거나, 봇과 연습할 수 있습니다".
- `links` — `kind` 는 `web` | `appstore` | `support`. 버튼 아이콘이 여기서 갈립니다.
- `storeName` — App Store 이름이 저장소 이름과 다를 때만 (예: MineApp → "지뢰찾기 아레나").
- `active` — "지금 받아서 해볼 수 있는가". LIVE 배지가 붙습니다.
- `color` — 글 머리의 프로젝트 알약, Live Service 목록의 점, 소개 페이지 버튼 배경. (저자 색과는 다른 계통입니다 — §4)

**소개 문구는 지어내지 마세요.** 해당 저장소의 `README.md` 나 App Store 설명을 읽고 씁니다. 출시된 앱이면 번들 ID로 스토어 메타데이터를 그대로 확인할 수 있습니다.

```bash
curl -s "https://itunes.apple.com/lookup?bundleId=com.LimJH.BeepTimer&country=kr"
```

---

## 6-2. 폰에서 쓰는 `/admin`

글은 대부분 폰에서 `/admin` 으로 씁니다. Decap CMS 는 데스크톱 기준으로 만들어져 있어서,
폰에 맞추는 건 `public/admin/index.html` 의 `<style>` 한 덩어리가 전부입니다.
클래스 이름은 emotion 이 만든 해시(`css-1fehwpo-AppMainContainer`)라 뒤의 컴포넌트 이름만
부분 일치로 잡습니다 — **Decap 버전을 올리면 여기부터 확인하세요.**

지금 걸려 있는 것들과 그 이유:

| 무엇 | 왜 |
|---|---|
| 입력칸 글자 `16px` | iOS 사파리는 16px 미만 입력칸을 탭하면 화면을 확대합니다. Decap 기본값이 15px 이라 필드마다 확대됐습니다 |
| 사이드바 대신 검색창만 | 검색이 사이드바 안에 있습니다. 사이드바를 통째로 숨기면 검색도 같이 사라집니다 — 글이 124편이라 검색 없이는 못 찾습니다 |
| 필드 간격 36 → 18px | 필드가 열 개라 기본 간격이면 스크롤이 한참입니다 |
| 본문 `min-height: 60vh` | 기본 300px 이면 대여섯 줄 만에 차고, 그때부터 작은 창 안에서 스크롤하며 쓰게 됩니다 |
| 사진 고르는 창 줄이기 | Decap 미디어 라이브러리가 920×576 고정입니다. 안쪽 줄들도 400px 로 박혀 있어서, 창만 줄이면 검색창과 "업로드"가 잘려 나갑니다 |
| 툴바 숨김 + `margin-top: 0` | **지금은 안 쓰이는 fallback 입니다** (본문 위젯을 갈아끼웠습니다 — 아래 참고). editor.js 가 죽어서 Decap 기본 편집기로 떨어질 때만 쓰입니다 |
| `white-space: nowrap` | "콘텐 츠""빠른 추 가""현재시 각"처럼 두 줄로 꺾였습니다 |
| 토글 두 개 `required: false` | Decap 의 boolean 은 **값이 없으면 "필수인데 안 채웠다"로 봅니다.** 손으로 쓴 글에는 `secret` 키가 없어서, 열어서 고치면 저장이 막히고 토글을 켰다 꺼야만 풀렸습니다 |
| 토글은 상자에서 꺼냄 | Decap 은 위젯 종류를 안 가리고 전부 같은 상자에 앉힙니다. 스위치는 20px 짜리라 화면 폭을 다 쓰는 상자 안에 점 하나가 떠 있게 됩니다 (`:has([role='switch'])` 로 잡습니다 — iOS 15.4+) |

색과 폰트는 블로그에서 그대로 옮겨왔습니다 (`tokens.css` 의 값을 손으로 적었습니다).
**limSystem 원본이 바뀌면 `tokens.css` 와 `admin/index.html` 둘 다 고쳐야 합니다.**
폰트도 마찬가지로 `public/fonts/PretendardVariable.woff2` 에 한 벌 더 있습니다 —
`src/assets/fonts/` 쪽은 Astro 가 해시 붙은 주소로 내보내서 정적 파일인 `/admin` 에서는
못 가져옵니다. 폰트를 갈아끼우면 두 곳 다 바꾸세요 (라이선스 사본도 같이 뒀습니다).

⚠ `/admin` 은 **OS 다크모드만 따라갑니다.** 블로그처럼 테마를 직접 고르는 토글은
없습니다 — Decap 이 만드는 DOM 이라 버튼을 붙일 자리가 없습니다.

### 본문 편집기 — tiptap 입니다

**Decap 의 markdown 위젯을 통째로 갈아끼웁니다.** 쓰는 화면이 곧 결과인
편집기(tiptap)에 직접 만든 툴바를 붙였습니다.

Decap 것은 **양쪽 다 못 씁니다** — 리치 텍스트는 폰에서 한글 입력이 끊기고,
원문 모드는 툴바 버튼이 전부 `disabled` 로 렌더됩니다.

```
admin-src/editor.js     ← 고치는 곳 (소스)
     ↓ npm run admin (esbuild)
public/admin/editor.js  ← 실제로 불러오는 것. 저장소에 커밋합니다
```

**커밋된 번들을 지우지 마세요.** `dev`·`build` 가 먼저 `npm run admin` 을
돌리지만, Vercel 이 `astro build` 를 직접 부르면 그 단계가 안 돕니다.
그때는 커밋된 파일이 그대로 나갑니다.

저장되는 건 여전히 **마크다운 원문**입니다. 글 124편이 마크다운 파일이라
형식을 못 바꿉니다. 변환은 `@tiptap/markdown` 이 하고, 이 블로그에만 있는
것(글자 색·형광펜)은 `admin-src/editor.js` 에서 마크로 정의해 왕복시킵니다.

**⚠ 왕복은 공짜가 아닙니다.** 마크다운 → tiptap → 마크다운을 거치면
목록 표시(`-   ` → `- `)나 구분선(`* * *` → `---`) 이 tiptap 방식으로 다시
쓰입니다. 한 글자만 고쳐도 파일 전체가 바뀐 것으로 남습니다.

그래서 **글을 열 때 한 번 되돌려 보고, 원본과 다르면 원문 모드로 엽니다.**
직접 쓴 글은 tiptap 이 만든 그대로라 서식 모드로 열리고, 티스토리에서
옮겨온 글은 원문으로 열립니다. "서식" 버튼으로 언제든 바꿀 수 있습니다.

```bash
node scripts/check-md-roundtrip.mjs          # 어떤 글이 안 돌아오는지
node scripts/check-md-roundtrip.mjs --diff   # 달라지는 줄까지
```

지금 기준으로 **123개 중 116개가 모양이 달라지고, 76개는 글자까지 달라집니다.**
대부분 `&gt;` 로 바뀌거나 벌거벗은 주소가 `[주소](주소)` 로 늘어나는 것이지만,
본문에 `<label>` 처럼 쓴 글자는 **없어집니다.** 옛 글을 서식 모드로 고칠
때는 이걸 알고 하세요.

- **확장을 뺄 때 조심하세요.** 표(`TableKit`)를 안 넣었더니 표가 있는 글을 열었다 저장할 때 표가 통째로 사라졌습니다. StarterKit 에 없는 것은 직접 넣어야 합니다.
- **글자 색은 `<span class="c-teal">` 처럼 클래스로 넣습니다.** 색 값을 본문에 박으면 다크·라이트 한쪽에서 반드시 안 읽힙니다. 실제 색은 `global.css` 가 테마별로 고릅니다 — **색을 늘리려면 `admin-src/editor.js` 의 `COLORS`, `global.css` 의 `.prose .c-*`, `admin/index.html` 의 편집기용 `.c-*` 셋을** 같이 고쳐야 합니다.
- **형광펜은 `<mark>` 입니다.** tiptap 기본값은 `==글자==` 인데 이 블로그의 remark 는 그 표기를 모릅니다.
- **사진은 `public/images/<주소>/` 로 올라갑니다.** 티스토리에서 옮겨온 404장과 같은 자리입니다 (§2). `config.yml` 의 컬렉션 `media_folder` 에 `{{fields.slug}}` 로 걸어 뒀습니다 — 그래서 **주소를 먼저 적어야** 사진 버튼이 열립니다 (안 적었으면 막고 알려줍니다).
- ⚠ **Decap 은 `h1…h6, p` 에 글자색을 전역으로 깝니다.** 편집기 안에서 색을 다시 주지 않으면 어두운 배경에 어두운 글자가 됩니다.
- ⚠ **editor.js 가 안 불러와지면 Decap 기본 raw 편집기로 떨어집니다.** 툴바만 없어지고 글은 계속 쓸 수 있습니다. `config.yml` 의 `modes: ['raw']` 와 index.html 의 `.cms-editor-raw` 규칙은 그때를 위해 남겨 둔 것입니다.
- ⚠ **Decap 은 React 를 밖으로 내보내지 않습니다.** 그래서 `React.Component` 를 이미 등록된 위젯(`CMS.getWidget('string').control`)의 프로토타입에서 꺼내 씁니다. Decap 을 올리면 여기부터 확인하세요.

`config.yml` 쪽에서 알아둘 것:

- **`주소`(slug) 필드가 필수입니다.** 제목에서 파일명을 뽑으면 한글 주소가 나옵니다. `^[a-z0-9]+(-[a-z0-9]+)*$` 로 막아 뒀습니다. 사진 폴더 이름도 이 값입니다.
- **기본 정렬은 `index.html` 의 인라인 스크립트가 정합니다.** Decap 3.9 는 `sortable_fields` 에 `default` 를 넣으면 "must be an array"로 설정을 통째로 안 읽습니다. `decap-cms.entries.sort` 를 처음 한 번만 채워 최신 글이 위로 오게 합니다.
- **폼을 고쳤는데 폰에서 안 바뀌면** `index.html` 의 `config.yml?v=` 숫자를 올리세요. `editor.js?v=` 도 같은 이유로 붙어 있습니다.

---

## 6-3. 비밀글

`secret: true` 를 붙이면 **본문이 잠깁니다.** `/admin` 에는 "비밀글로 두기" 토글이 있습니다.

**제목·날짜·요약·저자는 그대로 보입니다.** 목록에 자물쇠만 붙습니다. 티스토리 보호글과
같은 규칙이고, 감출 내용은 **본문에만** 쓰라는 뜻입니다. RSS 에서는 통째로 빠집니다 —
피드 리더는 비밀번호를 물어봐 주지 않으니 제목만 흘리는 항목이 남기 때문입니다.

### 비밀번호

글마다 다르지 않습니다. `SECRET_POST_PASSWORD` 하나로 전부 잠급니다 — 글마다 다르면
어딘가에 적어둬야 하고, 그러면 저장소에 비밀번호가 남습니다.

배포는 **Vercel → Settings → Environment Variables** 에 `SECRET_POST_PASSWORD` 로 넣습니다
(Production · Preview · Development 전부). 환경변수는 새 빌드부터 적용되니 넣은 뒤 한 번
다시 배포해야 합니다.

로컬에서도 비밀글을 보려면 저장소 맨 위에 `.env` 를 만들고 **배포와 같은 값**을 적습니다.
값이 다르면 로컬에서 잠근 글을 배포본에서 못 엽니다. `.env` 는 gitignore 돼 있습니다.

```bash
echo 'SECRET_POST_PASSWORD=여기에비밀번호' > .env
```

⚠ **비밀글이 있는데 이 값이 없으면 빌드가 멈춥니다.** 그냥 넘어가면 "비밀글"이라고
써놓고 본문은 다 보이는 상태로 배포됩니다. 그게 제일 나쁩니다.

### 어떻게 잠기는가

"JS로 가려두기"는 비밀글이 아닙니다 — 페이지 소스에 본문이 그대로 있습니다. 정적
사이트라 서버에서 막을 수가 없으니, **본문 자체를 암호문으로 내보냅니다.**

| 단계 | 어디 |
|---|---|
| 빌드할 때 잠금 (PBKDF2-SHA256 20만 회 → AES-GCM) | [`src/lib/secret.ts`](src/lib/secret.ts) |
| 읽는 사람 브라우저에서 풀기 | [`SecretGate.astro`](src/components/SecretGate.astro) 의 `<script>` |

잠글 대상은 `post.rendered.html` 입니다 — Astro 가 마크다운을 렌더해 엔트리에 담아둔
것으로, 코드 하이라이팅까지 끝난 상태입니다. 컨테이너 API 로 다시 그릴 필요가 없습니다.

- **비밀번호 확인 코드가 따로 없습니다.** 틀리면 AES-GCM 이 인증 태그에서 걸려
  예외를 던집니다. 못 풀면 틀린 겁니다.
- 한 번 열면 브라우저 `localStorage` 에 남아서 다음부터 안 묻습니다. 본문 아래
  "이 기기에서 비밀번호 지우기"로 지웁니다. 비밀번호를 바꾸면 옛 값은 자동으로 지워집니다.
- `crypto.subtle` 은 **보안 컨텍스트(https·localhost)에서만** 있습니다. 배포는 https 라 괜찮습니다.

⚠ `src/lib/secret.ts` 를 클라이언트 코드에서 import 하지 마세요. 비밀번호가 번들에
박혀서 나갑니다. `<script>` 안에는 암호문만 넘어갑니다.

---

## 6-4. 폰으로 보는 블로그

- **오른쪽 열(Live Service·Contact)은 폰에서 글 목록 아래로 갑니다.** 예전에는 1024px
  미만에서 통째로 숨겼는데, 그러면 폰으로 들어온 사람은 만든 것과 연락처를 볼 방법이
  없었습니다. `FeedLayout.astro` 의 `.rt` — sticky 관련 속성은 데스크톱 미디어쿼리 안에만 둡니다.
- **저자 목록 한 줄은 화면 양끝까지 흘립니다** (`margin-inline: -1rem`). 안쪽에서 딱
  끊기면 그게 끝인 줄 알거든요. 잘린 항목이 보여야 옆으로 더 있다는 게 읽힙니다.
- **본문 리듬은 640px 아래에서 좁힙니다** (`global.css`). 티스토리에서 옮겨온 글은 한 줄이
  곧 한 문단이라, 데스크톱 간격을 그대로 쓰면 한 화면에 열 줄쯤밖에 안 들어갑니다.

---

## 7. 명령어

```bash
npm run dev       # http://localhost:4321
npm run build     # 빌드 (frontmatter 스키마 검증 포함)
npm run preview   # 빌드 결과 확인

npm run post                     # 빌드 검증 → 커밋 → push
npm run post -- "넉백 버그 수정"    # 커밋 메시지 지정

npm run cms       # Decap 로컬 서버 (GitHub 없이 /admin 테스트)

npm run admin     # /admin 편집기 다시 묶기 (admin-src → public/admin/editor.js)
node scripts/check-md-roundtrip.mjs   # 마크다운 왕복 검사

npm run icons     # limSystem 아이콘 다시 긁어오기

node scripts/import-tistory.mjs --dry    # 티스토리 이관 (한 번 쓰고 끝났습니다)
```

`npm run icons` 는 `~/Develop/React/limSystem/src/assets/icons` 를 `src/assets/icons` 로 통째로 덮어쓰고 `src/lib/icon-names.ts` 를 다시 만듭니다. `src/assets/icons-extra/` 는 건드리지 않습니다. 경로가 다르면 인자로 넘기세요 — `node scripts/sync-icons.mjs <경로>`.

`draft: true` 인 글은 `dev` 에서는 보이고 `build` 에서는 빠집니다.

`npm run post` 는 **빌드가 깨지면 push 하지 않습니다.** frontmatter 오타가 배포된 뒤에 발견되는 걸 막기 위해서입니다.

---

## 8. 고친 것 기록

새 항목은 **위에** 붙입니다. 한 작업에 서너 줄이면 충분합니다 — 자세한 건 커밋에 있습니다.

### 2026-08-24 · 홈 카드에 마우스를 올리면 테두리가 서던 것

저자 색 2px 테두리(`outline`)가 커버 밖으로 둘러쳤는데, 저자 표지가 화면 위에
붙어 있는 구간에서는 그 선이 `All` 옆까지 올라온 것처럼 보였습니다.

- **테두리를 뺐습니다.** 제목에 밑줄이 그어지는 것만으로 어느 칸을 가리키고 있는지 충분히 읽힙니다.
- ⚠ **다시 넣지 마세요.** 목록에 상자를 두르지 않기로 한 것(§4)과 같은 이유입니다 — hover 때만 생기는 테두리도 결국 카드 윤곽입니다.

### 2026-08-13 · 카테고리·태그를 없애고 저자로 합쳤습니다 (매거진화)

분류가 세 겹이었습니다 — 카테고리 트리 15개, 태그, 그리고 프로젝트. 그런데
태그는 124편 중 8편에만 붙어 있었고, 카테고리는 주제(React·SwiftUI)로 잘려
있어서 "이 글이 어떤 성격인가"는 어디에도 없었습니다.

- **저자 하나로 합쳤습니다.** 트리도 태그도 없습니다. `/categories/*` 15개와 `/tags/*` 11개 주소가 사라졌고 **되살리지 않습니다** — 색인이 막 시작된 참이라 잃을 게 적다고 보고 그냥 버렸습니다. Search Console 은 나중에 다시 붙입니다.
- **iOS 를 공부에 합쳤습니다.** 학습 기록 113편이 전부 `study Lim` 입니다. iOS·React·Flutter 를 저자로 쪼개면 "주제별 태그"를 이름만 바꾼 것이 됩니다 — 저자는 주제가 아니라 성격으로 나눕니다.
- **BeepTimer 글 3편만 `개발자` 로 옮겼습니다.** 만든 앱의 devlog 라 학습 기록과 성격이 다릅니다. 옮기면서 `project: beeptimer` 도 같이 붙였습니다.
- ⚠ **이름과 `id` 가 다릅니다.** 화면은 `study Lim`·`개발자` 인데 주소는 `/authors/student`·`/authors/developer` 입니다. 이름은 바꿀 수 있지만 `id` 를 바꾸면 글 124편의 frontmatter 와 주소가 같이 움직입니다 — 이름만 고치세요.
- ⚠ **저자가 한 사람이라는 것을 사이트 어디에도 쓰지 않기로 했습니다.** 기획 단계에서 밝히는 쪽을 권했지만 안 밝히는 것으로 정해졌습니다. 자동으로 문구를 넣지 마세요.
- **색은 새 계통(`--who-*`)을 만들었습니다.** 프로젝트 색(`--nj-*`)은 알약 **배경**인데 저자 색은 **글자색**이라, 400 단계를 그대로 쓰면 라이트 테마에서 2.8:1 로 떨어집니다. 강조색처럼 테마별로 다른 값을 씁니다.
- **목록에서 카드를 걷어냈습니다.** 큰 제목 + 구분선입니다. 안 한 것: **사진 표지** — `heroImage` 가 0장이라 지금 넣으면 124편 전부가 빈 자리가 됩니다.
- **Works 를 Live Service 로 바꿨습니다.** 이름만 바뀌었고 `PROJECTS` 는 그대로입니다.
- **`/authors` 에 얼굴을 넣었습니다.** 사진이 없어서 글자 아바타로 채웠습니다 — 자세한 건 §3. 머리글은 `Editors` 하나만 둡니다 (설명 문장을 붙였다 뺐습니다).
- **Editors 를 오른쪽으로 옮기고 왼쪽은 비웠습니다.** 저자 목록이 왼쪽 끝에 혼자 있으면 지면과 멀어서, 프로젝트·연락처와 한 열에 모았습니다.
- **Profile 카드를 뺐습니다.** 저자 이름으로 굴러가는 지면에서 오른쪽에 얼굴과 실명 소개가 따로 서 있으면 두 체계가 겹칩니다. `OWNER` 는 Contact 링크와 저작권 줄에만 남았고, `role`·`bio` 는 쓰는 데가 없어져서 지웠습니다. ⚠ `public/avatar.svg` 는 이제 아무 데서도 안 씁니다 — `avatar-512.png` 는 공유 카드 기본 이미지라 계속 필요합니다.

### 2026-08-13 · 글 상세에 사이드바를 붙였다가 뺐습니다

넓은 화면에서 글 양옆이 400px 넘게 비어서, 오른쪽에 Profile·Works·Contact 를
붙이고 글 폭을 832 → 912px 로 넓혀 봤습니다. **써 보고 예전이 낫다고 판단해
되돌렸습니다**(`cfeef0b` → `faa8ac9`).

⚠ **같은 제안을 다시 하지 마세요.** 글 상세는 `max-width: 56rem` 한 열입니다.
양옆이 비어 보이는 건 알고 남겨둔 것입니다 — 읽는 화면에는 글만 있는 편이 낫습니다.
정말 다시 손대야 하면 되돌린 커밋에 구현이 통째로 남아 있습니다.

### 2026-08-13 · `/admin` 본문에 툴바를 붙였습니다

본문 칸에 글자만 칠 수 있었습니다. 제목·굵게·색은 마크다운을 외워서 손으로
쳐야 했고, **사진은 아예 못 넣었습니다.**

- **Decap 위젯을 갈아끼웠습니다** (`public/admin/editor.js`). 리치 텍스트는 폰에서 한글이 깨져서 못 쓰고, 원문 모드는 툴바가 전부 `disabled` 라 못 씁니다 — 양쪽 다 막혀 있어서 직접 만들었습니다. 자세한 건 §6-2.
- **저장되는 건 여전히 마크다운 원문입니다.** 형식을 바꾸면 글 117개와 갈라지고, 티스토리에서 옮겨온 것들을 다시 손봐야 합니다. 툴바는 커서 자리에 마크다운을 꽂아 넣기만 합니다.
- **색은 값이 아니라 클래스로 넣습니다.** `#58dddf` 를 본문에 박으면 라이트 테마에서 안 읽힙니다. 다섯 색 모두 두 테마에서 4.5:1 이상 나오게 `global.css` 에서 짝을 맞춰 뒀습니다.
- **사진은 `public/images/<주소>/`** — 손으로 쓰던 규칙(§2)에 맞췄습니다. 그래서 주소를 안 적었으면 사진 버튼이 안 열립니다.
- 안 한 것: **미리보기**. 마크다운을 그려 보려면 파서를 하나 더 얹어야 하는데, 폰 화면에서 반을 미리보기에 내주는 게 남는 장사인지 모르겠습니다.

### 2026-08-13 · 탭 아이콘이 아직 Astro 기본값이었습니다

탭에 뜨던 건 Astro 스타터의 "A" 아이콘이었습니다. 예전 티스토리/morethan-log
쪽에서 쓰던 **Lim 원형 로고**(`~/Develop/React/morethan-log/public/favicon.ico`)로
되돌리고, 폰에서 홈 화면에 추가할 때도 같은 로고가 나오게 했습니다.

- **`public/favicon.svg` 를 지운 게 핵심입니다.** 브라우저는 SVG 아이콘이 걸려 있으면 `.ico` 보다 그쪽을 먼저 씁니다 — 남겨두면 `.ico` 만 바꿔도 탭은 그대로입니다. 대신할 SVG 로고 원본이 없어서 `.ico` + PNG 조합으로 갔습니다.
- **홈 화면 아이콘은 `.ico` 안의 48×48 을 키워서 만들었습니다** (`apple-touch-icon` 180 · `icon-192` · `icon-512`). 원본이 그것뿐이라 512 는 눈에 띄게 뭉갭니다. 벡터나 큰 원본이 나오면 그때 다시 뽑는 게 맞습니다.
- 로고 바깥은 투명인데 **PNG 는 흰 배경에 얹어 내보냈습니다.** iOS 는 투명한 아이콘 뒤를 검게 깔아서, 그대로 두면 검은 원이 배경에 묻힙니다.
- `site.webmanifest` 를 새로 넣었습니다. `theme_color` 는 다크 배경(`--lim-gray-990`)에 맞췄습니다 — 기본 테마가 다크라서 라이트 값으로 두면 상단 바만 하얗게 뜹니다.

### 2026-08-19 · `/admin` 에서 옛 글을 고치면 저장이 막히던 것

"필수 필드를 놓치셨습니다" 가 뜨고, **비밀글 토글을 켰다 꺼야만** 저장이
됐습니다. 걸리는 필드는 둘이었습니다.

- **비밀글로 두기** — Decap 의 boolean 은 값이 아예 없으면(frontmatter 에 키가 없으면) 필수 검사를 통과 못 합니다. 토글을 만지면 그제야 `false` 가 들어가서 풀린 것이었습니다. `required: false` 를 붙였습니다 — 없으면 false 라는 뜻이고 스키마도 그렇게 봅니다. `draft` 도 같이 걸릴 수 있어 같이 붙였습니다.
- **주소** — 손으로 쓴 글에는 `slug` 키가 없었습니다. 이건 필수를 풀면 안 됩니다(새 글의 파일명을 이 값으로 만듭니다). 그래서 **글 126편에 `slug` 를 채웠습니다** — 값은 파일명과 같아서 주소는 하나도 안 바뀝니다.
- ⚠ **알게 된 것: `slug` 는 버려지는 키가 아니라 주소 그 자체입니다.** §3 에 "Astro 스키마가 모르는 키라 버린다"고 적어 뒀던 것은 틀렸습니다 — glob 로더가 그 값을 글의 id 로 씁니다. `/admin` 에서 주소를 고친 글 두 편이 이미 옛 주소에서 404 입니다.
- 재현하고 고친 뒤 로컬 Decap 으로 실제 저장까지 확인했습니다("변경사항 저장됨", 경고 없음).

### 2026-08-14 · 이름·메뉴·푸터 다듬기

- **개발자 → 임데브.** 화면 이름만 바꿨습니다. `id` 는 `developer` 그대로입니다 — 바꾸면 글 124편의 frontmatter 와 주소가 같이 움직입니다 (§3).
- **Live Service → Service.** 화면에 보이는 이름만 바꿨습니다. `PROJECTS` 는 그대로입니다.
- **`/authors` 에서 오른쪽 열을 걷어냈습니다.** 저자를 소개하는 자리에 옆에서 같은 저자 목록이 또 나오면 무엇을 보라는 것인지 알 수 없습니다.
- **푸터에서 글 수와 LIVE 배지를 뺐습니다.** 맨 아래는 가는 곳을 알려주는 자리라 숫자가 필요 없습니다. 메뉴 판에는 남겨 뒀습니다 — 거기서는 고르는 데 도움이 됩니다.
- **메뉴 판이 미끄러져 들어옵니다.** ⚠ 여닫는 것을 `hidden` 이 아니라 클래스로 합니다 — `hidden` 은 `display:none` 이라 중간 상태가 없어서 움직임을 그릴 수가 없습니다. 닫힌 동안에는 `visibility` 로 감춥니다. 화면 밖에 두기만 하면 탭 키가 거기로 들어갑니다.

### 2026-08-14 · 홈을 새로 만들었습니다 (시안 D 적용)

시안 넷과 헤더 셋을 만들어 보고 **D + 헤더1** 로 정해 홈에 넣었습니다.
`/preview/*` 와 `Preview*` 컴포넌트는 전부 지웠습니다 — 고른 뒤에도 남겨
두면 다음에 볼 때 어느 것이 진짜인지 알 수 없습니다.

- 시안용이던 것 셋만 이름을 바꿔 남겼습니다 — `Cover.astro`(커버 그림), `SiteFooter.astro`(아래 띠), 그리고 헤더.
- **홈만 사이드바가 없습니다.** 나머지는 그대로 3열입니다 (§4).
- **옛 카테고리는 안 되살렸습니다.** 메뉴의 큰 항목은 저자·프로젝트입니다. 되살리려면 124편의 frontmatter 를 다시 손대야 하는데, 저자 하나로 합친 지 하루밖에 안 됐습니다.
- 남은 것: **사진을 크기별로 뽑기.** 커버가 `public/` 원본 그대로라 폰에서 무겁습니다.

### 2026-08-14 · 이름을 lim's World 로 · 검색 · 헤더 시안

- **사이트 이름을 바꿨습니다.** `SITE_TITLE` 한 곳이 화면 전부를 덮지만, 홈 화면에 추가할 때 뜨는 이름(`site.webmanifest`)과 `/admin` 탭 제목은 따로라 같이 고쳤습니다. **주소(`lim-it.vercel.app`)와 저장소 이름은 그대로입니다.**
- **검색을 붙였습니다.** 정적 사이트라 서버가 없어서, 빌드할 때 색인(`/search.json`)을 내보내고 브라우저가 받아서 걸러냅니다. 83KB 입니다.
  - ⚠ **본문 전체는 안 넣습니다.** 제목·소개문·본문 앞 240자까지입니다. 전체를 넣으면 몇 MB가 되고 검색창을 열 때마다 그걸 받게 됩니다.
  - ⚠ **비밀글은 본문에서 아무것도 뽑지 않습니다.** 색인에 실으면 주소만 열어도 다 읽힙니다 (§6-3).
  - 형태소 분석 없이 **글자 포함**으로 찾습니다. 한국어는 이걸로 충분합니다 — "지뢰"로 "지뢰찾기"가 걸립니다.
- **헤더 시안 셋을 만들었습니다** (`/preview/header-a·b·c`). 지면은 D 그대로 두고 헤더만 갈아 끼웁니다. 그러려고 D의 지면을 `PreviewHome.astro` 로 떼어냈고, `BaseLayout` 에 `hideHeader` 를 붙였습니다.

### 2026-08-14 · D 를 다듬었습니다 — 색·카드·스크롤

D 로 정하고 세 가지를 손봤습니다.

- **색은 안 늘렸습니다.** 저자 색(`--who-*`)이 놓이는 자리만 늘렸습니다 — 표지 밑줄, 카드 hover 테두리, 그리고 사진 없는 글의 커버. 색 체계가 이미 둘(저자·프로젝트)이라 셋째를 만들면 어느 색이 분류인지 안 읽힙니다.
- **사진 없는 커버를 저자 색 판으로 바꿨습니다.** 그림 파일을 지웠습니다 — 자세한 이유는 §4.
- **카드**: 제목을 두 줄에서 자르고(길이가 제각각이면 아래가 들쭉날쭉합니다), 사진은 **위쪽 기준으로 자릅니다**(코드·앱 캡처는 위에 정보가 몰려 있습니다).
- **스크롤 넷**: 커버 떠오르기 · 저자 표지 상단 고정 · 첫 꼭지 패럴랙스(큰 화면만) · 진행 막대. 라이브러리 없이 IntersectionObserver 로 했습니다.
- ⚠ **숨기는 클래스는 스크립트가 붙입니다.** CSS 로 처음부터 숨기면 자바스크립트가 죽었을 때 지면이 통째로 안 보입니다. `prefers-reduced-motion` 도 확인합니다.
- ⚠ **패럴랙스는 틀이 아니라 안쪽 사진을 움직입니다.** 틀을 밀면 아래에 빈 틈이 생깁니다 (한 번 그렇게 만들었다가 고쳤습니다).

### 2026-08-14 · 홈 디자인 시안 세 개 (임시)

홈이 밋밋하다는 이야기가 나와서 롱블랙·폴인·디에디트를 다시 봤습니다.
셋의 공통점은 **꼭지마다 사진 한 장**이었고, 그게 이 블로그에 없던 것입니다.

- **커버가 없는 게 아니었습니다.** `heroImage` 는 0편인데 본문에 사진이 있는 글이 91편이었습니다. 첫 사진을 커버로 쓰기로 했습니다 (§4).
- **시안을 말로 설명하지 않고 페이지로 만들었습니다** — `/preview/a·b·c`. 진짜 글 데이터로 그려서 폰에서 비교할 수 있습니다. ⚠ **고르고 나면 `src/pages/preview/`·`PreviewNav`·`PreviewCover`·robots.txt 의 Disallow 를 같이 지웁니다.**
- 참고 사이트는 눈대중하지 않고 폰 크기에서 재 봤습니다 — 디에디트는 커버가 화면을 꽉 채우고 제목 36px/800, 목록 사진은 2:3. 폴인은 4:3 카드 + 1:1 작은 썸네일. 롱블랙은 하루 한 편을 세우고 아래에 가로 스크롤 묶음.
- 알게 된 것: **작은 썸네일(88px)에서는 코드 캡처가 아무것도 안 알려줍니다.** 사진을 작게 여러 장 늘어놓는 구성(C)은 이 블로그 사진과 안 맞습니다.
- **D 를 더했습니다** — A의 첫 꼭지·세로 사진 격자 + B의 저자별 구분 + C의 최근 글 목록. 저자 이름을 지면을 나누는 표지로 쓰기 때문에 A보다 크게 씁니다.
- **D 로 정했습니다.** 이어서 색·카드·스크롤을 다듬었습니다 — 아래 항목 참고.

### 2026-08-13 · 카테고리·프로젝트 값의 첫 글자가 잘리던 것

"게임 개발"이 "베임 개발"로, "MineApp"이 "VlineApp"으로 보였습니다.

react-select 가 **보이지 않는 입력칸을 값 글자와 같은 칸에 겹쳐** 두는데
(둘 다 `grid-area: 1/1`), 다크모드용으로 입력칸에 배경을 칠한 규칙이 여기까지
닿아서 그 4px 이 첫 글자 위에 얹혔습니다. 잘린 게 아니라 덮인 것이었습니다.

- 눈으로 찾기 어려운 종류라 **화면을 재 보고 찾았습니다** — 글자 상자와 입력칸의 좌표가 똑같이 x=44 였습니다. 비슷한 증상이 또 나오면 위치부터 재 보세요.

### 2026-08-13 · `/admin` 편집기를 tiptap 으로 바꿨습니다

마크다운을 손으로 쳐야 하고, 쓴 것이 어떻게 보일지는 게시해 봐야 알았습니다.
쓰는 화면이 곧 결과가 되도록 tiptap 으로 갈아끼웠습니다.

- **저장 형식은 안 건드렸습니다.** 글 117개가 마크다운 파일이라, 편집기만 바꾸고 파일은 그대로 둡니다. 자세한 건 §6-2.
- **옛 글은 원문 모드로 엽니다.** 왕복시키면 목록·구분선 표기가 다시 쓰여서 한 글자만 고쳐도 파일 전체가 바뀝니다. 열 때 한 번 되돌려 보고 원본과 다르면 원문으로 엽니다 — 판정 기준은 검사 스크립트와 같은 함수를 씁니다(다르면 검사는 통과인데 폰에서는 원문으로 열리는 일이 생깁니다).
- **표가 사라지는 걸 먼저 잡았습니다.** StarterKit 에 표가 없어서, 표가 있는 글을 열었다 저장하면 표가 통째로 없어졌습니다. `scripts/check-md-roundtrip.mjs` 를 만들어 글 전체를 돌려 보고 찾았습니다 — **확장을 더할 때마다 이걸 돌리세요.**
- 안 한 것: **본문에 쓴 HTML 살리기.** `<label>` 처럼 글자로 쓴 태그는 왕복에서 없어집니다. 그런 글이 8개인데, 원문 모드로 열리니 실제로 부딪힐 일은 없다고 봤습니다.
- 링크하신 tiptap simple-editor 템플릿은 안 썼습니다 — React 컴포넌트 묶음이라 정적 `/admin` 에 얹으려면 React 를 따로 넣어야 합니다. 코어만 쓰고 툴바는 쓰던 것을 그대로 뒀습니다.

### 2026-08-13 · `/admin` 토글이 빈 상자를 하나씩 차지하던 것

"초안으로 두기"·"비밀글로 두기"가 입력칸과 똑같은 상자에 담겨 있어서,
20px 짜리 스위치 하나가 59px 짜리 빈 판을 차지하고 있었습니다. 폰에서는
그만큼 스크롤이 늘어납니다. 라벨 옆에 스위치만 놓고 힌트는 아래로 내렸습니다.

- 상자만 지우면 라벨이 붕 뜹니다 — 라벨은 상자 위에 얹히는 **탭 모양**(위쪽 모서리만 둥글고 상자와 잇는 이음매가 붙습니다)이라, 같이 풀어서 평범한 글자로 만들었습니다.
- 위젯 종류를 가릴 방법이 emotion 해시밖에 없어서 `:has([role='switch'])` 로 잡았습니다. `:has()` 를 모르는 브라우저에서는 규칙이 통째로 무시돼 예전 모양 그대로 보입니다 — 깨지진 않습니다.

### 2026-08-13 · 글쓴이 표시를 뺐습니다

글 117개 중 116개가 "직접 씀"이었습니다. 어차피 전부 제 글이라 날짜 옆에
붙는 그 한 줄이 아무것도 구분해 주지 못했고, `/admin` 폼에서도 매번 그냥
넘기는 칸이었습니다.

- 화면·스키마·`/admin` 폼·글 117개의 frontmatter 에서 전부 뺐습니다. "나중에 쓸지도 모르니 값만 남겨두기"는 안 했습니다 — 안 쓰는 키가 남아 있으면 다음에 볼 때 뭔지 다시 알아봐야 합니다.
- **AI가 초안을 썼다는 표시도 같이 없어졌습니다.** 대신 §5 절차에서 사람이 반드시 읽고 고친 뒤 올리도록 했습니다. 표시로 면피하는 것보다 초안을 그대로 안 올리는 쪽이 맞다고 봤습니다.
- `/admin` 폼을 고쳤으므로 `index.html` 의 `config.yml?v=` 를 올렸습니다 (§6-2).

### 2026-08-13 · 검색엔진에 글이 제대로 안 잡히던 것

글 117개 중 116개에 `description` 이 없어서, meta description 이 전부
`SITE_DESCRIPTION` 하나로 채워지고 있었습니다. 검색 결과에서 어느 글인지
구분이 안 되는 상태였습니다.

- **없는 소개문을 지어내지 않기로 했습니다.** 대신 본문 앞부분에서 뽑아 `<head>` 에만 씁니다 — 화면 카드에는 여전히 안 나옵니다. 소개문이 없는 글은 없는 채로 보이는 게 맞다고 봤습니다. RSS 설명도 같은 값을 씁니다.
- **공유 카드가 텅 비던 것** — `heroImage` 가 없는 글이 대부분이라 OG 이미지가 아예 안 붙었습니다. 없으면 프로필 이미지로 떨어지게 했습니다.
- **구조화 데이터(JSON-LD)를 넣었습니다.** 글이면 `BlogPosting`, 나머지는 `WebSite`. ⚠ 여기 넣는 값은 **화면에도 있는 것**만 씁니다. 안 보이는 정보를 구조화 데이터에만 넣으면 스팸으로 봅니다.
- `robots.txt` 를 넣고 sitemap 위치를 알렸습니다. 막은 건 `/admin/` 뿐입니다.

⚠ **`BaseHead.astro` 의 `google-site-verification` 태그를 지우지 마세요.** Search Console 소유권 확인용인데, 확인이 끝난 뒤에도 구글이 주기적으로 다시 봅니다 — 사라져 있으면 속성이 해제되고 색인 현황·검색어 데이터를 못 봅니다. `lim-it.vercel.app` 은 서브도메인이라 DNS 확인 방식을 못 써서 이 태그가 유일한 끈입니다. **커스텀 도메인으로 옮기면** 그때는 DNS 방식으로 갈아타고 이 태그를 빼도 됩니다.

⚠ **비밀글(§6-3)에는 요약을 뽑지 않습니다.** 본문을 잠가 놓고 meta description
으로 앞부분을 흘리면 잠근 의미가 없습니다. RSS 는 비밀글을 아예 빼고 있어서
괜찮지만, **본문에서 뭔가를 뽑아 쓰는 코드를 새로 넣을 때는 `secret` 을 같이
확인하세요.**
