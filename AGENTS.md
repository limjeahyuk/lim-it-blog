# lim-it — 개발 매거진

**게임 devlog 전용이 아닙니다.** 공부한 것, 만든 것, 그냥 일기까지 다 들어옵니다.
지금은 게임 개발에 관심이 쏠려 있어서 그쪽 글이 늘고 있을 뿐입니다.

글의 기본은 **왜 그렇게 했는지**를 남기는 것입니다. Astro + MDX,
글 하나가 파일 하나이고, 커밋하면 Vercel이 배포합니다.

> `CLAUDE.md` 는 이 파일을 가리키는 심볼릭 링크입니다. 규약은 여기 한 곳에만 씁니다.

⚠ **블로그가 아니라 매거진으로 굴러갑니다.** 2026-08-13 에 카테고리와 태그를
없애고 **저자**로 합쳤습니다. 글 하나는 저자 한 명에게 속하고, 그게 유일한
분류입니다. 저자는 전부 한 사람이 쓰는 이름이지만 **사이트 어디에도 그렇게 적지
않습니다** — 정한 것이니 "같은 사람"이라는 문구를 넣지 마세요 (§3).

⚠ **"이 블로그는 게임 devlog다" 라고 전제하지 마세요.** 2026-08-12 에
티스토리(`hyuk-todayfeelsogood.tistory.com`)의 글 116개를 옮겨왔고, 지금
전체 글 131편 중 113편이 학습 기록(학생)입니다.

---

## 0. 작업 규칙 — 손대기 전에 읽으세요

**이 블로그를 고칠 때는 항상 이 파일부터 읽고 시작합니다.** 매번 같은 맥락에서
같은 판단을 하기 위해서입니다. 세션이 바뀌어도 결정의 근거는 여기 남아 있습니다.

**작업이 끝나면 — 코드를 고쳤을 때만 — §8 에 한 줄 남깁니다.**

- 남기는 건 **뭐가 불편해서 뭘 했는가** 입니다. "이랬는데 저래서 이렇게 바꿨습니다" 정도면 충분합니다.
- **코드 설명은 쓰지 않습니다.** 함수 이름, 인자, 구현 방식은 파일을 열면 보입니다. 여기 옮겨 적으면 코드가 바뀔 때 같이 썩습니다.
- 대신 **코드만 봐서는 모르는 것**을 씁니다 — 왜 이 방법이었는지, 뭘 안 하기로 했는지, 어디를 건드리면 안 되는지.
- 규약이 새로 생겼으면 §8 이 아니라 **해당 절(§1~§7)에 직접** 씁니다. §8 은 기록이지 규약이 아닙니다.

**글을 쓴 것은 §8 에 적지 않습니다.** 글은 그 자체로 남습니다. 말투(§1)와
쓰는 방식(§2)만 지키면 됩니다.

**끝나면 커밋하고 push 합니다.** 혼자 하는 저장소라 브랜치를 따로 파지 않고
`main` 에 바로 올립니다. `npm run post` 를 쓰면 빌드가 깨졌을 때 push 를 막아 줍니다.
커밋 메시지는 **왜 고쳤는지가 보이게** 씁니다 — "BaseHead 수정"이 아니라
"fix: 공유 카드에 이미지가 안 뜨던 것".

---

## 1. 말투 — 제일 자주 틀리는 부분

**참고 기준:** 티스토리에서 옮겨온 글들 (`src/content/posts/` 의 `# 티스토리에서 옮겨왔습니다:` 주석이 붙은 것들). 원본은 https://hyuk-todayfeelsogood.tistory.com/

글은 전부 **-습니다체 존댓말**로 씁니다. 기술 학습 일지 같은 톤입니다.

| | 이렇게 | 이렇게 말고 |
|---|---|---|
| 종결어미 | "~했습니다""~입니다""~됩니다" | "~했다""~이다""~한다" |
| 자기지칭 | **저** | 나, 내가 |
| 독자 | 가끔 질문을 던집니다 —"카드 두 장인 걸까요?" | "~해보자"같은 청유·명령 |
| 이모지 | 거의 안 씁니다 | 문단마다 이모지 |

- 부드럽게 풀어도 됩니다: "~하더라고요""~거든요""~같습니다"
- **코드 주석은 예외입니다.** 원본 코드를 그대로 인용할 때는 손대지 않습니다.

### 저자마다 다른 것과 같은 것

**위의 표는 저자가 누구든 똑같습니다.** 종결어미, 자기지칭, 이모지, 꺾쇠 금지,
한 문장 한 단락 — 전부 공통입니다. 매거진은 저자가 달라도 지면 톤은 하나입니다.

**저자마다 다른 것은 `src/data/authors.json` 의 `voice` 에 있습니다**
(`/admin` 의 「에디터」에서 봅니다).
문장 길이, 결론을 내리는지, 숫자를 얼마나 쓰는지 같은 것들입니다.

⚠ **글을 쓰기 전에 그 저자의 `voice` 를 읽으세요.** 종결어미까지 저자마다
다르게 만들지 마세요 — 같은 지면에서 나온 글로 안 읽힙니다.

### 인용 부호는 큰따옴표만 씁니다

⚠ **꺾쇠 `「」` 를 쓰지 마세요.** 강조하거나 말을 따올 때는 `""` 를 씁니다.

```
✗  죽지 않는 게임에서는 「무엇을 뺏을지」가 곧 난이도입니다
✓  죽지 않는 게임에서는 "무엇을 뺏을지?"가 곧 난이도입니다
```

물음이면 물음표까지 안에 넣습니다. 따옴표를 안 써도 읽히면 그냥 빼는 쪽이 낫습니다.

### 직관적인 단어를 씁니다

비유로 돌려 말하지 말고 **화면에 보이는 그대로** 씁니다. 읽는 사람이 한 번 더 해석해야 하는 말은 소제목에서 특히 나쁩니다.

```
✗  깃발에 주인이 생겼습니다
✓  깃발에 색상을 추가했습니다
```

같은 이유로 "프런티어"처럼 코드에서만 쓰는 말도 본문에서는 풉니다 — "이미 연 칸 옆".

기준 샘플은 `src/content/posts/status-effects-redefined.md` 입니다. **새 글을 쓰기 전에 그것부터 읽으세요.**

### ⚠ 사람이 쓰는 말투는 이것보다 헐겁습니다

2026-08-19 에 [`blog-rebuild-notes.md`](src/content/posts/blog-rebuild-notes.md) 를
`/admin` 에서 직접 고치셨습니다. **그 글이 진짜 말투 표본입니다** — 위 규칙(-습니다체·
"저"·꺾쇠 금지)은 그대로인데, AI가 쓴 문장이 지나치게 반듯해서 손을 본 것입니다.

무엇이 바뀌었는지가 곧 규칙입니다.

| | AI 초안 | 사람이 고친 것 |
|---|---|---|
| 제목 | `티스토리 116개를 옮기고 블로그를 고친 이틀` | `블로그 수정` |
| 소개문 | `정적 사이트에 비밀글을 붙이고 폰에서 글을 쓰게 만들면서 내린 판단들` | `블로그 수정하면서 생긴 것.` |
| 첫 문단 | 바로 기술 이야기 | **왜 다시 만들고 싶었는지** (morethan-log·노션을 안 쓰게 된 이야기) |
| 소제목 | `## 이미지 404장을 전부 받아왔습니다` | 지우고 `---` 로 |
| 단정 | `**그건 비밀글이 아닙니다.**` | 지움 |

- **앞뒤를 사람 말로 엽니다.** 기술 문단은 그대로 두되, 도입과 마무리에 왜 하고 싶었는지·하고 나서 어땠는지를 답니다.
- **말끝을 흐려도 됩니다** — "~네요", "~싶기도 하고..", "그래도 .. 하나하나 고쳐볼 생각입니다! :)"
- **말줄임표와 감탄사를 씁니다** — `...`, `ㅎㄷㄷ`, `허헣`, 취소선(`~~ ~~`), `:)`. 그림 이모지는 여전히 안 씁니다.
- **자기 자랑·자책을 조금 섞습니다** — "꽤나 많이 썻네요... 나 좀 열심히 했을지도?"
- **AI를 썼다는 것을 숨기지 않습니다** — "AI에게 정리 해달라고 했고 그걸 제가 좀 고쳤습니다".
- **결론을 굵게 못박지 않습니다.** 판단은 적되 선언은 덜어냅니다.
- 소제목은 `##` 도 쓰고 `---` 로만 나누기도 합니다. 덩어리만 갈리면 됩니다.

⚠ **기술 설명 문단까지 구어로 풀지는 마세요.** 사람이 고친 글에서도 숫자와 근거가 있는
문단은 거의 그대로 남았습니다. **바꾸는 건 이음새와 앞뒤**입니다.

---

## 2. 글 쓰는 방식

**이렇게 씁니다**

- **문제부터 씁니다.** 뭐가 불편했고 왜 손대야 했는지가 첫 문단에 있어야 합니다.
- **왜 그 선택인지 씁니다.** 무엇을 했는지는 커밋 로그에도 있습니다. 블로그에 쓸 값어치가 있는 건 판단의 이유입니다.
- **틀렸던 것도 씁니다.** 잘못 짰다가 고친 것, 예상 못 한 부작용, 뒤늦게 발견한 버그. 이게 제일 읽을 만합니다.
- **구체적인 숫자와 이름을 씁니다.** "밸런스를 조정했습니다"가 아니라 "전사 20에서 50으로, 10시드 × 1200런, 클리어율 33.0%".
- **끝에 남은 걸 한두 개 씁니다.** 다음에 읽을 사람(=본인)에게 쓸모 있는 문장으로.

**이렇게 안 씁니다**

- 커밋 메시지를 그대로 옮기지 않습니다. 변경사항 나열은 changelog지 글이 아닙니다.
- 확인 안 한 코드를 넣지 않습니다. 파일을 안 읽었으면 스니펫을 쓰지 않습니다.
- 과장하지 않습니다. "혁신적인""완벽하게 해결" 같은 말은 쓰지 않습니다.
- 튜토리얼처럼 쓰지 않습니다. 남을 가르치는 글이 아니라 **내가 뭘 했는지 남기는 글**입니다.
- 커밋에 없는 동기를 추측해서 채우지 않습니다. 모르면 안 씁니다.

### 단락과 사진

읽는 사람이 스크롤로 지치지 않게 하는 쪽이 우선입니다.

**기본은 한 문장에 한 단락입니다.** 마침표를 찍었으면 빈 줄을 넣고 다음 줄로 갑니다. 짧아서 혼자 두기 애매한 문장만 앞뒤와 붙입니다.

```
✗  스피드 레이스는 같은 시드로 만든 각자의 보드를 각자 풉니다. 상대가 어디를
   열었는지는 안 보이고, 진행바 숫자만 보입니다. 그런데 만들고 나니 아쉬웠습니다.

✓  스피드 레이스는 같은 시드로 만든 각자의 보드를 각자 풉니다.

   상대가 어디를 열었는지는 안 보이고, 진행바 숫자만 보입니다.

   그런데 만들고 나니 아쉬웠습니다.
```

⚠ **줄바꿈은 반드시 빈 줄이어야 합니다.** `remark-breaks` 를 안 쓰기 때문에 한 번 개행한 것은 화면에서 한 문단으로 이어 붙습니다. 이걸 바꾸려면 티스토리에서 옮겨온 116개가 같이 영향을 받으니 건드리지 마세요.

⚠ **`/admin` 서식 모드에서는 Enter 한 번이 이 빈 줄까지 넣습니다.** 문단을 가르면 마크다운에 빈 줄이 들어갑니다 — tiptap 이 그렇게 내보냅니다. 앞 줄에 이어 붙이고 싶으면 **Shift+Enter** 입니다(`  \n` 으로 나갑니다). 손으로 파일을 쓸 때는 위 규칙 그대로입니다.

- **한 단락은 서너 줄을 넘기지 않습니다.** 화면에서 글자 벽이 되면 읽다가 놓칩니다.
- **소제목(`##`)으로 덩어리를 나눕니다.** 긴 글이면 특히. 나중에 본인이 다시 찾아 읽을 때 목차가 됩니다.
- **화면·에러·결과는 글로 설명하지 말고 사진을 붙입니다.** "이런 화면이 나왔습니다" 다음에 이미지 한 장이 문단 셋보다 낫습니다.
- **손으로 쓸 때**는 `public/images/<slug>/` 에 두고 `![설명](/images/<slug>/파일명.png)` 로 씁니다. 티스토리에서 옮겨온 404장이 이 규칙을 따르고 있습니다.
- **`/admin` 에서 올린 사진은 Cloudinary 주소(`https://res.cloudinary.com/…`)로 들어갑니다** (2026-09-07 · §6-5). 둘이 섞여 있는 것이 정상입니다 — 옛 사진을 옮기지 않았습니다.
- **alt 는 채웁니다.** 이미지가 안 뜰 때 남는 유일한 단서입니다.
- **크기를 줄인 사진만 `<img src alt width>` 로 적습니다.** 마크다운 사진 문법에는 크기를 적을 자리가 없어서입니다 (`/admin` 의 「사진 크기」가 이 값을 씁니다 — §6-2). 크기를 안 줄 것이면 `![...]` 그대로 두세요.
- 코드가 길면 스니펫으로 자릅니다. 파일 전체를 붙여넣지 않습니다.

### 마크다운 함정 하나

한국어에서 `**"따옴표"**여야` 처럼 **닫는 `**` 앞뒤가 문장부호와 한글**이면 강조가 안 먹고 별표가 그대로 나옵니다 (CommonMark 규칙). 닫는 `**` 앞은 한글이나 숫자로 끝내세요.

```
✗  **"이번 라운드에 뭘 하면 안 되는가"**여야 합니다
✓  **이번 라운드에 뭘 하면 안 되는가**여야 합니다
```

---

## 3. frontmatter 규약

```yaml
---
title: 제목 (가장 중요한 정보 하나만)
description: 한 문장 요약   # 선택
pubDate: 2026-08-07
author: developer      # authors.json 의 id 와 일치
project: gridbrawl     # 선택. 만든 것 글에만
draft: false
---
```

**제목은 그 글이 뭔지 바로 보여야 합니다.** 본문과 달리 존댓말로 맞출 필요가 없습니다 — 명사로 끊는 편이 짧고 잘 읽힙니다.

| 이렇게 | 이렇게 말고 |
|---|---|
| `보물찾기 모드 추가` | `보물찾기는 죽는 대신 길을 잃습니다` |
| `타임어택 대결 모드 추가` | `같은 판을 둘이 동시에 푸는 레이스를 붙였습니다` |
| `공유할 수 있는 시드 추가` | `지뢰찾기 판 하나를 숫자 하나로 줄였습니다` |

오른쪽은 읽고 나면 멋있는데, 목록에서 처음 보면 무슨 글인지 모릅니다. **제목에서 비유·반전·요약을 하지 마세요.**

**`description` 은 딱 한 문장입니다.** 마침표를 찍었으면 거기서 끝냅니다 — 뒤에 문장을 더 붙이지 않습니다.

```
✓  51 x 51 한가운데 보물을 두고 누가 더 빨리 찾는 게임
✗  51×51 한가운데 보물을 두고 꼭짓점에서 길을 뚫습니다. 죽지 않는 지뢰찾기를 만들려다 …
```

**제목이 이미 다 말하고 있으면 `description` 을 아예 빼세요.** 같은 말을 늘려 쓴 줄이 하나 더 붙을 뿐입니다.

- **`description` 은 선택입니다.** 없으면 목록 카드에서 그 줄이 통째로 빠집니다. 티스토리에서 옮겨온 116개에는 거의 없습니다 — 억지로 채우지 마세요. 검색엔진에 나갈 meta description 은 없을 때 [`src/lib/excerpt.ts`](src/lib/excerpt.ts) 가 본문 앞부분에서 알아서 뽑습니다 (화면에는 안 보입니다). **SEO 때문에 description 을 채울 필요는 없습니다.**
- **`pubDate` 로는 예약 발송이 안 됩니다.** 정적 사이트라 예약을 밟아 줄 서버가 없습니다 — 미래 날짜를 적어도 그 자리에서 바로 나가고, **맨 위로 정렬되기만** 합니다. 이 값이 하는 일은 정렬(서고의 연도 묶음 · 홈 차례 · 프로젝트의 "최근 업데이트")과 화면의 날짜, 그리고 RSS·JSON-LD 에 나가는 발행일입니다. 안 내보내고 싶으면 `draft: true` 를 쓰세요.
  ⚠ **`/admin` 에는 이 칸이 안 보입니다** (2026-09-04). 새 글이면 지금 날짜가 자동으로 들어가고, 옛 글이면 파일에 적힌 값이 그대로 남습니다 — 고쳐야 하면 파일을 직접 여세요.
- `author` 는 `src/data/authors.json` 의 `id` 와 **반드시** 일치해야 합니다. 안 맞으면 빌드가 깨집니다 (의도한 것 — 오타를 배포 전에 잡습니다).
- `project` 는 `PROJECTS[].id` 와 일치해야 합니다. **저자와 별개입니다** — devlog 글은 보통 `author: developer` + `project: gridbrawl` 둘 다 붙습니다.
- **주소는 `slug` 가 정합니다.** Astro 의 glob 로더는 frontmatter 의 `slug` 를 글의 id 로 씁니다 — `slug: blog-design` 이면 파일명이 `blog-magazine-remake.md` 여도 주소는 `/posts/blog-design` 입니다. 영문 소문자 + 하이픈.
  ⚠ **`slug` 를 고치면 주소가 바뀝니다.** 옛 주소는 그 자리에서 404 가 됩니다 (2026-08-19 에 두 편이 그렇게 옮겨졌습니다). 본문의 사진은 `/images/…` 절대 주소라 그대로 뜹니다.
  ⚠ **글 전부에 `slug` 를 채워 뒀습니다** (2026-08-19 · 값은 파일명과 같습니다). `/admin` 의 "주소" 칸이 필수라, 비어 있으면 열어서 고칠 때 저장이 막힙니다.
- `heroImage` 는 **공개 경로**입니다 — `/images/<주소>/파일명`. `/admin` 의 「커버 사진」 칸이 채웁니다. ⚠ Astro 자산이 아닙니다 (§4).
- `secret: true` 면 **비밀글**입니다 (§6-3).
- `slug` 는 새 글의 **파일명**도 정합니다 (`config.yml` 의 `slug: '{{fields.slug}}'`). 사진이 올라가는 폴더 이름(`public/images/<주소>/`)도 같은 값입니다.
- **빈 문자열은 "값 없음"으로 봅니다.** `author: ''` 는 통과합니다 (`content.config.ts` 의 `blankAsUndefined`). `/admin` 에서 선택을 골랐다 지우면 키가 사라지지 않고 빈 문자열로 남는데, 그것 때문에 빌드가 깨지면 폰에서는 원인을 알 방법이 없습니다. **오타는 여전히 걸립니다** — `author: studnet` 은 그대로 빌드가 깨집니다.

### 저자 — 이 사이트의 유일한 분류

⚠ **카테고리와 태그는 없습니다.** 2026-08-13 에 둘 다 없애고 저자로 합쳤습니다.
`/categories/*` 와 `/tags/*` 주소도 같이 사라졌고, **되살리지 않습니다.**

**글 하나는 저자 한 명에게 속하고, 그게 전부입니다.**

| | 저자 |
|---|---|
| 개수 | 글마다 **하나** |
| 구조 | 평평함 (트리 없음) |
| 용도 | 누가 쓴 글인가 |
| 화면 | 왼쪽 사이드바 `Editors`, `/authors`, `/authors/<id>` |

지금 있는 저자 (`src/data/authors.json`):

| id | 이름 | 무엇을 쓰나 | 색 |
|---|---|---|---|
| `student` | 학생 | 배운 것 — 수업 필기, 공부 정리 | 파랑 |
| `developer` | 개발자 | 만든 것 — devlog, 이 블로그를 고친 기록 | 빨강 |

⚠ **이름과 `id` 가 다릅니다.** 화면은 「학생」·「개발자」인데 주소는
`/authors/student`·`/authors/developer` 입니다. **이름은 언제든 바꿔도 되고**
`/admin` 에서 바꿉니다 — `id` 를 바꾸면 글 131편의 frontmatter 와 주소가 같이
움직입니다.

**저자는 화면 요소가 아니라 글의 규약입니다.** 그래서 항목 하나가
보이는 것(`name`·`bio`·`color`)과 **안 보이는 것(`voice`)** 을 같이
들고 있습니다. `voice` 는 그 저자로 글을 쓸 때 지키는 말투이고, 화면에는 안
나옵니다. 두 개를 다른 파일에 두면 소개는 바꿨는데 말투는 그대로인 일이 생깁니다.

### 저자를 늘리는 것은 `/admin` 에서 합니다 (2026-09-09)

**「에디터」 컬렉션**입니다 — 머리띠의 「글」 옆 탭(폰에서는 둘째 줄).
사람마다 카드가 하나씩이고, 「편집」을 누르면 이름·소개·색·아바타 글자·얼굴
사진·말투를 고칩니다. 늘리는 것은 오른쪽 위 「에디터 추가」입니다. 순서는
줄을 끌어서 바꿉니다 — **그 순서가 그대로 화면 순서**입니다 (사이드바·알약
줄·`/authors`).

⚠ **「삭제」는 글이 딸린 사람에게는 안 먹힙니다** — 그대로 지우면 빌드가
멈춥니다(아래). 먼저 그 글들을 다른 에디터로 옮기세요. 지우고 나서 **「게시」를
눌러야** 파일에 남습니다.

예전에는 `src/consts.ts`·`global.css`·`config.yml` **세 군데**를 손으로
고쳐야 했습니다. 글은 폰에서 쓰는데 쓰는 사람을 늘리는 것만 노트북을 열어야
하는 것이 이상해서 옮겼습니다.

| 어디 | 무엇 |
|---|---|
| `src/data/authors.json` | **값의 원본.** `/admin` 이 고치는 파일 |
| `src/consts.ts` 의 `AUTHORS` | 그 파일을 읽어 화면이 쓰는 모양으로 바꿉니다 |
| `scripts/sync-authors.mjs` | `/admin` 쪽 둘에 옮겨 적습니다 (`npm run admin` 이 부릅니다) |

- ⚠ **색은 팔레트에서 고릅니다** — 파랑·빨강·초록·주황·청록 다섯. `global.css` 에 미리 깔려 있습니다 (§4). 색을 더 늘릴 때만 CSS 를 엽니다.
- ⚠ **`config.yml` 의 「저자」 목록을 손으로 고치지 마세요.** `<lim:authors>` 표시 사이를 스크립트가 갈아 끼웁니다. Decap 의 select 는 정적이라 다른 파일을 못 읽고, `relation` 으로 바꾸면 저자 알약(§6-2)이 안 그려집니다.
- ⚠ **그래서 폰에서 에디터를 더하면 글에 붙일 수 있는 것은 다음 배포부터입니다.** 저장은 곧 커밋이고, Vercel 이 `npm run build` 를 부르면서 목록이 따라옵니다. 그 사이에도 `/authors/<id>` 지면은 이미 나갑니다.
- ⚠ **글이 딸린 에디터를 지우면 빌드가 깨집니다** (`z.enum`). 지우기 전에 그 이름으로 쓴 글을 옮기세요 — 의도한 것입니다.
- ⚠ **`voice` 는 여러 줄 칸입니다.** 한 줄에 하나씩 적고 빈 줄은 버립니다. 목록 위젯으로 뒀더니 접힌 줄에 「한 줄」이라는 필드 이름만 보여서 하나씩 펼쳐 봐야 했습니다.

### 저자 얼굴

**사진이 아직 없습니다.** 그래서 `initial` 한 글자를 저자 색 원에 앉혀 씁니다
([`Avatar.astro`](src/components/Avatar.astro)). 빈 원을 두면 준비 중인 것처럼
보여서 채워 둔 것이고, 사진이 생기면 이렇게 갈아끼웁니다.

1. `public/authors/` 에 파일을 넣습니다
2. `authors.json` 의 `avatar` 에 `/authors/<파일명>` 을 적습니다 (`/admin` 의 「얼굴 사진」으로 올리면 Cloudinary 주소가 들어갑니다)

- **사진은 흑백으로 깔립니다** (`filter: grayscale(1)`). 색이 제각각인 사진이 나란히 서면 저자 색보다 사진이 먼저 읽혀서 분류가 흐려집니다. 컬러로 두려면 `Avatar.astro` 의 그 한 줄을 빼세요.
- ⚠ **아바타를 SVG 파일로 뽑지 마세요.** 색이 `--who-*` 라 테마마다 달라야 하는데, 파일로 두면 한쪽 테마에서 대비가 떨어집니다. 그래서 컴포넌트에서 그립니다.
- 원 배경은 저자 색 16% 를 카드색에 섞은 것입니다. 글자가 큰 편이라 대비 3:1 만 넘으면 되는데, 지금은 다크 4.0:1 · 라이트 5.0:1 입니다.

⚠ **글이 0개인 저자는 사이드바에서 숨습니다.** `/authors` 목록에는 나옵니다 —
사이드바는 고르는 곳이고 `/authors` 는 소개하는 곳이라 규칙이 다릅니다.

---

## 4. 디자인 시스템

### 출처

색·radius·아이콘 전부 **limSystem** 에서 가져왔습니다.

```
색/radius   원본: ~/Develop/React/limSystem/src/util/theme.ts
            사본: src/styles/tokens.css           ← 손으로 옮김
아이콘      원본: ~/Develop/React/limSystem/src/assets/icons/*.svg
            사본: src/assets/icons/*.svg           ← npm run icons
            추가: src/assets/icons-extra/*.svg     ← limSystem 에 없어서 손으로 넣음
```

limSystem 은 별도 Next.js 앱(`private: true`)이라 패키지로 못 가져옵니다. **컴포넌트(React)는 Astro 에서 못 씁니다** — 가져올 수 있는 건 값과 SVG 뿐입니다.

**색은 손으로 옮긴 것이라 원본이 바뀌면 `tokens.css` 도 같이 고쳐야 합니다.** 아이콘은 `npm run icons` 로 다시 긁어오면 됩니다.

### 아이콘 — 이모지 대신 씁니다

목록 머리글·사이드바 제목·버튼에는 **limSystem SVG** 를 씁니다. 이모지는 OS마다 그림이 달라서 다크 테마에서 혼자 튀거든요.

```astro
---
import Icon from '../components/Icon.astro'
---
<span class="title-ico"><Icon name="notebook" size={17} /> All Posts</span>
```

- 이름은 파일명 그대로입니다. 목록은 `src/lib/icon-names.ts` (자동 생성).
- 없는 이름을 쓰면 **빌드가 깨집니다.** 의도한 것입니다 — 오타를 배포 전에 잡습니다.
- 원본 SVG 는 색이 `#3C3E44` 로 박혀 있는데 `Icon.astro` 가 `currentColor` 로 바꿔 줍니다. 그래서 **부모의 `color` 를 따라갑니다** — 아이콘에 색을 직접 주지 말고 부모에 의미 토큰을 주세요.
- 아이콘 + 글자를 한 줄로 놓을 때는 `.title-ico`(global.css)나 `.side-title` 을 씁니다.

지금 쓰는 것들:

| 자리 | 아이콘 | 어디서 |
|---|---|---|
| Tags | `label` | limSystem |
| Live Service | `book-open` | icons-extra |
| Posts / Devlog | `notebook` | limSystem |
| Profile | `smile` | icons-extra |
| Contact | `chat-circle-dots` | icons-extra |
| 테마 토글 | `moon` / `sun` | icons-extra |
| 웹에서 플레이 | `play-circle` | limSystem |
| App Store 에서 받기 | `download` | limSystem |

### 아이콘이 limSystem 에 없을 때

limSystem 에는 **해·달·말풍선·펼친 책·웃는 얼굴·게임패드·메일·GitHub** 이 없습니다. 두 가지 방법이 있습니다.

1. **뜻이 통하는 다른 아이콘을 고른다** — 있는 걸로 되면 이쪽이 낫습니다.
2. **`src/assets/icons-extra/` 에 넣는다** — 같은 팩(24×24 · `fill="#3C3E44"`)에서 가져온 SVG만 넣으세요. 선 굵기가 다르면 나란히 놨을 때 바로 티가 납니다.

⚠ **`npm run icons` 는 `src/assets/icons/` 를 통째로 지웠다 다시 씁니다.** 손으로 넣은 아이콘을 거기 두면 다음 sync 때 사라집니다. 반드시 `icons-extra/` 에 두세요. 이름이 양쪽에 겹치면 sync 스크립트가 오류로 멈춥니다.

⚠ **파일명은 kebab-case 로 바꿔서 넣습니다.** limSystem 규약이라 `Book_Open.svg` → `book-open.svg` 처럼 맞춥니다.

### 토큰 사용 규칙

**컴포넌트에서 `--lim-*` 원시 값을 직접 쓰지 마세요.** 의미 토큰만 씁니다.

| 의미 토큰 | 용도 |
|---|---|
| `--bg` | 페이지 배경 |
| `--card` | 카드 배경 |
| `--surface` | 알약·인라인코드 배경, hover |
| `--surface-2` | 표 헤더·비밀글 자물쇠 판 배경 |
| `--border` | 선, 구분선 |
| `--text` | 본문 글자 |
| `--text-dim` | 보조 글자 |
| `--text-faint` | 날짜·개수 같은 흐린 글자 |
| `--accent` | 강조 (읽기 진행 막대·포커스 테두리·주 버튼 배경) |
| `--accent-text` | 본문 링크 |
| `--accent-fg` | **강조색 판 위에 얹는 글자** (주 버튼·비밀글 단추) |
| `--live-bg` · `--live-fg` | LIVE 배지 (빨강. 강조색이 아닙니다) |
| `--quote-bar` | 인용문 왼쪽 선 |
| `--pill-fg` | 색 있는 알약 위의 글자 |
| `--who-<저자id>` | 저자 이름 글자색 |

⚠ **`--pill-fg` 는 두 테마 모두 어두운 색입니다.** 강조색 판 위에 쓰면
라이트에서 어두운 글자 + 진한 파랑이 되어 3.5:1 로 떨어집니다 — 거기는
`--accent-fg` 입니다.

⚠ **판 네 겹(`--bg`·`--card`·`--surface`·`--border`)은 2026-09-08 시안
값입니다** (`tokens.css` 의 `--ui-*`). limSystem 회색이 아니고, `npm run icons`
처럼 원본에서 다시 긁어올 수 있는 값도 아닙니다 — 고치려면 시안을 다시 보거나
그 값을 직접 손대야 합니다. **글자색은 여전히 limSystem 입니다.**

### 코드 블록 — 판 위에 언어 이름과 「복사」

노션과 같은 자리입니다. 감싸는 `figure.code` 와 머리 줄은
[`astro.config.mjs`](astro.config.mjs) 의 `rehypeCodeChrome` 이 빌드할 때
답니다.

- ⚠ **머리 줄을 판 안에 겹쳐 놓지 마세요.** Shiki 가 배경색을 `!important` 로 박아서 안쪽 색을 맞출 수가 없고, 코드 첫 줄과도 겹칩니다.
- ⚠ **판 색은 Shiki 테마 것을 안 씁니다.** `github-light` 의 배경이 `#fff` 라 라이트 테마에서 카드와 구분이 안 됩니다. `--surface` 로 덮되 **글자색은 그대로 둡니다** — 색까지 덮으면 하이라이팅이 죽습니다.
- ⚠ **토막(span)마다 배경이 또 깔려 있습니다.** 판 색만 바꾸면 그 흰 조각이 줄 앞머리에 네모로 남습니다. 투명으로 덮어 둔 이유입니다 — 줄 강조(transformer)를 쓰기 시작하면 여기부터 보세요.
- **「복사」는 문서에 한 번 걸린 것이 받습니다** ([`PostLayout.astro`](src/layouts/PostLayout.astro)). 비밀글은 본문이 나중에 풀려서 DOM 에 들어오는데, 그렇게 들어온 코드 블록도 그대로 눌립니다.
- ⚠ **클립보드는 보안 컨텍스트(https·localhost)에서만 있습니다.** 없으면 단추를 아예 안 보이게 둡니다 (`html.has-copy` 로 켭니다) — 눌리지도 않는 단추가 코드마다 붙어 있는 것보다 낫습니다.

### 인용문 — 왼쪽 선 하나 + 옅은 판

`.prose blockquote` 입니다. 왼쪽에 3px 선(`--quote-bar`), 배경은 `--card`,
오른쪽만 둥근 모서리.

⚠ **2026-09-07 의 "배경을 다시 깔지 마세요" 를 2026-09-08 시안이 뒤집은
것입니다.** 그때는 글 본문이 카드 위에 있어서 인용문에까지 판을 깔면 코드블록·
표와 같은 무게로 읽혔는데, 지금은 본문이 지면 배경 위에 바로 놓입니다
(`PostLayout` 에서 카드를 걷어냈습니다). **글 본문을 다시 카드 위로 올리면
이 배경도 같이 걷어내세요.**

⚠ 왼쪽 선은 `--border` 로는 판 위에서 안 보입니다 — `--quote-bar` 입니다.

⚠ 편집기 쪽(`admin-src/editor.css`)도 같은 모양이라 한쪽만 고치면 갈라집니다.
**판 색만 다릅니다** — 편집기 바닥이 이미 `--a-card` 라, 거기서는 한 칸 옆인
`--a-surface` 를 깝니다.

radius 도 마찬가지입니다. `--radius-sm/md/lg/full` 은 limSystem 원본이고, `--radius-card`(16px)는 morethan-log 풍 카드를 위해 **블로그에서 추가한 확장값**입니다.

### 강조색과 프로젝트 색 — Neon Jungle

**전부 limSystem 400 단계에서 골랐습니다.** 별도 팔레트가 아니라 limSystem 안의 조합이라, 원본이 바뀌면 같이 따라갑니다.

| hue | 토큰 | limSystem | 프로젝트 | 다크 대비 |
|---|---|---|---|---|
| 0° | `--nj-red` | RED_400 | — (비어 있음) | 6.3:1 |
| 33° | `--nj-amber` | ORANGE_300 | — (비어 있음) | 10.1:1 |
| 34° | `--nj-orange` | ORANGE_400 | **BeepTimer** | 7.7:1 |
| 121° | `--nj-green` | GREEN_400 | **MineApp** | 7.8:1 |
| 181° | `--nj-teal` | TEAL_400 | **GridBrawl** | 10.7:1 |
| 209° | `--nj-blue` | BLUE_400 | — (비어 있음) | 6.2:1 |

토큰 여섯은 그대로 두되 **지금 쓰는 건 셋뿐입니다.** 프로젝트를 늘리면 빈 칸에서 골라 쓰세요.

**강조색은 파랑입니다** (2026-09-08 시안).

| 토큰 | 값 | 쓰임 | 대비 |
|---|---|---|---|
| `--ui-blue-dark` | `#6ea8ff` | 다크 강조·링크 | 8.5:1 |
| `--ui-blue-light` | `#2563eb` | 라이트 강조·링크 | 5.2:1 (흰 배경) |
| `--ui-red-dark` | `#ff7070` | 다크 LIVE·개발자 | 7.6:1 |
| `--ui-red-light` | `#dc2626` | 라이트 LIVE·개발자 | 4.8:1 (흰 배경) |

⚠ **`--nj-accent`·`--nj-accent-text`·`--nj-accent-deep`(청록) 셋은 이제 아무
데서도 안 씁니다.** 프로젝트 색 `--nj-teal` 과 헷갈리지 마세요 — 그쪽은
GridBrawl 알약으로 그대로 살아 있습니다.

⚠ **강조 파랑과 저자 「학생」의 파랑이 같은 값입니다. 빨강도 「개발자」와 LIVE
배지가 같습니다.** 시안이 그렇게 그려져 있습니다 — 한쪽만 바꾸면 링크와 저자
이름이 다른 파랑이 되어 화면에서 갈라집니다.

⚠ **`--nj-orange`(34°)와 `--nj-amber`(33°)는 색상이 1° 차이입니다.** limSystem 에 보라·분홍 계열이 없어서 6개를 전부 다른 계열로 못 뽑았습니다. 명도로 갈리니(7.7:1 vs 10.1:1) **둘을 목록에서 나란히 두지 마세요.** (지금은 amber 를 안 쓰고 있어서 문제가 없습니다.)

프로젝트가 더 늘면 limSystem 의 300/500 단계에서 뽑되, **기존 색과 hue 가 30° 이상 떨어지거나 명도가 확실히 다른 것**을 고르세요.

### 색을 바꿀 때 반드시 확인할 것

파스텔은 배경에 따라 대비가 확 달라집니다. **눈대중하지 말고 계산하세요.**

- **본문 링크(`--accent-text`)는 4.5:1 이상.** 밝은 색을 흰 배경에 그대로 올리면 2:1 대에서 놉니다 — 그래서 라이트·다크가 값이 다릅니다.
- **색 알약 위 글자(`--pill-fg`)는 두 테마 모두 어두운 색.** 프로젝트 알약 배경이 양쪽 다 밝은 파스텔이라, 흰 글자면 1.5~3.0:1 로 떨어집니다.
- **강조색 판 위 글자는 `--accent-fg` 입니다.** 라이트는 진한 파랑이라 흰 글자, 다크는 밝은 파랑이라 어두운 글자로 **테마마다 뒤집힙니다** — `--pill-fg` 를 쓰면 라이트에서 3.5:1 입니다.
- **`.live` 배지는 `--live-bg` / `--live-fg` 한 쌍입니다.** `--accent` 나 `--card` 를 쓰지 마세요.

새 색이 필요하면 `tokens.css` 에 추가하고, `global.css` 에서 의미 토큰으로 연결하세요.

### 저자 색 (`--who-*`) — 프로젝트 색과 쓰임이 다릅니다

⚠ **프로젝트 색은 알약 배경이고, 저자 색은 글자색입니다.** 그래서 `--nj-*`
(400 단계 파스텔)를 저자에 그대로 쓰면 **라이트 테마에서 안 읽힙니다** —
흰 배경에 blue_400 은 2.8:1 밖에 안 나옵니다.

강조색과 같은 방식으로 테마마다 다른 값을 씁니다 (`global.css`).

⚠ **2026-09-09 에 이름에서 팔레트로 바꿨습니다.** 예전에는
`--who-student`·`--who-developer` 처럼 **저자마다** 두 줄을 손으로 넣었는데,
그러면 `/admin` 에서 에디터를 더해도 색이 없어서 이름이 안 보입니다 (§3).
지금은 다섯 색을 미리 깔아 두고 `authors.json` 의 `color` 가 그중 하나를
고릅니다.

| 이름 | 다크 | 라이트 | 대비 (다크 / 라이트) | 지금 쓰는 저자 |
|---|---|---|---|---|
| `--who-blue` | `--ui-blue-dark` | `--ui-blue-light` | 7.8:1 / 5.2:1 | 학생 |
| `--who-red` | `--ui-red-dark` | `--ui-red-light` | 7.0:1 / 4.8:1 | 개발자 |
| `--who-green` | `--lim-green-400` | `--lim-green-600` | 8.3:1 / 6.1:1 | — |
| `--who-orange` | `--lim-orange-400` | `--lim-orange-700` | 8.3:1 / 5.4:1 | — |
| `--who-teal` | `--lim-teal-400` | `--lim-teal-800` | 11.4:1 / 8.3:1 | — |

⚠ **2026-09-08 시안부터 파랑·빨강은 곧 강조색·LIVE 색입니다** (`--who-blue`
= `--accent`, `--who-red` = `--live-bg`). 한쪽만 고치면 갈라집니다.

⚠ **`--who-teal` 은 프로젝트 색 `--nj-teal`(GridBrawl 알약)과 다른 값입니다.**
저자 색은 글자라 라이트에서 더 어두워야 합니다.

색을 늘리려면 **`global.css` 의 두 블록에 한 줄씩** 넣고, `consts.ts` 의
`AUTHOR_COLORS` 와 `config.yml` 의 「색」 목록에도 같은 이름을 더하세요.
대비는 계산해 보세요 (눈대중 금지 — 400 단계는 라이트에서 거의 항상
떨어집니다). **원시 `--nj-*` 를 저자에 직접 넣지 마세요.**

⚠ **모르는 색 이름이 와도 빌드는 안 깨집니다** — 파랑으로 떨어집니다
(`consts.ts` 의 `colorVar`). 폰에서 저장한 것 때문에 사이트가 통째로 안 나가는
쪽이 더 나쁩니다.

### 헤더와 메뉴

**이름 하나 + 햄버거 하나입니다** (`Header.astro`). 예전에는 오른쪽에
Editors·Posts·Live Service 가 늘어서 있었는데, 거기에 검색과 테마까지
붙이면 다섯 개가 되어 폰에서 화면 끝에 닿습니다. 그래서 접었습니다.

| | 폰 | 큰 화면 |
|---|---|---|
| 메뉴 판 | 화면 전체 | 오른쪽 절반 (최소 24rem) |

- 판 안에 **검색칸 · EDITORS · POSTS · LIVE SERVICE · 아래 링크**가 다 있습니다.
- 오른쪽 위는 **해/달 → 닫기** 순서입니다.
- ⚠ **판을 열 때 검색칸에 포커스를 주지 마세요.** 폰에서 키보드가 같이 올라와 메뉴의 절반을 덮습니다. 메뉴를 열었다고 검색을 하려는 것은 아닙니다 — 키보드는 검색칸을 눌렀을 때만 올라옵니다. 대신 판 자체(`tabindex="-1"`)로 포커스를 옮겨서, 키보드로 다니는 사람이 판 안에서 이어지게 합니다.
- ⚠ **뒤 지면 잠그기는 폰과 데스크톱이 다릅니다.** iOS 사파리는 `overflow: hidden` 으로 터치 스크롤이 안 막힙니다 — 판을 열고 쓸어내리면 뒤가 그대로 움직입니다. 폰에서는 `body` 를 `position: fixed` 로 못박고 지금 스크롤 위치만큼 위로 올려 둡니다 (닫을 때 그 자리로 되돌립니다). **데스크톱에는 이 방법을 쓰면 안 됩니다** — `body` 가 흐름에서 빠지면서 `sticky` 헤더가 화면 밖으로 나가고, 판이 절반만 덮는 데스크톱에서는 그게 그대로 보입니다.
- ⚠ **읽기 진행 막대는 헤더에만 둡니다.** 페이지에서 한 번 더 그리면 겹칩니다.
- ⚠ **테마 토글은 한 페이지에 하나만.** `#theme-toggle` 을 id 로 찾기 때문에 둘을 놓으면 뒤엣것이 안 먹습니다.

### 검색

정적 사이트라 서버가 없습니다. 빌드할 때 색인을 내보내고
(`src/pages/search.json.ts` → `/search.json`, 83KB) 브라우저가 받아서
걸러냅니다 (`/search`).

- **본문 전체는 안 넣습니다.** 제목·소개문·본문 앞 240자까지입니다. 전체를 넣으면 몇 MB가 되고 검색창을 열 때마다 그걸 받습니다.
- ⚠ **비밀글은 본문에서 아무것도 뽑지 않습니다** (§6-3 과 같은 이유). 제목만 나오고 자물쇠가 붙습니다.
- 형태소 분석 없이 **글자 포함**으로 찾습니다 — "지뢰"로 "지뢰찾기"가 걸립니다.
- 주소의 `?q=` 로 결과를 그대로 주고받을 수 있습니다.

### 홈 지면

홈은 사이드바가 없습니다. 화면 폭을 다 쓰고 네 덩어리로 갑니다.

```
[ 헤드라인 ]   최신 한 편을 크게 (사진 위에 제목) + 옆에 카드 셋
[ 인기 글 ]    카드 넷 — 폰 2열 · 큰 화면 4열 (조회수 순 · 못 받아오면 「최신 글」)
[ 더 읽을 글 ] 번호 + 제목 + 날짜 (언제나 최신순) + 전체보기
[ 아래 띠 ]    저작권 한 줄 + 연락처 두 개
```

⚠ **2026-09-08 시안에서 두 덩어리의 이름이 맞바뀌었습니다.** 전에는 카드가
「최신 글」이고 번호 줄이 조회수 순이었는데, 카드는 헤드라인·옆줄이 최신 넷을
먼저 집어간 **다섯째 글부터**라 「최신 글」이라는 이름이 거짓말이었습니다.

- ⚠ **위에서부터 최신순으로 잘라 씁니다.** 겹치지 않게 이어서 집어가기 때문에 **한 덩어리를 늘리면 아래가 그만큼 밀립니다.** 지금은 1 + 3 + 4 + 8 = 열여섯 편입니다.
- ⚠ **저자로 나누지 않습니다.** 그래서 **새 글이 한 저자에게 몰리면 위쪽이 그 저자로 다 찹니다** — 지금 최신 12편이 전부 「개발자」라 「학생」은 "더 읽을 글" 다섯째 줄에서야 처음 나옵니다. 알고 고른 것입니다 (§8). 심해지면 한 덩어리를 저자별로 돌리는 쪽을 먼저 보세요.
- ⚠ **헤드라인의 그늘(`.hero::after`)을 옅게 만들지 마세요.** 커버가 밝은 앱 캡처거나 게임 화면처럼 잔글씨가 빽빽하면 제목이 배경과 섞입니다. 한 번 얕게 깔았다가 그것 때문에 다시 진하게 만든 값입니다.
- **인기 글 카드 넷은 브라우저가 갈아 끼웁니다** (2026-09-08 · §6-6). 서버는 최신순으로 그려 두고, `/api/views` 에서 순서를 받아 오면 제목·사진·저자까지 인기순으로 바꿉니다 (사진과 저자는 `/ranks.json` 에서 옵니다). ⚠ **조회수 숫자는 목록에 안 보입니다** — 날짜 자리는 그대로 날짜입니다.
  ⚠ **못 받아오면 제목이 「최신 글」로 남습니다.** 세지도 않은 것에 「인기 글」이라고 써 붙이면 센 척하는 것이 됩니다 — 예전에 번호를 순위로 안 쓴 이유가 그것이었고, 그 규칙은 그대로입니다. **2026-09-08 에 "데이터 없으면 최신 글" 로 다시 확인했습니다 — 뒤집지 마세요.**
  ⚠ **「더 읽을 글」에서는 위에 이미 나온 여덟 편을 뺍니다.** 같은 화면에 두 번 나옵니다.
- ⚠ **저자 알약을 다시 넣지 마세요.** 원본 시안의 "카테고리" 자리를 에디터 알약으로 바꿔 봤다가 걷어냈습니다. 저자로 좁히는 건 `/posts` 위의 알약 줄이 합니다.
- 카드 사진은 **16:10**, 옆줄 썸네일은 **1:1** 입니다. 사진은 **위쪽 기준으로 자릅니다** — 코드·앱 캡처는 위에 정보가 몰려 있습니다.
- 제목은 **두 줄에서 자릅니다.** 길이가 제각각이면 카드 아래가 들쭉날쭉합니다.
- **스크롤 효과는 없습니다.** 예전 홈에 있던 패럴랙스·저자 표지 고정·떠오르기는 이 지면으로 오면서 다 빠졌습니다.

### 서고 (`/posts`) 와 저자 지면 (`/authors/<id>`) — 한 쌍입니다

홈의 "더 읽을 글 · 전체보기" 가 오는 곳입니다. 홈과 **같은 폭·같은 여백**을 씁니다.

```
[ Editors 알약 줄 ]                    ← 두 지면이 같은 줄을 씁니다
[ 전체 글 · 총 131개 ]  또는  [ 얼굴 + 이름 + 소개 + 글 116편 ]
[ 제목 · 저자 · 09.08 ]  또는  [ 2026년 9월 8일 · 제목 ]
[ ‹ 1 2 3 4 5 … 7 › ]                 ← 스무 편마다
```

⚠ **2026-09-08 시안에서 거르기가 없어졌습니다.** 예전에는 서고에 131줄을 다
그려 놓고 `?author=` 로 숨겼다 켰는데, 쪽 나누기가 생기면서 "거르기 × 쪽"
두 상태를 한 화면에서 들고 있어야 했습니다. 저자별 목록은 이미
`/authors/<id>` 가 하고 있어서 **알약이 그쪽으로 넘어가기만 합니다.**

- **알약 줄은 [`EditorTabs.astro`](src/components/EditorTabs.astro) 하나입니다.** 서고는 얼굴이 붙은 큰 것(`stack`), 저자 지면은 얼굴 없는 작은 것(`inline`) — 저자 지면은 바로 아래에 큰 얼굴이 또 나오기 때문입니다. ⚠ **글이 0편인 저자는 여기서 빠집니다** (`/authors` 목록에는 나옵니다).
- ⚠ **옛 `?author=` 주소는 서고의 script 가 `/authors/<id>` 로 넘깁니다.** 안 넘기면 그 주소로 들어온 사람에게 아무 일도 안 일어난 채 전체 목록만 보입니다.
- **쪽 나누기도 브라우저가 합니다** ([`src/lib/pager.ts`](src/lib/pager.ts) · 한 쪽 20편). 쪽마다 주소를 따로 내는 쪽(`/posts/page/2`)과 견줬는데, 이쪽이면 131편의 링크가 한 문서 안에 다 있어서 검색엔진이 한 번에 봅니다. 쪽 번호는 `?page=` 로 남습니다.
  ⚠ **자바스크립트가 죽으면 줄이 전부 보이고 쪽 단추가 안 나옵니다.** 안 눌리는 번호가 남아 있는 것보다 낫다고 보고 그렇게 뒀습니다.
  ⚠ **쪽 단추 모양은 `global.css` 의 `.pager` 입니다** — 두 지면이 같이 씁니다.
  ⚠ **`.rows .row` 로 찾습니다.** `.row` 만으로 찾으면 알약 줄의 감싸개까지 걸려서 두 번째 쪽부터 에디터 알약이 통째로 사라집니다 (실제로 그랬습니다).
- ⚠ **연도 묶음과 썸네일이 없어졌습니다.** 쪽이 나뉘면 한 쪽에 한두 해만 들어가서 소제목이 쪽마다 하나씩 떠 있는 꼴이 되고, 썸네일은 원래도 640px 아래에서는 빼고 있었습니다.
- ⚠ **여기는 지면이 아니라 서고입니다.** 카드를 두르지 않습니다 — 홈만 예외입니다.
- ⚠ **저자 이름은 저자 색이 아니라 본문 색입니다** (저자 지면의 큰 제목). 바로 위 알약이 이미 그 색으로 눌려 있어서, 제목까지 색이면 같은 색 덩어리가 둘이 됩니다.
- 폰에서는 알약 줄이 화면 밖으로 흐르고, **눌린 알약이 보이는 데까지 줄을 밀어 둡니다** (`EditorTabs` 의 script). ⚠ 거기서 `scrollIntoView` 를 쓰지 마세요 — 페이지까지 같이 스크롤됩니다.

### 서비스 지면 (`/projects`)

만든 것 셋을 보여주는 자리입니다. 홈·서고와 **같은 폭(1280px)·같은 여백**을
씁니다 (아래 띠는 이제 `BaseLayout` 이 모든 지면에 답니다). 페이지가 셋인데
(`/projects` · `/projects/<id>` · `/projects/<id>/about`) 서로 오가는 자리라
껍데기가 다르면 넘어갈 때마다 지면이 튑니다 — **셋을 같이 고치세요.**

```
/projects            사진 + 이름 + 소개 + 스택 + 데브로그 수  ← 가로 카드 셋
/projects/<id>       머리(사진·버튼) + 숫자 띠 + Devlog 목록
/projects/<id>/about 소개문 + 할 수 있는 것 + 스택 + 받는 곳
```

- ⚠ **여기는 카드를 두릅니다.** "목록에 상자를 두르지 않는다"(§4)는 글이 100개 넘게 늘어서는 지면 이야기입니다. 여기는 셋뿐이고, 사진·소개·스택·데브로그 수가 한 덩어리로 묶여야 서로 안 섞입니다.
- **썸네일은 그 프로젝트 최신 devlog 의 커버입니다.** 프로젝트 대표 사진을 따로 안 들고 있어서인데, 없는 사진을 만들어 두는 것보다 실제로 올린 화면이 나오는 편이 낫다고 봤습니다. 한 장도 없으면 `Cover` 가 프로젝트 색 판으로 채웁니다 (BeepTimer 가 그 경우입니다).
- ⚠ **카드 안 사진에 `aspect-ratio: auto` 만 주지 마세요.** 사진이 거꾸로 카드 높이를 끌고 갑니다 — 세로로 긴 폰 캡처가 커버인 MineApp 카드가 혼자 593px 이 됐습니다. 틀을 `position: absolute` 로 띄워서 높이를 못 정하게 하고, 칸 높이는 오른쪽 글이 정하게 둡니다.
- ⚠ **`/projects/<id>` 의 머리는 한 열입니다** (2026-09-08 시안). 이름·소개·스택·버튼이 폭을 다 쓰고, 사진은 그 아래에 **띠처럼** 깔립니다 — 큰 화면에서 `3 / 1` 입니다. 16/10 그대로 두면 1280px 폭에서 높이가 800px 이라 화면 하나를 통째로 먹습니다. ⚠ 그 비율은 `!important` 로 덮습니다 — `Cover` 가 비율을 인라인 style 로 달기 때문입니다.
- ⚠ **주 버튼("웹에서 바로 플레이" 같은 것)은 프로젝트 색이 아니라 강조색입니다.** 프로젝트 색으로 칠하면 같은 자리의 같은 동작이 지면마다 다른 색이 됩니다. `/projects/<id>/about` 의 「받는 곳」 버튼도 같은 색입니다 — **둘을 같이 고치세요.** 프로젝트 색은 알약과 Service 목록의 점에 그대로 남아 있습니다.
- ⚠ **숫자 띠는 칸마다 따로 선 카드이고, Devlog 목록도 카드입니다.** 한 판을 1px 로 가르던 모양에서 바꾼 것입니다 — 아래위가 "판 하나 / 카드 여럿" 으로 갈렸습니다.
- **숫자 띠의 플랫폼과 "최근 업데이트"는 코드가 채웁니다.** 가운데 칸만 `PROJECTS[].stats` 입니다.
- ⚠ **"최근 업데이트"는 마지막 devlog 날짜지 릴리즈 날짜가 아닙니다.** 정적 사이트라 스토어 배포일을 알 방법이 없어서 셀 수 있는 것만 셉니다 (홈의 번호를 순위로 안 쓰는 것과 같은 이유).
- **LIVE 배지는 `global.css` 의 `.live` 하나를 셋이 같이 씁니다.** ⚠ 배경은 **빨강(`--live-bg`)** 입니다 — 강조색이 파랑이 되면서, 링크·번호·주 버튼과 같은 파랑을 배지에까지 쓰면 "지금 되는 것" 이라는 표시가 안 읽힙니다 (개발자 빨강과 같은 값).
- 목록 카드와 프로젝트 머리의 소개 한 줄은 `tagline` + `summary` **첫 문장**입니다 (`src/lib/sentence.ts`). ⚠ 없는 문장을 새로 쓰지 말라고 그렇게 한 것입니다.

### 레이아웃 — 이제 3열 지면이 없습니다

⚠ **2026-09-08 에 `FeedLayout`(12열 그리드 · 오른쪽 열)과 `PostCard` 를
지웠습니다.** 마지막까지 쓰던 `/authors/<id>` 가 시안대로 한 열이 되면서
아무도 안 부르게 됐습니다. **되살리지 마세요** — 오른쪽 열이 하던 일(Editors ·
Service · Contact)은 메뉴 판과 아래 띠가 나눠 갖고 있습니다.

지금은 모든 지면이 이렇습니다.

| | |
|---|---|
| 폭 | `max-width: 1280px` (글 본문만 `56rem`) |
| 가로 여백 | `1.25rem`, 900px 부터 `2rem` |
| 헤더 | `height: 3rem` sticky |
| 아래 띠 | `BaseLayout` 이 **모든 지면**에 답니다 |

⚠ **지면에서 `<SiteFooter />` 를 또 부르지 마세요** — 두 번 나옵니다.

### 지면 — 카드가 아니라 줄입니다

⚠ **글 목록에 상자(카드)를 두르지 마세요.** 매거진들(the-edit·longblack·
magazine-b·folin)을 보고 정한 것입니다. 상자를 겹겹이 쌓는 것보다 **큰 제목 +
여백 + 얇은 구분선** 쪽이 글이 100개 넘을 때 덜 지칩니다.

⚠ **홈은 예외입니다.** 홈만 카드를 두릅니다 — 열여섯 편을 추려 놓은 지면이라
상자가 덩어리를 갈라 줍니다. `/posts` 처럼 **글을 다 늘어놓는 목록은 여전히
줄입니다.** 홈이 카드라고 해서 그쪽까지 카드로 바꾸지 마세요.

- **꼭지 크기는 전부 같습니다.** 첫 꼭지만 키우는 것(`lead`)을 넣었다 걷어냈습니다 — 매거진 관례지만 목록에서 그 한 줄만 튀어 보였습니다. 다시 넣지 마세요.
- **한 줄에 색은 저자 이름 하나뿐입니다.** 프로젝트 이름은 글자로만 둡니다 — 색이 둘이면 어느 쪽이 분류인지 안 읽힙니다
- 머리글은 아이콘 대신 **letter-spacing 준 대문자 작은 글자**(`LATEST`, `EDITORS`)

### 커버 사진

`heroImage` 를 지정한 글은 **0편**입니다. 그래서 한동안 "커버가 없다"고 보고
타이포그래피로만 지면을 만들었는데, 세어 보니 **128편 중 95편(74%)이 본문에
사진을 쓰고 있었습니다** (티스토리에서 받아온 404장).

그래서 [`src/lib/cover.ts`](src/lib/cover.ts) 가 순서대로 찾습니다.

1. `heroImage` — 손으로 고른 것
2. 본문에 나오는 첫 사진
3. 없음 — 그리는 쪽에서 **저자 색 판**으로 채웁니다

- ⚠ **기본 커버를 그림 파일로 만들지 마세요.** 한 장을 다크·라이트 양쪽에 쓰면 한쪽에서 반드시 튑니다 (어두운 판이 흰 배경에서 검은 사각형이 됐습니다). CSS 로 그리면 테마별로 밝기를 맞출 수 있고, 저자마다 색이 달라 33편이 다 같아 보이지도 않습니다.
- ⚠ **그 판에 글자를 넣지 마세요.** 커버 위에 제목을 얹는 지면에서 겹칩니다 (한 번 넣었다가 뺐습니다).
- ⚠ **비밀글은 본문을 뒤지지 않습니다.** 잠근 본문에서 뽑은 사진을 목록에 띄우면 잠근 의미가 없습니다 (§6-3 과 같은 이유).
- ⚠ **바깥 주소는 `src/lib/cover.ts` 의 `ALLOWED_HOSTS` 에 있는 것만 커버로 씁니다** — 지금은 `https://res.cloudinary.com/` 하나입니다 (§6-5). 티스토리 CDN 주소는 만료되면 404 라 여기를 `startsWith('http')` 로 넓히면 그 글 116편이 도로 걸립니다 (§5-2).
- 본문 사진은 `public/` 에 있는 원본이 그대로 나갑니다. **크기별로 뽑지 않습니다** — 목록에 사진을 여러 장 띄우기로 정하면 그때 Astro 자산으로 옮겨야 합니다.
- ⚠ **`heroImage` 는 Astro 자산이 아니라 공개 경로 문자열입니다** (`/images/<주소>/파일명`). `/admin` 의 사진 고르는 창이 그 경로를 적어 주는데 `image()` 는 공개 경로를 못 읽고 **빌드를 멈춥니다** (`ImageNotFound`). 사진 429장이 이미 다 `public/` 에 있어서 자산 쪽으로 옮길 수도 없습니다. 그래서 커버도 **크기별로 안 뽑힙니다** — 본문 사진과 같은 사정입니다.
- `/admin` 에 「커버 사진」 칸이 있습니다. 비우면 위의 2번(본문 첫 사진)으로 떨어집니다. **사진을 끌어다 놓아도 되고** 「이미지 선택」으로 골라도 됩니다 (§6-2).

### 글이 많아서 생긴 규칙

글이 128편이라 목록을 그냥 다 그리면 페이지가 감당이 안 됩니다.

| 화면 | 무엇을 |
|---|---|
| `/` | **최근 20편**만. 첫 꼭지는 크게, 끝에 "전부 보기" |
| `/posts` | **서고.** 한 줄씩 + 스무 편마다 쪽 나누기 |
| `/authors` | 저자 소개. 얼굴 + 이름 + 한 줄 (글 0편인 저자도 나옵니다) |
| `/authors/<id>` | 그 저자의 글. 서고와 같은 줄·같은 쪽 나누기 |

`/` 의 `RECENT` 상수를 건드릴 때는 서고가 있다는 걸 잊지 마세요 — 첫 화면을 늘리는 것보다 저자로 좁히는 쪽이 낫습니다.

### 폰트

**Pretendard Variable** 한 벌만 씁니다 (`src/assets/fonts/PretendardVariable.woff2`, 100~900).

라이선스는 **SIL OFL 1.1** 이고 `src/assets/fonts/Pretendard-LICENSE.txt` 에 동봉돼 있습니다. **이 파일을 지우지 마세요.** OFL은 폰트를 재배포할 때 라이선스 사본을 같이 넣도록 요구합니다.

---

## 5. 게임 저장소에서 devlog 뽑기 (게임 글을 쓸 때만)

게임 소스는 이 저장소에 없습니다. 전부 별도 비공개 저장소이고, 로컬에서는 형제 디렉토리에 있습니다.

```
~/Develop/Claude/
  ├── blog/            ← 여기
  ├── GridBrawl/       ← 사이트에 올라가 있음 (진행 중)
  ├── MineApp/         ← 사이트에 올라가 있음 (App Store 출시)
  ├── BeepTimer/       ← 사이트에 올라가 있음 (App Store 출시)
  ├── CoupleApp/       ← 안 올림. git 저장소도 아님
  ├── MiniGame_Speeder/  ← 안 올림
  └── TossTreasureHunt/  ← 안 올림
```

⚠ **만든 걸 전부 사이트에 올리지 않습니다.** 지금 보여줄 수 있는 셋(GridBrawl · MineApp · BeepTimer)만 `PROJECTS` 에 있습니다. 나머지 저장소의 글을 쓰려면 먼저 프로젝트를 추가해야 합니다 (§6).

글을 쓸 때는 해당 게임 저장소를 세션에 추가합니다.

```
/add-dir ~/Develop/claude/GridBrawl
```

### 절차

1. **커밋을 읽습니다.** 기억이 아니라 diff에서 출발합니다.
   ```bash
   git -C ~/Develop/claude/GridBrawl log --format='%ad | %s' --date=short -20
   git -C ~/Develop/claude/GridBrawl show <hash>
   ```
2. **글감이 되는 커밋을 하나 고릅니다.** 여러 커밋을 한 글에 욱여넣지 않습니다. "왜"가 있는 커밋 하나가 글 하나입니다.
3. **코드를 실제로 읽습니다.** 스니펫을 넣을 거면 파일을 열어서 확인합니다. **절대 지어내지 않습니다.**
4. `src/content/posts/<slug>.md` 로 씁니다. `author: developer` · `project: <id>`.
5. 사람이 읽고 고칩니다. 초안을 그대로 올리지 않습니다.

---

## 5-2. 티스토리에서 옮겨온 글

2026-08-12 에 `hyuk-todayfeelsogood.tistory.com` 의 글 **116개**를 옮겨왔습니다
(2022-03 ~ 2026-01). 파일 맨 위에 원본 주소가 주석으로 붙어 있습니다.

```
# 티스토리에서 옮겨왔습니다: https://hyuk-todayfeelsogood.tistory.com/122
```

- 옮긴 도구는 [`scripts/import-tistory.mjs`](scripts/import-tistory.mjs) 입니다. 한 번 쓰고 끝이지만, 다시 긁어야 할 때를 위해 남겨뒀습니다.
- 파일명은 **`<옛 카테고리 잎>-<티스토리 글번호>`** 입니다 (`flutter-122`, `swiftui-68`, `sdk-90`).
- 이미지 404장은 `public/images/<slug>/` 에 **받아서** 넣었습니다 (48MB).
  ⚠ **티스토리 CDN 주소를 그대로 쓰면 안 됩니다.** 서명된 URL 이라 `expires` 가 지나면 전부 404 가 되고, 서명을 뗀 주소도 404 입니다. 그래서 받아온 것입니다.
- `description` 은 대부분 비어 있습니다. 원본에 없었기 때문입니다 — 지어내지 말고, 손볼 때 채우세요.
- 티스토리에서 카테고리가 안 달려 있던 글 5개는 제목을 보고 정했습니다. 근거가 약한 곳이라 `import-tistory.mjs` 의 `UNCATEGORIZED` 표에 모아뒀습니다.

---

## 6. 프로젝트 추가

**고칠 곳은 두 군데입니다.**

1. `src/consts.ts` 의 `PROJECTS` 배열
2. `public/admin/config.yml` 의 `project` select — 같은 `id` 를 넣습니다

`PROJECTS` 항목 하나가 페이지 두 개를 만듭니다.

| 경로 | 무엇 | 쓰는 필드 |
|---|---|---|
| `/projects/<id>` | 그 프로젝트의 devlog 목록 | `name` `tagline` `platform` `stack` `color` `active` |
| `/projects/<id>/about` | **소개 페이지** — 받는 곳·플레이하는 곳 | 위 + `summary` `features` `links` |

devlog 페이지 **상단 카드를 누르면 소개 페이지로 갑니다.** 그래서 `summary`·`features`·`links` 를 비워두면 소개 페이지가 껍데기가 됩니다 — 새 프로젝트를 넣을 때 같이 채우세요.

- `summary` — 두세 문장. 뭘 하는 물건인지 + 왜 그렇게 만들었는지.
- `features` — 4~5줄. **구체적으로** 씁니다. "멀티플레이 지원"이 아니라 "방 코드 6자리로 친구를 부르거나, 봇과 연습할 수 있습니다".
- `links` — `kind` 는 `web` | `appstore` | `support`. 버튼 아이콘이 여기서 갈립니다.
- `storeName` — App Store 이름이 저장소 이름과 다를 때만 (예: MineApp → "지뢰찾기 아레나").
- `stats` — `/projects/<id>` 위쪽 숫자 띠의 **가운데 칸**입니다. 플랫폼과 최근 업데이트는 코드가 채우니 그 프로젝트에서만 나오는 것을 두세 개 씁니다. ⚠ **지어내지 마세요** — `summary`·`features` 나 devlog 에 이미 있는 사실만 옮겨 적습니다. 숫자 띠는 화면에서 제일 단정적으로 읽히는 자리라 틀리면 바로 티가 납니다.
- `active` — "지금 받아서 해볼 수 있는가". LIVE 배지가 붙습니다.
- `color` — 글 머리의 프로젝트 알약, Live Service 목록의 점, 소개 페이지 버튼 배경. (저자 색과는 다른 계통입니다 — §4)

**소개 문구는 지어내지 마세요.** 해당 저장소의 `README.md` 나 App Store 설명을 읽고 씁니다. 출시된 앱이면 번들 ID로 스토어 메타데이터를 그대로 확인할 수 있습니다.

```bash
curl -s "https://itunes.apple.com/lookup?bundleId=com.LimJH.BeepTimer&country=kr"
```

---

## 6-2. 폰에서 쓰는 `/admin`

글은 대부분 폰에서 `/admin` 으로 씁니다. Decap CMS 는 데스크톱 기준으로 만들어져 있어서,
폰에 맞추는 건 `public/admin/index.html` 의 `<style>` 한 덩어리가 전부입니다.
클래스 이름은 emotion 이 만든 해시(`css-1fehwpo-AppMainContainer`)라 뒤의 컴포넌트 이름만
부분 일치로 잡습니다 — **Decap 버전을 올리면 여기부터 확인하세요.**

지금 걸려 있는 것들과 그 이유:

| 무엇 | 왜 |
|---|---|
| 입력칸 글자 `16px` | iOS 사파리는 16px 미만 입력칸을 탭하면 화면을 확대합니다. Decap 기본값이 15px 이라 필드마다 확대됐습니다 |
| 사이드바를 통째로 감춤 | 컬렉션(글·에디터)이 **머리띠 탭**으로 올라갔습니다 (2026-09-09 시안). 안에 남은 것은 찾기 칸뿐이라 `skin.js` 가 제목 줄로 옮깁니다 (`moveSearch`) — 되살리려면 탭·찾기 칸을 같이 되돌리세요, 안 그러면 같은 것이 화면에 둘씩 보입니다 |
| 폰에서 머리띠 두 줄 | 탭이 넷(글·에디터·미디어·조회수)이 되면서 375px 에서 390px 로 넘쳤습니다 — 글자를 줄이고 여백을 걷어도 402px 이라 안 맞습니다. 첫 줄은 이름·해달·새 글·아바타, **둘째 줄이 탭**입니다. 옆으로 흘려 감추는 쪽(가로 스크롤)은 넷 중 둘이 화면 밖이면 있는 줄도 몰라서 버렸습니다 |
| 필드 간격 36 → 18px | 필드가 열 개라 기본 간격이면 스크롤이 한참입니다 |
| 본문 `min-height: 60vh` | 기본 300px 이면 대여섯 줄 만에 차고, 그때부터 작은 창 안에서 스크롤하며 쓰게 됩니다 |
| 사진 고르는 창 줄이기 | Decap 미디어 라이브러리가 920×576 고정입니다. 안쪽 줄들도 400px 로 박혀 있어서, 창만 줄이면 검색창과 "업로드"가 잘려 나갑니다 |
| 툴바 숨김 + `margin-top: 0` | **지금은 안 쓰이는 fallback 입니다** (본문 위젯을 갈아끼웠습니다 — 아래 참고). editor.js 가 죽어서 Decap 기본 편집기로 떨어질 때만 쓰입니다 |
| `white-space: nowrap` | "콘텐 츠""빠른 추 가""현재시 각"처럼 두 줄로 꺾였습니다 |
| 토글 두 개 `required: false` | Decap 의 boolean 은 **값이 없으면 "필수인데 안 채웠다"로 봅니다.** 손으로 쓴 글에는 `secret` 키가 없어서, 열어서 고치면 저장이 막히고 토글을 켰다 꺼야만 풀렸습니다 |
| 글쓰기 화면 폭 | 머리띠·목록이 1440px 인데 글쓰기만 1240/760px 이라, 큰 화면에서 혼자 좁아 보였습니다. 바깥을 1440 으로 맞추고 안쪽은 1200px·1600px 에서 넓힙니다 (760 → 940 → 1120). ⚠ **화면 폭을 다 쓰지는 않습니다** — 블로그 글 지면이 56rem(896px)이라 훨씬 넓히면 줄바꿈 위치가 올린 글과 달라집니다 |
| 토글은 상자에서 꺼냄 | Decap 은 위젯 종류를 안 가리고 전부 같은 상자에 앉힙니다. 스위치는 20px 짜리라 화면 폭을 다 쓰는 상자 안에 점 하나가 떠 있게 됩니다 (`:has([role='switch'])` 로 잡습니다 — iOS 15.4+) |

색과 폰트는 블로그에서 그대로 옮겨왔습니다 (`tokens.css` 의 값을 손으로 적었습니다).
**블로그 색이 바뀌면 `tokens.css` 와 `admin/index.html` 둘 다 고쳐야 합니다** —
2026-09-08 에 판 네 겹과 강조색(청록 → 파랑)을 시안 값으로 같이 옮겼습니다.
⚠ 그러면서 다크의 `--a-card`(#17181b)와 `--a-surface`(#1b1d20)가 아주 가까워졌습니다.
도구 띠와 본문을 가르는 것은 이제 색보다 **`--a-border` 선**입니다.
폰트도 마찬가지로 `public/fonts/PretendardVariable.woff2` 에 한 벌 더 있습니다 —
`src/assets/fonts/` 쪽은 Astro 가 해시 붙은 주소로 내보내서 정적 파일인 `/admin` 에서는
못 가져옵니다. 폰트를 갈아끼우면 두 곳 다 바꾸세요 (라이선스 사본도 같이 뒀습니다).

⚠ **테마는 직접 고릅니다** — 머리띠와 **글 쓰는 화면 위 띠**의 해/달입니다
(`skin.js` 가 답니다. 2026-08-25 · 글 화면은 2026-09-08). 고른 적이 없으면 OS 를
따르고, 블로그와 **같은 열쇠**(`localStorage.theme`)라 한쪽에서 고른 것이 다른
쪽에도 갑니다.

### 본문 편집기 — tiptap 입니다

**Decap 의 markdown 위젯을 통째로 갈아끼웁니다.** Decap 것은 양쪽 다 못
씁니다 — 리치 텍스트는 폰에서 한글 입력이 끊기고, 원문 모드는 툴바 버튼이
전부 `disabled` 로 렌더됩니다.

⚠ 2026-09-04 에 **Toast 에서 tiptap 으로 되돌아왔습니다.** 왜인지는 §8.

```
admin-src/index.js      ← 입구. CSS 를 끌어오고 아래 둘을 부릅니다
admin-src/editor.js     ← 본문 편집기
admin-src/blocks.js     ← 문단 종류 표 (제목 1~3 · 본문)   ┐ 하니 매거진에서
admin-src/shortcuts.js  ← 단축키                          ┘ 옮겨온 것
admin-src/upload.js     ← 떨어뜨린·붙여넣은 사진을 Cloudinary 로 (아래 참고)
admin-src/align.js      ← 문단 정렬 (아래 참고)
admin-src/table.js      ← 표 손잡이 (아래 참고)
admin-src/editor.css    ← 편집기 모양. 본문 안쪽은 블로그 `.prose` 그대로
admin-src/skin.js       ← 화면을 시안 모양으로 고쳐 앉히는 것 (아래 참고)
admin-src/media.js      ← 사진 폴더가 없는 글도 열리게 하는 것 (아래 참고)
admin-src/drafts.js     ← 임시저장 (아래 참고)
     ↓ npm run admin (esbuild)
public/admin/editor.js  ← 실제로 불러오는 것 (아홉이 한 덩어리로 묶입니다)
public/admin/editor.css ← index.html 이 <link> 로 겁니다
```

**커밋된 번들 둘을 지우지 마세요.** `dev`·`build` 가 먼저 `npm run admin` 을
돌리지만, Vercel 이 `astro build` 를 직접 부르면 그 단계가 안 돕니다.
그때는 커밋된 파일이 그대로 나갑니다.

⚠ **`editor.js` 는 CSS 를 import 하지 않습니다.** `scripts/check-md-roundtrip.mjs`
가 이 파일을 node 에서 그대로 불러다 쓰는데, node 는 `.css` import 를 못
읽습니다. CSS 는 `admin-src/index.js` 가 담당합니다 — 갈라 둔 이유가 그것입니다.

#### 임시저장 — `admin-src/drafts.js`

「저장」은 곧 GitHub 커밋입니다. 그 사이에 탭이 죽거나(폰에서는 다른 앱으로
넘어가기만 해도 사파리가 화면을 버립니다) 저장이 안 나가면(2026-09-07 에
실제로 그랬습니다 — §8) 쓰던 글이 통째로 사라졌습니다. Decap 은 고치던 것을
아무 데도 안 남깁니다 — 리덕스 안에만 있어서 새로고침 한 번이면 끝입니다.

그래서 **커밋하기 전에 한 벌을 `localStorage` 에 둡니다** (`lim.draft.v1:` 접두어).

| | |
|---|---|
| 남기는 칸 | 저자·제목·주소·한 줄 요약·커버 사진·본문 (skin.js 의 `WRITE_FIELDS` 와 같은 줄) |
| 언제 | 손을 멈추고 1.5초 · 계속 쳐도 10초에 한 번 · 탭이 죽기 직전 · 「임시저장」을 누를 때 |
| 되살리기 | 글을 열 때 파일과 다르면 폼 맨 위에 줄이 뜹니다 — 「되살리기 / 버리기」 |
| 지울 때 | 저장이 **성공**하면 버립니다. 남는 글 수는 10개 |

⚠ **이것은 백업이지 저장이 아닙니다.** 이 브라우저에만 있고 다른 기기에서는
안 보입니다. 사이트에 나가는 것은 여전히 「저장」뿐입니다 — **화면 문구에서
둘을 헷갈리게 쓰지 마세요.**

- ⚠ **값을 읽고 쓰는 길은 위젯이 직접 등록합니다** (`registerPort`). DOM 에서 input 을 찾아 값을 넣는 방법은 안 됩니다 — 커버 사진과 저자에는 input 이 아예 없고(§6-2 의 "라벨 for" 이야기와 같은 자리), React 가 들고 있는 값을 DOM 으로 밀어 넣는 것은 리덕스와 어긋납니다.
- ⚠ **`get()` 은 싸야 합니다.** skin.js 의 손질 한 바퀴에서 같이 도는데 그게 **글자를 칠 때마다**입니다. 그래서 본문의 `get()` 은 tiptap 을 다시 훑지 않고 마지막으로 내보낸 것(`lastEmitted`)을 돌려줍니다. 진짜 지금 것이 필요한 **남기기 직전에만** `flush()` 로 맞춥니다 — 안 그러면 마지막 150ms 안에 친 글자가 빠집니다.
- ⚠ **타이머를 다시 거는 조건은 "지난 바퀴에 본 것과 다른가" 입니다.** "남겨 둔 것과 다른가" 로 두면 화면이 바뀔 때마다 1.5초가 처음으로 되감겨서 **영영 안 터집니다** (실제로 그랬습니다 — 10초 천장에 걸릴 때만 남았습니다).
- ⚠ **되살릴지 물어보는 동안에는 아무것도 안 남깁니다.** 답하기 전에 덮으면 되살릴 것이 없어집니다.
- ⚠ **폼이 다 차려지기 전에 시작하지 않습니다.** 옛 글은 값이 늦게 옵니다 — 그 사이의 빈 폼을 남기면 멀쩡한 임시저장본을 덮어씁니다.
- ⚠ **저장이 실패하면 버리지 않습니다.** 성공·실패는 Decap 의 알림 클래스(`Toastify__toast--success`)로 가릅니다 — 판정을 우리가 따로 하면 언젠가 둘이 어긋납니다 (「저장하는 중…」과 같은 규칙 — §6-2). **클래스가 없으면 실패로 봅니다.**
- ⚠ **새 글에서 딴 글로 옮겨 갈 때 함부로 버리지 마세요.** 저장돼서 주소가 `new` → `entries/<주소>` 로 바뀐 것과, 그냥 옛 글을 한 번 열어 본 것을 구분해야 합니다 — 도착한 주소가 그 글의 `slug` 와 같을 때만 버립니다. 안 그러면 쓰다 만 새 글을 두고 다른 글을 보는 것만으로 남겨 둔 것이 사라집니다.
- **「발행 설정」(작성일·프로젝트·초안·비밀글)은 안 남깁니다.** 저장하기 직전에 정하는 것이라 되살릴 것이 없습니다.
- **원문 모드도 됩니다** — 옛 글은 늘 원문으로 열립니다(129편 중 119편). 되살릴 때 textarea 는 `defaultValue` 로 그린 것이라 값을 직접 써 넣습니다.

**머리띠의 「쓰다 만 글」 단추** (`skin.js` 의 `openDraftsPanel`)

되살리기 줄은 **지금 연 글**에 남은 것만 묻습니다. 그런데 새 글은
`posts/new` 한 자리에 쌓이고, 옛 글에 남긴 것은 **그 글을 다시 열기 전에는
있는지조차 알 수가 없습니다** — 목록 화면에는 아무 표시도 없었습니다. 그래서
머리띠에서 한눈에 보고 건너갑니다.

- ⚠ **남은 것이 없으면 단추를 감춥니다.** 되찾는 자리라, 되찾을 것이 없는데 "쓰다 만 글 0" 이 머리띠에 있으면 눌러 봐야 빈 판입니다.
- ⚠ **판은 `document.body` 에 답니다.** 「발행 설정」 모달과 다릅니다 — 그쪽은 React 가 그린 필드를 담고 있어서 글 화면(`pane`) 안이어야 하는데, 이 판은 우리가 만든 것뿐이고 **목록 화면에서도 열려야** 합니다.
- ⚠ **여기서 곧장 되살리지 않습니다.** 그 글로 건너가기만 하고, 되살릴지는 원래 있던 줄이 묻습니다 — 폼을 덮어쓰는 일이라 확인하는 자리가 하나만 있는 편이 낫습니다.
- ⚠ **폰에서는 글자를 감추고 숫자만 남깁니다.** 다 적으면 머리띠 오른쪽 끝이 44px 밀려서 「게시」와 아바타가 화면 밖으로 나갑니다 (재서 확인). 그래서 글자와 숫자를 **다른 span 에** 담습니다 — 한 덩어리면 CSS 로 못 가릅니다. 읽어 주는 이름(`aria-label`)에는 그대로 다 있습니다.

⚠ **글 → 글은 주소만 바꿔서는 안 넘어갑니다.** Decap 이 주소는 바꿔 놓고 폼은
**옛 글 그대로** 둡니다 — 고칠 것이 없는 깨끗한 글에서도 그렇습니다(재현해서
확인). 그러면 엉뚱한 글 위에 "쓰다 만 글이 있습니다" 가 떠서, 되살리면 남의
글을 덮어씁니다. **목록을 한 번 거치면** 넘어갑니다(`goToDraft`). 그래도 안
바뀌면 「주소」 칸을 보고 통째로 다시 엽니다 — 조용히 엉뚱한 글을 보여 주는
쪽이 제일 나쁩니다.

#### 문단 정렬 — `admin-src/align.js`

도구 띠의 **왼쪽 · 가운데 · 오른쪽**입니다 (2026-09-07). 왼쪽은 "감싼 것을
푸는 것"이라 눌림 표시가 없습니다.

⚠ **문단에 속성만 붙이는 방식은 안 됩니다.** 그쪽이 쓰기에는 더 좋은데,
`@tiptap/markdown` 이 `paragraph` 같은 기본 토큰의 렌더러를 먼저 잡아 두고
그것만 씁니다 (`registry` 가 `nodeTypeRegistry` 보다 우선 — 번들에서 확인했고
실제로 가로채 보려다 실패했습니다). 그래서 **새 이름의 블록으로 감쌉니다**.

```
<div class="ta-center"><p><strong>굵게</strong>와 <span class="c-teal">색</span></p></div>
```

- ⚠ **안쪽까지 HTML 입니다.** CommonMark 는 HTML 블록 **안의 마크다운을 안 읽습니다** — `<div>**굵게**</div>` 는 블로그에서 별표가 그대로 보입니다. 그래서 `generateHTML` 로 안쪽을 통째로 HTML 로 바꿔 넣습니다. 되읽는 쪽은 그 HTML 을 그대로 파싱해서 마크·제목·사진이 다 살아 돌아옵니다 (재 보고 확인).
- ⚠ **정렬을 준 덩어리만 HTML 로 나갑니다.** 안 준 문단은 하나도 안 건드립니다 — 사진 크기(`<img width>`)와 같은 규칙입니다. 왕복 검사 숫자가 그대로인지(129편 중 119 / 76) 꼭 확인하세요.
- ⚠ **`generateHTML` 에 넘길 확장 묶음을 모듈 변수로 들고 있습니다** (`setAlignExtensions`). `renderMarkdown` 안에서는 `this.options` 가 없어서입니다 — 라이브러리가 그 함수를 확장 컨텍스트가 아닌 핸들러에 매어 부릅니다.
- ⚠ **CSS 가 두 곳입니다** — 블로그 `global.css` 의 `.prose .ta-*` 와 편집기 `admin-src/editor.css` 의 같은 규칙. 한쪽만 고치면 쓸 때와 올린 뒤가 갈라집니다.
- **단축키는 안 매어 뒀습니다.** 도구 띠 단추뿐입니다.

#### 표 손잡이 — `admin-src/table.js`

행·열을 넣고 지우는 것은 **표에 직접 붙은 손잡이**가 합니다 (2026-09-07,
노션과 같은 모양). 열 손잡이는 표 위, 행 손잡이는 표 왼쪽이고, 누르면 그
줄이 통째로 골라지면서 「왼쪽에 넣기 / 오른쪽에 넣기 / 지우기」가 뜹니다.
오른쪽 끝과 아래의 **＋** 는 끝에 한 줄 더 붙입니다.

- ⚠ **문서를 하나도 안 건드립니다.** 손잡이는 `.lim-md-editor` 안에 겹쳐 놓는 판이고, 자리는 칸의 `getBoundingClientRect()` 로 잽니다. 저장되는 마크다운도 왕복 검사 숫자도 그대로입니다.
- ⚠ **판(`.lim-tbl`)은 `pointer-events: none` 이어야 합니다.** 안 그러면 표를 덮어서 칸을 눌러도 커서가 안 들어갑니다. 손잡이만 다시 켭니다.
- ⚠ **열린 메뉴는 DOM 이 아니라 "어느 손잡이"로 들고 있습니다.** 손잡이를 누르면 칸 선택이 바뀌고 그게 곧 다시 그리기라, 메뉴 노드를 들고 있으면 그 자리에서 지워집니다 (실제로 한 번 그렇게 안 떴습니다).
- ⚠ **명령을 `editor.chain().focus()` 로 부르지 마세요.** 포커스가 먼저 가면서 방금 잡은 칸 선택이 풀려서, 「이 열 지우기」가 커서 있던 칸 하나만 지웁니다. prosemirror-tables 명령을 직접 부릅니다.
- ⚠ **`requestAnimationFrame` 으로 모으지 마세요.** 화면이 안 보이는 동안 안 돌아서 손잡이가 옛 자리에 멈춥니다 (`skin.js` 와 같은 함정).
- **폰에서는 hover 가 없습니다.** 그래서 **고른 자리가 든 표**에도 손잡이를 그립니다 — 칸을 한 번 탭하면 뜹니다. 375px 에서 손잡이와 메뉴가 편집기 안에 들어오는 것을 재서 확인했습니다.
- ⚠ **`.ProseMirror` 의 좌우·위 여백(22px)이 손잡이가 설 자리입니다** (14px 손잡이 + 4px 간격). 줄이면 손잡이가 편집기 테두리 밖으로 나갑니다.
- **안 한 것: 끌어서 자리 바꾸기.** 노션은 손잡이를 끌어 열 순서를 바꾸는데, 그건 칸 내용을 통째로 옮기는 일이라 따로 짜야 합니다.

도구 띠 아래 줄에는 **머리행 · 표 지우기** 둘만 남습니다.

⚠ **「합치기」를 다시 넣지 마세요.** GFM 표에는 `colspan` 을 적을 자리가
없어서 편집기에서만 합쳐지고 저장하면 도로 갈라집니다 — 재현해서 확인했습니다
(`<th colspan="2">` 가 `| 가 |  |` 로 나갔습니다). 그래서 표는 **언제나 반듯한
격자**입니다.

#### 사진을 떨어뜨리기·붙여넣기 — `admin-src/upload.js`

편집기에 사진 파일을 **끌어다 놓거나 ⌘V 로 붙이면** Cloudinary 로 올라가고
커서 자리에 들어갑니다 (2026-09-07). 「사진」 단추도 그대로 있습니다.

⚠ **저장소로 올리는 길은 안 씁니다.** Decap 위젯이 주는 `onPersistMedia` 는
base64 커밋이라, 사진을 Cloudinary 로 옮긴 이유(§6-5)와 정면으로 부딪힙니다.
Decap 이 쓰는 Cloudinary 미디어 라이브러리 위젯도 `show()`/`hide()` 뿐이라
손에 든 File 을 건넬 수가 없습니다 (번들에서 확인). 그래서 브라우저에서
Cloudinary 로 **바로** 올리고, 그러려면 unsigned 업로드 프리셋이 필요합니다
(`config.yml` 의 `upload_preset` · §6-5).

- ⚠ **단추로 넣은 사진과 같은 주소 모양이어야 합니다.** 단추 쪽은 Cloudinary 가 만들어 둔 derived 주소(`asset.derived[0]`)라 변환이 이미 붙어 있고, 직접 올리면 원본 주소만 돌아옵니다 — 그대로 두면 폰 원본 5MB 가 본문에 박힙니다. `config.yml` 의 `default_transformations` 를 읽어서 같은 조각을 끼워 넣습니다 (`c_limit,f_auto,q_auto,w_1600`).
- ⚠ **「올리는 중」 표시를 본문 노드로 넣지 마세요.** blob: 주소를 가진 임시 사진을 끼워 넣으면, 올리는 동안 글을 치는 순간 그게 마크다운으로 나가서 **파일에 blob: 주소가 저장됩니다.** ProseMirror decoration 으로 둡니다 — 문서가 아니라 화면에만 있어서 저장될 수가 없고, 그동안 글을 쳐도 자리가 알아서 따라 움직입니다.
- ⚠ **여러 장은 차례로 올립니다.** 넣은 다음 자리를 `selection.to` 로 잡습니다 — `from` 은 방금 넣은 사진 **앞**이라, 그걸 쓰면 두 번째 장이 첫 번째 앞으로 들어갑니다 (재현해서 고쳤습니다).
- ⚠ **사진이 아닌 파일도 기본 동작을 막습니다.** 안 막으면 브라우저가 그 파일로 페이지를 넘겨서 **쓰던 글이 통째로 날아갑니다.**
- ⚠ **원문 모드에도 같은 것이 달려 있습니다** (`editor.js` 의 `onRawDrop`·`onRawPaste`). 거기는 그냥 textarea 라 안 막으면 위와 같은 일이 납니다. **옛 글은 늘 원문으로 열립니다**(129편 중 119편) — 드물게 쓰는 자리가 아닙니다. 자리표시를 문서에 둘 수 없어서(글자를 넣으면 그게 곧 저장될 원문입니다) 도구 띠 아래 한 줄로 알립니다.
- **붙여넣기는 사진이 있으면 같이 온 `text/html` 을 버립니다.** 웹에서 사진을 복사하면 남의 서버 `<img>` 가 딸려 오는데, 그걸 그대로 걸면 언젠가 404 가 됩니다 (티스토리 CDN 과 같은 자리 — §5-2).
- 한 장 10MB 까지입니다 (unsigned 업로드 한도). 넘으면 올리기 전에 알립니다.

#### 사진 폴더가 없는 글 — `admin-src/media.js`

`media_folder` 가 글마다 갈라져 있어서 (`/public/images/{{fields.slug}}`)
Decap 은 **글을 열 때** 그 글의 사진 폴더를 훑습니다. 그런데 사진을 한 장도
안 쓴 글은 폴더가 없습니다 — 지금 128편 중 34편입니다.

`npm run cms` 의 로컬 프록시는 없는 폴더에 **500 을 냅니다**. `getMedia` 가
튕기면 글 읽기 전체가 같이 튕겨서, 화면이 하얗게 남고 "항목 불러오기 실패"
만 뜹니다. 그래서 **없는 폴더 = 사진 없음** 으로 넘깁니다.

- ⚠ **`.gitkeep` 으로 폴더를 미리 만들지 마세요.** 사진 없는 글이 계속 나오는 한 끝나지 않습니다.
- ⚠ **`proxy` 백엔드만 감쌉니다.** 배포에서 쓰는 GitHub 백엔드는 404 를 받고 이미 빈 목록으로 넘깁니다. 거기까지 감싸면 진짜 실패(토큰 만료·네트워크)도 "사진 없음"으로 삼켜서, 사진이 있는데 없는 것처럼 보입니다.
- 삼킬 때 `console.warn` 을 남깁니다. **지우지 마세요** — 폴더가 없는 것과 프록시가 죽은 것을 화면에서는 구분할 수 없습니다.

저장되는 건 여전히 **마크다운 원문**입니다. 글 128편이 마크다운 파일이라
형식을 못 바꿉니다.

**⚠ 왕복은 공짜가 아닙니다.** 마크다운 → 문서 → 마크다운을 거치면 목록 표시
(`-   ` → `- `)나 구분선(`* * *` → `---`)이 tiptap 방식으로 다시 쓰입니다.
한 글자만 고쳐도 파일 전체가 바뀐 것으로 남습니다.

그래서 **글을 열 때 한 번 되돌려 보고, 원본과 다르면 원문 모드로 엽니다**
(`isRoundTripSafe`). 직접 쓴 글은 서식으로 열리고, 티스토리에서 옮겨온 글은
원문으로 열립니다. "원문"/"서식" 단추로 언제든 바꿀 수 있습니다.

```bash
node scripts/check-md-roundtrip.mjs          # 어떤 글이 안 돌아오는지
node scripts/check-md-roundtrip.mjs --diff   # 달라지는 줄까지
```

지금 기준으로 **128편 중 119편이 모양이 달라지고, 76편은 글자까지 달라집니다.**
(Toast 때는 123 / 21 이었습니다 — **알고 되돌아온 것입니다**, §8.)

#### 단축키 — 하니 매거진에서 옮겨온 것

`admin-src/blocks.js` · `admin-src/shortcuts.js` 는
`~/Develop/Company/hani/magazine` 의 같은 이름 파일을 옮긴 것입니다. 둘 다
순수 tiptap 확장이라 React 없이 그대로 돕니다.

| | |
|---|---|
| ⌘⌥1~3 · ⌘⌥0 | 제목 1~3 · 본문 |
| ⌘B · ⌘I · ⌘⇧X · ⌘E | 굵게 · 기울임 · 취소선 · 인라인 코드 |
| ⌘⌥⇧1~5 · ⌘⌥⇧0 | 글자 색 다섯 · 색 빼기 |
| ⌘⌥Y · ⌘\ | 형광펜 · 서식 지우기 |
| ⌘⇧B · ⌘⇧8 · ⌘⇧7 · ⌘⇧9 | 인용 · 글머리 · 번호 · 할 일 |
| ⌘⌥C · ⌘K · ⌘⌥P | 코드블록 · 링크 · 사진 |

- ⚠ **⌘⌥1~3 을 다시 매는 이유**: StarterKit 의 Heading 이 ⌘⌥1~6 을 h1~h6 에 이미 매어 둡니다. 화면 이름은 「제목 1~3」이고 문서로는 h2~h4 라, 그대로 두면 한 칸씩 밀립니다. `priority` 를 올려야 Heading 보다 먼저 키를 봅니다.
- ⚠ **단추와 키가 같은 함수를 부르게 두세요.** 따로 적으면 언젠가 둘이 다르게 동작합니다. 툴팁에 적는 글자도 실제로 매어 둔 키와 같아야 합니다.
- ⚠ **한글 조합 중에도 듣게 따로 손을 봤습니다** (`shortcuts.js` 의 `ImeShortcuts`). ProseMirror 는 조합(IME) 중에 들어온 키를 통째로 무시하는데, 한국어는 마지막 글자가 거의 항상 조합 중이라 **단축키가 거의 안 들었습니다**. ⌘·Ctrl 이 눌린 키에 한해 조합을 먼저 끝내고 넘깁니다 — Enter·Backspace 까지 손대면 한글 입력이 망가집니다.
- ⚠ **⌘⌥C(코드블록)는 브라우저가 먼저 가져갑니다.** 크롬·사파리에서 개발자도구를 여는 자리라 페이지에서 못 막습니다. 도구 띠의 「코드블록」 단추를 쓰세요.
- **하니에 있지만 안 가져온 것**: 폰트 · 줄간격 · 콜아웃 · 토글 · 본문 크기 5단계. 전부 **마크다운에 담을 자리가 없습니다** — 하니는 문서를 JSON 으로 저장해서 되는 것이고 여기는 `.md` 파일입니다. 넣으면 편집기에서는 보이는데 저장하면 사라집니다. (**정렬만 2026-09-07 에 넣었습니다** — 아래 `align.js`. 마크다운에 자리가 없는 것은 같은데, 사진 크기처럼 그 덩어리만 HTML 로 내보내는 길이 있었습니다.)
- **하니 도구 띠(React 1,092줄)는 안 가져왔습니다.** `/admin` 에는 쓸 수 있는 React 가 없습니다.

**여기서 더한 것: 인용문을 `| ` 로 시작하기** (`editor.js` 의 `QuoteBar`).

줄 맨 앞에서 `|` 를 치고 스페이스를 누르면 인용문이 됩니다. 마크다운 기본인
`> ` 도 그대로 됩니다 — StarterKit 의 Blockquote 가 매어 둔 것이라 **빼지
않았습니다.**

- ⚠ **저장되는 마크다운은 언제나 `> ` 입니다.** 바뀌는 것은 치는 방법뿐이고 파일에 `|` 가 남지 않습니다 (왕복 검사 숫자도 그대로 — 130편 중 119 / 76).
- ⚠ **표 안에서도 먹습니다.** 칸 맨 앞에서 `| ` 를 치면 그 칸 안에 인용문이 생깁니다 — 기본 `>` 도 원래 그렇습니다. 되돌리기(⌘Z) 한 번으로 `|` 가 돌아옵니다.
- 단축키(⌘⇧B)와 도구 띠의 「인용」은 그대로입니다. 툴팁에 이 방법도 같이 적어 뒀습니다.

#### 그 밖에

- ⚠ **원문 모드에서는 tiptap 이 들고 있는 것을 밖으로 흘려보내지 마세요.** 그건 파일의 글이 아니라 tiptap 이 다시 쓴 것입니다 — 손으로 고친 원문이 덮입니다. `flush()` 가 원문 모드에서 그냥 돌아서는 이유입니다.
- **도구 띠 아래 한 줄은 고른 것에 따라 바뀝니다** — 사진을 고르면 크기, 코드 블록 안이면 언어. 둘 다 고르기 전에는 쓸 일이 없는 칸이라 상시로 두지 않았습니다 (단추가 이미 스물셋입니다). 아무것도 안 골랐으면 줄 자체가 없습니다.
  - **사진 크기는 `<img width>` 로 나갑니다.** `![설명](주소)` 에는 크기를 적을 자리가 없어서, 크기를 준 사진만 HTML 로 내보냅니다. ⚠ **100% 는 크기를 지우는 것**이고 그래야 `![...]` 로 돌아갑니다 — HTML 로 적힌 사진이 늘면 그만큼 왕복이 나빠집니다. 읽어 들이는 쪽은 원래 됐습니다 (`@tiptap/extension-image` 가 `width` 를 들고 있습니다).
  - **코드 언어는 ```swift 의 그 글자입니다.** 색칠은 편집기에서 안 보입니다 — 블로그는 Shiki 가 빌드할 때 칠하고, 그걸 편집기에 넣으려면 하이라이터를 하나 더 얹어야 합니다 (번들만 커집니다).
- ⚠ **도구 띠는 줄바꿈합니다. 넘치는 단추를 "..." 안에 숨기지 마세요.** Toast 를 쓰던 동안 제일 자주 쓰는 「사진」이 거기 숨어서 매번 두 번 눌러야 했습니다. 단추가 늘면 띠가 한 줄 더 길어지는 편이 낫습니다 (폰에서 네 줄, 23개가 다 보입니다).
- ⚠ **편집기 안쪽 모양은 블로그 `.prose`(global.css)를 그대로 옮긴 것입니다** (`admin-src/editor.css`). 편집기와 올라간 글이 다르면 게시해 봐야 확인이 되고, 그러면 위지윅을 쓰는 이유가 없어집니다. **`.prose` 를 고치면 여기도 같이 고치세요.**
- ⚠ **Decap 은 `h1…h6, p` 에 글자색을 전역으로 깝니다.** 물려받는 색은 직접 적힌 색을 못 이겨서, 편집기 안에서 `color: inherit` 을 도로 걸지 않으면 어두운 배경에 어두운 글자가 됩니다 (재 보니 `rgb(49,61,62)` 였습니다).
- **글자 색은 `<span class="c-teal">` 처럼 클래스로 넣습니다.** 색 값을 본문에 박으면 다크·라이트 한쪽에서 반드시 안 읽힙니다. 색을 늘리려면 `editor.js` 의 `COLORS`, `global.css` 의 `.prose .c-*`, `editor.css` 의 `.c-*`, 그리고 `shortcuts.js` 가 보는 순서까지 같이 봅니다.
- **형광펜은 `<mark>` 입니다.** tiptap 기본 Highlight 는 `==글자==` 인데 이 블로그의 remark 는 그 표기를 모릅니다.
- ⚠ **확장을 뺄 때 조심하세요.** 표(`TableKit`)를 안 넣었더니 표가 있는 글을 열었다 저장할 때 표가 통째로 사라졌습니다. StarterKit 에 없는 것은 직접 넣어야 합니다.
- **사진은 Cloudinary 로 올라갑니다** (§6-5). 저장소를 안 거치니 **주소를 먼저 적지 않아도** 사진 단추가 열립니다 — 2026-09-07 이전과 다릅니다. 단추 말고 **끌어다 놓거나 붙여넣어도** 됩니다 (위 `upload.js`).
- **「커버 사진」 칸도 끌어다 놓을 수 있습니다.** Decap 의 `image` 위젯을 감싸서(`editor.js` 의 `LimImageControl`) 본문과 **같은 길**로 Cloudinary 에 올립니다 — 그래야 단추로 고른 것과 주소 모양(변환 조각)이 같습니다. ⚠ `dragover` 와 `drop` 을 **둘 다** 막아야 합니다. 안 막으면 브라우저가 그 파일로 페이지를 넘겨서 쓰던 글이 날아갑니다. ⚠ Decap 의 `onPersistMedia`(저장소로 base64 커밋)를 쓰지 마세요 — §6-5 와 정면으로 부딪힙니다.
- ⚠ **editor.js 가 안 불러와지면 Decap 기본 raw 편집기로 떨어집니다.** 툴바만 없어지고 글은 계속 쓸 수 있습니다. `config.yml` 의 `modes: ['raw']` 와 index.html 의 `.cms-editor-raw` 규칙은 그때를 위해 남겨 둔 것입니다.
- ⚠ **Decap 은 React 를 밖으로 내보내지 않습니다.** 그래서 `React.Component` 를 이미 등록된 위젯(`CMS.getWidget('string').control`)의 프로토타입에서 꺼내 씁니다. Decap 을 올리면 여기부터 확인하세요.

### 화면 껍데기 — `admin-src/skin.js`

2026-08-25 에 목록·편집 화면을 시안대로 다시 앉혔습니다. **CSS 만으로는 안 되는
것 셋**을 이 파일이 합니다.

| 무엇 | 왜 CSS 로 안 되나 |
|---|---|
| 목록 한 줄을 네 칸으로 (제목·작성자·날짜·상태) | Decap 은 `<h2>` 에 **글자 한 덩어리**로 그립니다. `config.yml` 의 `summary` 를 ` · ` 로 쪼개 span 넷으로 다시 놓습니다 |
| 편집 화면을 시안 차례로 세우고 분류를 「발행 설정」 모달로 내리기 | 필드 열 개가 `config.yml` 차례대로 한 줄로 놓입니다. 묶어서 옮기려면 감싸는 상자가 있어야 합니다 |
| 「저장」을 가로채 발행 설정을 먼저 묻기 | Decap 의 저장은 드롭다운입니다. 사이에 무엇을 끼울 자리가 없습니다 |
| 저자를 알약, 프로젝트를 라디오로 (`select` 위젯 갈아끼우기) | react-select 는 눌러야 펴집니다. 둘·셋뿐인데 매번 세 동작입니다 |
| 본문 아래 글자 수·저장 여부 | Decap 에 없습니다. 긴 글에서는 위 띠가 화면 밖으로 나가 저장됐는지 안 보입니다 |
| 머리띠에 컬렉션 탭(글·에디터) | Decap 은 컬렉션 **전부**를 「콘텐츠」 링크 하나로 묶고 어느 쪽인지는 사이드바에서 고르게 합니다. 눌림 표시(`header-link-active`)도 어느 컬렉션이든 켜져 있어서 못 씁니다 — 감추고 우리가 답니다 |
| 찾기 칸을 제목 줄로 옮기기 | Decap 은 사이드바 안에 그립니다. 사이드바를 감추면 검색이 같이 사라집니다 (글이 131편이라 검색 없이는 못 찾습니다) |
| 표 감싸개에 이름 붙이기(`.lim-table`) | 머리 줄과 줄들을 한 판에 넣어야 하는데, 감싸개가 **클래스 없는 div** 라 CSS 에서 잡을 이름이 없습니다 |
| 머리띠 높이 재기(`--lim-head`) | 조회수 지면이 머리띠 아래에 붙는 고정 판입니다. 폰에서 두 줄이 되면서 60px 이 아니라 81px 이 됐는데, 숫자를 CSS 에 또 적으면 한쪽만 고쳤을 때 지면 위가 잘립니다 |
| 제목·한 줄 요약의 안내글(placeholder) | Decap 은 placeholder 를 안 답니다 |
| "lim's World \| Admin" · 해/달 단추 · 상태 알약 | Decap 에 아예 없는 것들입니다 |
| 글 쓰는 화면 위 띠에도 해/달 | 글을 열면 머리띠 자리를 위 띠가 덮습니다 — 머리띠에만 달면 글을 쓰는 동안 테마를 못 바꿉니다 |

- **필드를 가르는 기준은 이름입니다.** emotion 해시나 순서에 안 기댑니다. ⚠ `config.yml` 에 필드를 더하면 `WRITE_FIELDS` 도 같이 보세요 — 안 적힌 것은 「발행 설정」으로 갑니다 (사라지지는 않습니다).
- ⚠ **이름은 라벨의 `for` 에서 뽑습니다.** 입력칸 id 로 찾으면 **사진 위젯을 놓칩니다** — 그건 input 을 안 그려서 `<이름>-field-N` 이 어디에도 없고, 그래서 커버 사진 칸이 「발행 설정」으로 떨어졌습니다. 라벨은 위젯 종류를 안 가리고 언제나 `for="<이름>-field-N"` 을 답니다.
- ⚠ **글자 수는 `contenteditable`·`textarea` 중 화면에 보이는 쪽을 셉니다.** 문서 차례로는 tiptap 이 먼저라 그냥 첫 번째를 잡으면 원문 모드에서 안 보이는 쪽을 셉니다. textarea 는 글자를 쳐도 DOM 이 안 바뀌어 MutationObserver 가 안 깨어나므로 `input` 도 따로 듣습니다.
- ⚠ **알약·라디오·글자 수 줄을 「위젯 상자」 규칙에서 빼세요** (`index.html` 의 `:not(.lim-pills):not(.lim-radios):not(.lim-foot)`). 안 빼면 그 줄들에도 카드 배경과 테두리가 칠해집니다.
- ⚠ **`[id^='title-field-']` 같은 규칙에는 부모를 하나 붙이세요.** 바로 위의 `[class*='ControlContainer'] > input` 이 태그를 하나 더 들고 있어 특이도가 높습니다 — 그냥 두면 제목이 16px 로 눌립니다. 커버 판의 점선도 같은 이유로 밀렸습니다.
- ⚠ **해/달은 두 곳에 있어도 만드는 함수가 하나입니다** (`themeButton`). 그리고 `paintThemeButton` 은 화면에 있는 것을 **전부** 칠합니다 — 하나만 칠하면 다른 쪽이 옛 그림으로 남습니다. 글 화면에서의 자리는 `index.html` 의 `order: -2` 가 정합니다 (상태 알약도 그 칸 맨 앞에 끼어드는데 어느 쪽이 먼저 붙는지가 글마다 다릅니다).
- ⚠ **`summary` 의 구분자(` · `)를 바꾸면 `skin.js` 의 `SEP` 도 같이 바꿔야 합니다.** 지금 제목 128개에 그 글자가 들어간 것은 없습니다.
- ⚠ **React 가 그린 노드를 지우지 않습니다.** 목록 제목 글자도 지우지 않고 CSS 로 감추기만 합니다 — 지우면 정렬을 바꿨을 때 React 가 화면에 없는 노드에 새 제목을 써서 줄이 안 바뀝니다.
- ⚠ **MutationObserver 를 지역 변수에 두지 마세요.** 표준이 "다른 참조가 없으면 지워도 된다"고 허용합니다.
- ⚠ **모으는 데 `requestAnimationFrame` 을 쓰지 마세요.** 화면이 안 보이는 동안(다른 앱으로 넘어갔을 때)은 rAF 가 아예 안 돌아서, "모으는 중" 표시만 켜진 채 멈춥니다. 실제로 이것 때문에 **처음 한 번만 손질되고 그 뒤로는 아무 일도 안 일어났습니다.** 타이머를 씁니다.
- **여기가 죽어도 Decap 은 굴러갑니다.** 전부 try/catch 안이고, 못 고치면 Decap 기본 모양이 그대로 보입니다 (목록은 `제목 · 저자 · 날짜 · false` 한 줄이 됩니다).

#### 「에디터」 화면 (2026-09-09)

컬렉션이 **둘**이 됐습니다 — 「글」과 「에디터」. 값은 `src/data/authors.json`
파일 하나이고 목록 위젯으로 늘립니다 (§3).

⚠ **skin.js 의 손질은 거의 다 「글」 전용입니다.** 목록 한 줄을 네 칸으로
가르는 것도, 폼을 한 열로 세우고 나머지를 「발행 설정」으로 내리는 것도.
그대로 두면 에디터 칸이 전부 모달로 끌려 들어가 화면에서 사라집니다. 그래서
`inPostEntry()`·`inPostList()` 가 **주소를 먼저 봅니다**.

- ⚠ **다른 목록으로 넘어가면 단 것을 걷어냅니다** (`.lim-thead`·`.lim-count`). React 는 제가 만든 노드를 안 지웁니다 — 안 걷으면 「에디터」 화면에 「제목·작성자·날짜·상태」 줄과 「131개」가 그대로 남습니다.
- ⚠ **`[class*='ListCard']` 는 줄만 잡는 이름이 아닙니다.** 안쪽 링크(`ListCardLink`)와 제목(`ListCardTitle`)까지 걸립니다 — `:not(.lim-row)` 를 붙였더니 링크에서 참이 되면서 **글 목록의 원래 요약 글자가 제목 위에 다시 보였습니다.** `li` 를 꼭 앞에 붙이세요.
- ⚠ **임시저장도 글만 남깁니다** (`drafts.js` 의 `entryKey`). 에디터 폼에는 남길 칸이 하나도 없어서 「쓰다 만 글」에 이름 없는 줄만 쌓입니다.
- ⚠ **「새 글」은 언제나 글입니다** (`findCollection('posts')`). 화면에 처음 보이는 컬렉션 링크를 따라가면 엉뚱한 데서 새 항목을 만듭니다.
- 위 띠의 「글 편집」은 `html[data-lim-collection]` 으로 갈립니다 — 에디터를 고치는 동안에는 「에디터」입니다.
- 목록 위젯의 줄·판 색도 `data-lim-collection='editors'` 안에서만 칠합니다. ⚠ Decap 기본은 **밝은 회색 판**이라 다크에서 글자가 묻힙니다 (접힌 줄은 `#dfdfe3`, 펼친 판은 흰색 — 재 보고 확인).
- **미리보기는 껐습니다** (`editor: preview: false`). 켜 두면 화면이 반으로 갈리고 오른쪽에 방금 친 글자가 한 번 더 나옵니다.

##### 두 화면을 다시 그립니다 — `admin-src/editors.js`

Decap 이 그리는 것은 **파일 하나짜리 줄**(목록)과 **목록 위젯**(고치는
화면)입니다. 목록 화면에는 「에디터」라고 적힌 줄 하나만 있어서, 누가 있는지
보려면 눌러 들어가야 했습니다.

```
#/collections/editors                  사람마다 카드 (얼굴·이름·소개·글 수·편집·삭제)
#/collections/editors/entries/authors  줄 + 펼치면 두 칸 폼
```

- ⚠ **목록 카드의 명단은 마지막 배포 기준입니다** (`authors.gen.js`). `/admin` 은 정적 페이지라 `authors.json` 을 읽을 방법이 없습니다 (§3 의 `config.yml` 과 같은 사정). 대신 **고치는 화면을 한 번 열면** 그때 본 명단을 이 브라우저에 적어 두고(`lim.editors.v1`) 다음부터 그것을 먼저 씁니다 — 폰에서 사람을 더한 뒤 목록으로 돌아왔을 때 없는 사람처럼 보이지 않게. 「글 N편」만 언제나 gen 에서 옵니다 (글 수는 폼에 없습니다).
- ⚠ **카드에서 값을 고치지 않습니다.** 「편집」·「삭제」·「에디터 추가」는 전부 고치는 화면으로 건너가 **Decap 의 진짜 단추를 누릅니다** — 폼을 거치지 않으면 리덕스가 든 값과 어긋납니다 (저장을 대신 눌러 주는 것과 같은 방법).
- ⚠ **글이 딸린 사람은 「삭제」가 막습니다.** 그대로 지우면 빌드가 멈추는데(`z.enum` — §3), 폰에서는 배포하고 나서야 알게 됩니다. 지우는 것으로 저장까지 되지도 않습니다 — 「게시」를 눌러야 파일에 남습니다.
- ⚠ **펼친 폼을 두 칸으로 세우는 이름은 매 손질마다 다시 붙입니다** (`lim-egrid`). Decap 이 펼칠 때와 접을 때 감싸는 div 를 갈아 끼워서, 접힌 채로 붙여 둔 이름이 펼치는 순간 사라집니다 (실제로 그랬습니다 — 칸이 한 줄로 늘어섰습니다).
- ⚠ **줄의 차례는 `order` 로 정합니다** (끌개 · 얼굴 · 이름 ‥ 글 수 · 펼치기 · ✕). 접기·지우기 단추는 React 것이라 DOM 에서 옮기면 다음 그리기에 제자리로 돌아옵니다. 둘은 생김새가 같아서 **차례**로 가릅니다 — 첫째가 접기, 마지막이 지우기입니다.
- ⚠ **바깥 판을 벗기는 규칙은 특이도를 넘겨야 합니다.** 「위젯 상자」 규칙이 `:not()` 을 여섯 개 달고 있어서 (0,7,0) 입니다 — 그냥 쓰면 져서 판이 그대로 남습니다 (!important 는 둘 다 붙어 있습니다).

#### 「발행 설정」은 저장할 때 묻습니다

작성일 · 프로젝트 · 초안 · 비밀글 넷은 **글을 쓰는 동안에는 볼 일이 없고
저장하기 직전에만 정하는 것들**입니다. 본문 아래에 늘 펼쳐 두면 폰에서
스크롤만 길어지고, 정작 저장할 때는 위 띠만 보고 눌러서 초안 토글을 그냥
지나칩니다. 그래서 「저장」을 누르면 모달로 묻습니다.

- **Decap 의 「저장」은 사실 드롭다운입니다** (`PublishButton`). 눌러야 메뉴가 열리고 「지금 게시」를 한 번 더 눌러야 합니다. 그 **첫 번째 누름**을 가로채 모달을 열고, 모달의 「저장」이 원래 순서(단추 → 첫 메뉴 항목)를 대신 밟습니다.
- ⚠ **「지금 게시」를 찾는 데 시간을 넉넉히 주세요 — 저장이 실제로 나가는 곳입니다.** 예전에는 30ms 씩 열두 번(360ms)만 보고 포기했는데, 글이 길거나 기기가 느리면 React 가 그 안에 드롭다운을 못 그립니다. 그러면 **저장이 통째로 안 나갑니다** — 2026-09-07 에 실제로 그랬습니다(§8). 빈 글은 6ms 면 뜨는데 그 숫자를 믿은 것이 화근이었습니다. 지금은 **MutationObserver 로 기다리고 6초**를 씁니다.
- ⚠ **못 찾았을 때 조용히 넘어가지 마세요.** 저장이 시작조차 안 된 것입니다. 화면에 알리고(`say()`), **다음 「저장」 한 번은 가로채지 않아서** Decap 의 원래 두 단계로 저장할 수 있게 둡니다.
- ⚠ **드롭다운을 찾는 선택자를 느슨하게 풀지 마세요.** `[role='menuitem']` 만으로 찾으면 오른쪽 위 아바타 메뉴의 **「로그아웃」이 걸립니다.** 반드시 `[class*='DropdownList']` 안에서 찾습니다.
- ⚠ **`PublishButton` 일 때만 가로챕니다.** 고칠 것이 없는 글에서는 같은 자리가 `PublishedToolbarButton`(메뉴에 「복제」뿐)입니다 — 거기까지 가로채면 복제를 못 합니다. 두 이름은 서로 부분문자열이 아닙니다 (재 보고 확인했습니다).
- ⚠ **`document` 의 캡처 단계에서 잡습니다.** React 는 루트 컨테이너에 듣기 때문에 그보다 먼저 걸립니다. 여기서 `stopPropagation` 을 해야 드롭다운이 안 열립니다.
- ⚠ **모달은 `pane` 안에 둡니다** (자리는 `position: fixed`). 밖으로 옮기면 글 화면이 닫힐 때 React 가 `pane` 만 걷어가고 모달은 남습니다 — 안에 든 것이 React 가 그린 필드 칸이라 유령이 됩니다.
- ⚠ **드롭다운의 나머지 둘(「게시하고 새로 만들기」·「게시하고 복제」)은 이제 못 누릅니다.** 혼자 쓰는 저장소에서 쓸 일이 없다고 보고 버렸습니다. 되살리려면 모달 아래에 줄로 다세요 — 가로채기를 푸는 쪽으로 돌아가지 마세요.
- **모달 안에 필수 칸을 두지 마세요.** 저장이 검사에 걸리면 모달은 이미 닫힌 뒤라 어느 칸이 문제인지 안 보입니다. 지금 셋(프로젝트·초안·비밀글)은 전부 `required: false` 입니다.

#### 저장했다는 것을 알려 주는 자리

**판정은 Decap 것을 씁니다.** 저장이 됐는지 아는 것은 Decap 뿐이고, 끝나면
알림을 띄웁니다 — 성공 "항목 저장됨", 실패 "이런! 필수 필드를 놓치셨습니다"
(번들의 ko 로케일). 우리가 따로 판정하면 언젠가 둘이 어긋납니다. 그래서
**보이는 자리만** 손봅니다.

- **누르고 나서 결과가 올 때까지 「저장하는 중…」 입니다.** 배포의 GitHub 백엔드는 저장이 곧 커밋이라 몇 초 걸립니다. 예전에는 모달을 그 자리에서 닫아 버려서 **그동안 화면에 아무 일도 안 일어났습니다** — 됐는지 안 됐는지 알 방법이 없었습니다. ⚠ 글자만 바꾸지 말고 단추를 **눌리지 않게** 막으세요. 안 막으면 기다리다 한 번 더 눌러 저장이 두 번 나갑니다.
- ⚠ **알림을 보는 감시자는 "저장을 기다리는 중"일 때만 반응해야 합니다** (`saving` 플래그). 알림은 저장 말고도 뜨는데, 아무 때나 모달을 닫으면 `closeSaveModal()` 이 초점을 위 「저장」 단추로 옮겨서 **글을 쓰던 중에 커서가 튑니다.**
- ⚠ **빠져나갈 길을 두세요** (20초). 알림이 영영 안 오면 모달이 「저장하는 중…」에 갇힙니다. 풀어 주되 **됐다고 알리지는 않습니다.**
- **실패해도 모달을 닫습니다.** 걸리는 칸(본문 같은 것)은 모달 밖이라 열어 둔 채로는 어디가 문제인지 볼 수가 없습니다.
- **알림 모양은 `index.html` 에서 이 화면 색으로 맞춥니다** (`.Toastify__*`). 기본값은 흰 글자에 초록/빨강이라 다른 사이트에서 온 것처럼 보였습니다. 성공은 왼쪽에 강조색 선, 실패는 붉은 선.

#### 작성일은 화면에 없습니다

물어볼 것이 아닙니다 — 새 글이면 `config.yml` 의 `default: '{{now}}'` 가 지금
날짜를 넣고, 옛 글이면 파일에 적힌 값이 그대로 있습니다. 예약 발송도 안 되는
값이라(§3) 고를 이유가 없어서 「발행 설정」에서 뺐습니다.

- ⚠ **감추기만 합니다. 안 그리면 안 됩니다.** `{{now}}` 를 진짜 시각으로 바꿔 주는 것은 Decap 의 DateTime 위젯이고, 그 일은 위젯이 **mount 될 때** 일어납니다 (번들에서 확인 — `componentDidMount` 에서 값이 `{{now}}` 면 지금으로 바꿉니다). 칸을 안 그리면 새 글의 작성일이 `{{now}}` 라는 **글자 그대로** 저장되고 `z.coerce.date()` 가 그걸 못 읽어 빌드가 깨집니다. 그래서 `.lim-hidden`(display:none) 안으로 옮기기만 합니다.
- ⚠ **`widget: hidden` 으로 바꾸지 마세요.** 같은 이유입니다 — 그 위젯은 `{{now}}` 를 풀어 주지 않습니다.
- ⚠ **"저장할 때마다 지금 시각으로" 로 바꾸지 마세요.** 옛 글의 오타 하나를 고쳐도 날짜가 오늘로 바뀌어 서고·홈·RSS 맨 위로 올라옵니다. 티스토리에서 옮겨온 116편은 2022~2026 의 원본 날짜입니다 (§5-2).

#### 프로젝트는 라디오입니다

저자(알약)와 같은 이유로 `select` 위젯을 갈아끼웁니다. 셋뿐인데 눌러서
펴고 고르고 접히는 것이 매번 세 동작이었습니다.

- ⚠ **맨 아래 「없음」은 `config.yml` 에 없는, `skin.js` 가 붙이는 칸입니다.** 라디오는 눌러서 끄는 것이 없어서, 이 칸이 없으면 한 번 고른 뒤로 영영 못 비웁니다. 값은 빈 문자열이고 스키마가 그걸 "값 없음"으로 봅니다 (§3).
- ⚠ **라디오의 `name` 은 `forID` 를 씁니다.** 고정 문자열을 주면 프로젝트 칸이 둘 이상 뜰 때 서로를 끕니다.

`index.html` 이 하는 일도 같이 늘었습니다.

- **테마를 직접 고릅니다** (머리글의 해/달). 블로그와 **같은 열쇠**(`localStorage.theme`)를 써서 같은 주소끼리 값을 나눠 씁니다 — 블로그에서 밝게 보고 있었으면 여기도 밝게 열립니다. 색은 세 번 적습니다(기본 라이트 · OS다크 · 강제다크). ⚠ **어떤 색도 다크 블록에만 적지 마세요.**
- ⚠ **Decap 을 겨냥한 규칙에는 `!important` 를 붙이세요.** emotion 이 만드는 `<style>` 이 이 `<style>` **뒤에** 들어가서, 특이도가 같으면 그쪽이 이깁니다 (`justify-content` 하나 빠뜨렸다가 단추가 화면 가운데에 섰습니다).
- **글자를 바꿔 다는 것은 `font-size: 0` + `::before`** 로 합니다 ("빠른 추가"→"새 글", "글 컬랙션에 작성하는 중"→"글 편집", "항목 삭제"→"삭제", 저장 단추). React 가 다시 그려도 그대로 남습니다.

`config.yml` 쪽에서 알아둘 것:

- **`주소`(slug) 필드가 필수입니다.** 제목에서 파일명을 뽑으면 한글 주소가 나옵니다. `^[a-z0-9]+(-[a-z0-9]+)*$` 로 막아 뒀습니다. 사진 폴더 이름도 이 값입니다.
- **비어 있으면 `editor.js` 가 시각으로 채웁니다** — `2026-08-25-0930`. 그대로 두면 주소가 그 값이 되고, 고치고 싶으면 지우고 쓰면 됩니다. 왜 제목·번호가 아니라 시각인지는 `admin-src/editor.js` 의 `LimStringControl` 위에 적어 뒀습니다.
  ⚠ **`widget` 은 `string` 그대로 둬야 합니다.** 채우는 일은 등록된 `string` 위젯을 갈아끼워서 합니다 — `config.yml` 에 새 위젯 이름을 적으면 `editor.js` 가 안 떴을 때 폼이 통째로 깨집니다 (지금은 칸이 빈 채로 뜨는 게 전부입니다).
  ⚠ **값이 있는 칸은 안 건드립니다.** 옛 글을 열었을 때 주소가 바뀌면 그 자리에서 404 가 됩니다.
- **기본 정렬은 `index.html` 의 인라인 스크립트가 정합니다.** Decap 3.9 는 `sortable_fields` 에 `default` 를 넣으면 "must be an array"로 설정을 통째로 안 읽습니다. `decap-cms.entries.sort` 를 처음 한 번만 채워 최신 글이 위로 오게 합니다.
- **폼을 고쳤는데 폰에서 안 바뀌면** `index.html` 의 `config.yml?v=` 숫자를 올리세요. `editor.js?v=` 도 같은 이유로 붙어 있습니다.

---

## 6-3. 비밀글

`secret: true` 를 붙이면 **본문이 잠깁니다.** `/admin` 에는 "비밀글로 두기" 토글이 있습니다.

**제목·날짜·요약·저자는 그대로 보입니다.** 목록에 자물쇠만 붙습니다. 티스토리 보호글과
같은 규칙이고, 감출 내용은 **본문에만** 쓰라는 뜻입니다. RSS 에서는 통째로 빠집니다 —
피드 리더는 비밀번호를 물어봐 주지 않으니 제목만 흘리는 항목이 남기 때문입니다.

### 비밀번호

글마다 다르지 않습니다. `SECRET_POST_PASSWORD` 하나로 전부 잠급니다 — 글마다 다르면
어딘가에 적어둬야 하고, 그러면 저장소에 비밀번호가 남습니다.

배포는 **Vercel → Settings → Environment Variables** 에 `SECRET_POST_PASSWORD` 로 넣습니다
(Production · Preview · Development 전부). 환경변수는 새 빌드부터 적용되니 넣은 뒤 한 번
다시 배포해야 합니다.

로컬에서도 비밀글을 보려면 저장소 맨 위에 `.env` 를 만들고 **배포와 같은 값**을 적습니다.
값이 다르면 로컬에서 잠근 글을 배포본에서 못 엽니다. `.env` 는 gitignore 돼 있습니다.

```bash
echo 'SECRET_POST_PASSWORD=여기에비밀번호' > .env
```

⚠ **비밀글이 있는데 이 값이 없으면 빌드가 멈춥니다.** 그냥 넘어가면 "비밀글"이라고
써놓고 본문은 다 보이는 상태로 배포됩니다. 그게 제일 나쁩니다.

### 어떻게 잠기는가

"JS로 가려두기"는 비밀글이 아닙니다 — 페이지 소스에 본문이 그대로 있습니다. 정적
사이트라 서버에서 막을 수가 없으니, **본문 자체를 암호문으로 내보냅니다.**

| 단계 | 어디 |
|---|---|
| 빌드할 때 잠금 (PBKDF2-SHA256 20만 회 → AES-GCM) | [`src/lib/secret.ts`](src/lib/secret.ts) |
| 읽는 사람 브라우저에서 풀기 | [`SecretGate.astro`](src/components/SecretGate.astro) 의 `<script>` |

잠글 대상은 `post.rendered.html` 입니다 — Astro 가 마크다운을 렌더해 엔트리에 담아둔
것으로, 코드 하이라이팅까지 끝난 상태입니다. 컨테이너 API 로 다시 그릴 필요가 없습니다.

- **비밀번호 확인 코드가 따로 없습니다.** 틀리면 AES-GCM 이 인증 태그에서 걸려
  예외를 던집니다. 못 풀면 틀린 겁니다.
- 한 번 열면 브라우저 `localStorage` 에 남아서 다음부터 안 묻습니다. 본문 아래
  "이 기기에서 비밀번호 지우기"로 지웁니다. 비밀번호를 바꾸면 옛 값은 자동으로 지워집니다.
- `crypto.subtle` 은 **보안 컨텍스트(https·localhost)에서만** 있습니다. 배포는 https 라 괜찮습니다.

⚠ `src/lib/secret.ts` 를 클라이언트 코드에서 import 하지 마세요. 비밀번호가 번들에
박혀서 나갑니다. `<script>` 안에는 암호문만 넘어갑니다.

---

## 6-4. 폰으로 보는 블로그

- ⚠ **오른쪽 열이 이제 없습니다** (2026-09-08 · `FeedLayout` 을 지웠습니다 — §4).
  Service·Contact 는 메뉴 판과 아래 띠에 있고, 아래 띠는 모든 지면에 붙습니다.
- **저자 알약 줄은 화면 양끝까지 흘립니다** (`margin-inline: -1.25rem`). 안쪽에서 딱
  끊기면 그게 끝인 줄 알거든요. 잘린 항목이 보여야 옆으로 더 있다는 게 읽힙니다.
  ⚠ **눌린 알약이 화면 밖이면 거기까지 줄을 밀어 둡니다** (`EditorTabs` 의 script).
  375px 에서 「개발자」가 그 경우였습니다.
- **본문 리듬은 640px 아래에서 좁힙니다** (`global.css`). 티스토리에서 옮겨온 글은 한 줄이
  곧 한 문단이라, 데스크톱 간격을 그대로 쓰면 한 화면에 열 줄쯤밖에 안 들어갑니다.

---

## 6-5. 사진은 Cloudinary 로 올라갑니다

2026-09-07 부터 `/admin` 에서 올린 사진은 **저장소를 안 거칩니다.**
`public/admin/config.yml` 의 `media_library` 한 덩어리가 전부입니다.

**왜 옮겼나.** GitHub 백엔드는 사진 한 장을 base64 커밋으로 올립니다. 폰
원본이 3~5MB 라 올리는 데 한참 걸리고, 올린 만큼 `.git` 이 계속 불어납니다
(`public/images` 54MB · `.git` 68MB). 게다가 `media_folder` 가
`{{fields.slug}}` 라 **주소를 먼저 적어야** 사진 단추가 열렸습니다.

- ⚠ **`<script>` 를 더 걸지 마세요.** `decap-cms@3.9.0` 번들에 이미 들어 있고 `registerMediaLibrary(cloudinary)` 로 등록까지 돼 있습니다 (번들에서 확인). 위젯 본체만 열 때 `media-library.cloudinary.com` 에서 받아옵니다.
- ⚠ **`api_secret` 을 `config.yml` 에 적지 마세요.** 저장소가 공개이고 이 파일은 그대로 배포됩니다. `cloud_name`·`api_key` 는 브라우저에 나가는 값이라 공개돼도 됩니다 — 올리려면 Cloudinary 로그인이 따로 필요합니다.
- **돌려받는 것은 전체 https 주소입니다.** 번들에서 확인한 기본값이 `use_secure_url: true` · `use_transformations: true` · `output_filename_only: false` 라, `default_transformations` 를 먹인 주소가 그대로 본문에 들어갑니다.
- **`default_transformations` 로 원본을 줄입니다** — 긴 변 1600px(`crop: limit` 이라 작은 사진은 안 늘립니다) · 화질과 형식은 브라우저에 맞춰 자동. 폰 원본을 그대로 거는 것을 막는 자리입니다.
- ⚠ **`src/lib/cover.ts` 의 `ALLOWED_HOSTS` 와 한 쌍입니다.** 커버는 본문 첫 사진에서 뽑는데(§4) 그게 이제 바깥 주소입니다. 여기를 안 뚫으면 **새 글은 커버가 전부 저자 색 판**이 됩니다. 올리는 곳을 바꾸면 두 곳을 같이 고치세요.
- ⚠ **저장소 안의 사진 429장은 고르는 창에 안 나옵니다.** 전역 `media_library` 가 Decap 내장 라이브러리를 통째로 대신합니다. 이미 본문에 박힌 주소라 화면에는 그대로 뜨고, **다시 골라 넣을 수만** 없습니다.
- ⚠ **옛 사진을 옮기지 않았습니다.** 옮겨 봐야 `.git` 히스토리에 남아서 용량이 안 줄고(`filter-repo` 를 돌려야 합니다), 만료 없는 주소를 굳이 만료될 수 있는 곳으로 옮기는 셈입니다. **새 사진만** 갑니다.
- ⚠ **되돌리려면 `config.yml` 의 `media_library` 를 지우는 것으로 끝나지 않습니다.** `editor.js` 의 `openMedia()` 에 있던 "주소를 먼저 적어 주세요" 가드를 같이 되살려야 하고(안 그러면 사진이 `public/images` 바닥에 쌓입니다), **떨어뜨리기·붙여넣기(`admin-src/upload.js`)도 같이 걷어내야 합니다** — 그쪽은 Cloudinary 로만 올립니다. 컬렉션의 `media_folder`·`public_folder` 두 줄은 그대로 남겨 뒀습니다.

### `upload_preset` — 떨어뜨리기·붙여넣기에만 쓰는 값

**「사진」 단추는 이 값 없이도 돕니다.** 그쪽은 Cloudinary 창에서 로그인해서
올리니까요. 편집기에 **떨어뜨리거나 붙여넣을 때만** 필요합니다 (§6-2) —
그때는 브라우저가 파일을 직접 보내는데, 로그인 창을 띄울 자리가 없어서
unsigned 로 올립니다.

만드는 곳: Cloudinary → Settings → Upload → Upload presets → Add upload
preset → **Signing Mode 를 Unsigned 로.**

- ⚠ **이 이름은 공개됩니다.** 저장소가 공개이고 `config.yml` 은 그대로 배포돼서, `cloud_name` 과 이 이름만 있으면 남도 이 프리셋으로 올릴 수 있습니다. **막는 자리는 코드가 아니라 프리셋 설정입니다** — 폴더를 고정하고, 최대 용량과 허용 형식을 좁혀 두세요.
- ⚠ **`api_secret` 은 여전히 어디에도 적지 않습니다.** unsigned 는 secret 없이 올라갑니다.
- **서명 방식으로 바꾸려면** `api_secret` 을 들고 있는 서버리스 함수가 하나 더 필요합니다 (`api/` 에 붙이면 됩니다 — OAuth 두 개가 이미 거기 있습니다). 대신 로컬 `npm run cms` 에는 `/api` 가 없어서 떨어뜨리기를 배포에서만 확인하게 됩니다. **그 대가 때문에 지금은 unsigned 입니다.**

**대가는 이것입니다.** 지금까지는 저장소만 있으면 사진이 안 죽었습니다.
이제 Cloudinary 계정이 막히거나 한도를 넘으면 새 사진이 404 입니다 —
티스토리 CDN 때문에 404장을 받아온 것(§5-2)과 같은 자리에 서는 셈입니다.
**알고 고른 것입니다.**

### 사진 창을 열면 콘솔에 뜨는 CORS·404 — 우리 것이 아닙니다

「사진」을 누르면 콘솔에 이런 것이 서너 줄씩 쌓입니다. **고칠 수 있는
것이 아니고, 아무것도 안 망가집니다.** 다음에 또 보고 파헤치지 마세요.

```
Access to fetch at 'https://gw.console.cloudinary.com/mediaflows/api/v2/flows/damapp/m1zcmsux'
  from origin 'https://console.cloudinary.com' has been blocked by CORS policy
GET .../mediaflows/api/v2/flows/damapp/m1zcmsux  net::ERR_FAILED 404
[Unicorn] Rollbar log sent  {level: 'error', …}
```

- **부르는 쪽도 받는 쪽도 Cloudinary 입니다.** origin 이 `console.cloudinary.com`(사진 고르는 창 iframe)이고 대상이 `gw.console.cloudinary.com` 입니다. 우리 페이지가 낸 요청이 아닙니다 — `lim-it.vercel.app` 은 여기 없습니다.
- **MediaFlows 라는 딴 제품을 찾아보는 것입니다.** `flows/damapp/<cloud_name>` — 우리 계정에 그런 flow 가 없으니 404 입니다. 안 쓰는 기능이라 만들 이유도 없습니다.
- **CORS 로 보이는 것은 그 404 응답에 헤더가 안 붙어서입니다.** 로그인 없이 같은 주소를 부르면 401 이 오는데 그때는 `access-control-allow-origin: https://console.cloudinary.com` 이 붙어 있습니다 (재 보고 확인). 즉 정상 응답에는 헤더가 있고 404 에만 빠집니다 — **Cloudinary 서버가 정하는 것이라 우리 쪽에서 붙일 자리가 없습니다.**
- `[Unicorn] Rollbar` 는 Cloudinary 자기네 오류 수집기입니다. "preloaded but not used" 42줄도 그쪽 앱 것입니다.
- ⚠ **`config.yml` 을 만져서 없앨 수 없습니다.** `cloud_name`·`api_key`·`upload_preset`·`default_transformations` 중 어느 것도 이 probe 를 켜고 끄지 않습니다. 창을 열면 그쪽 코드가 자기 판단으로 부릅니다.

### 「이미지 선택」으로 넣을 때 터지던 것 — Decap 을 감쌌습니다

**콘솔 오류(위)와 다른 이야기입니다.** 이쪽은 진짜로 안 됐습니다 — 창에서
사진을 올리고 고른 다음 Insert 를 누르면 아무것도 안 들어갔습니다.

Decap 3.9 가 고른 사진을 주소로 옮기는 자리는 이렇습니다 (번들 그대로).

```js
(asset.derived && use_transformations ? asset.derived[0] : asset)[...]
```

`derived` 가 **없을** 때는 원본으로 잘 떨어지는데 **빈 배열**이면 `[]` 가
참이라 `[][0]`, 즉 `undefined` 를 읽습니다 —
`TypeError: Cannot read properties of undefined (reading 'secure_url')`.
**방금 올린 사진**은 변환본이 아직 없어서 여기 걸립니다. 그래서 이미 있던
사진을 고르면 되고 새로 올린 것만 안 됐습니다.

고치는 자리는 `admin-src/upload.js` 의 `guardMediaLibrary()` 입니다.

- ⚠ **Decap 안은 못 고칩니다** — `insertHandler` 가 모듈 안에 닫혀 있습니다. Cloudinary 위젯을 만들 때 그 handler 를 **감싸서**, 자산을 넘겨주기 전에 손봅니다.
- **`derived` 가 비었으면 우리가 채웁니다.** `default_transformations` 를 주소 조각으로 옮겨서(`withTransform`) 넣습니다 — Cloudinary 가 만들어 줬을 주소와 같은 모양입니다. **`derived` 가 아예 없을 때도 채웁니다**: 그때 Decap 은 원본 주소로 떨어지는데, 그러면 폰 원본이 그대로 본문에 박힙니다.
- 이제 **「사진」 단추 · 끌어다 놓기 · 커버 사진**이 전부 같은 주소를 만듭니다 (`…/c_limit,f_auto,q_auto,w_1600/…`).
- ⚠ **`window.cloudinary` 를 getter 로 감쌉니다.** Cloudinary 스크립트가 `window.cloudinary = {}` 를 먼저 놓고 `createMediaLibrary` 를 **나중에** 얹어서, set 만 지켜보면 놓칩니다 (실제로 놓쳤습니다).
- ⚠ **`use_transformations: false` 로 끄는 쪽은 안 골랐습니다.** 그러면 터지지는 않지만 주소에 변환이 안 붙어서 폰 원본 3~5MB 가 그대로 걸립니다.

---

## 6-6. 조회수

2026-09-08 부터 글마다 조회수를 셉니다. 쓰임은 둘입니다 — **홈의 「인기 글」
순서**와 **글 머리에 찍히는 숫자**. 전체 표는 `/admin` 에서만 봅니다.

```
글을 열면      PostLayout 의 script → POST /api/views  {slug}  → 올린 뒤의 값
이미 센 기기면 script → GET /api/views?slug=<s>          → 그 글 하나의 숫자
홈이 줄 세울 때 script → GET /api/views                   → 인기순 slug 목록 (숫자 없음)
내가 볼 때      /admin 의 「조회수」 탭 → GET /api/views?full=1 + 열쇠
```

⚠ **글 한 편의 숫자는 2026-09-08 시안 때문에 공개로 열었습니다.** 그전에는
POST 가 204 를 돌려주고 공개 GET 은 순서만 줬는데, 시안이 글 머리에 조회수를
찍어 놨습니다. **전체 표는 여전히 열쇠가 있어야 합니다** — 한 편씩 물어보는
것과 131편을 통째로 받아 가는 것은 다른 이야기입니다.

| 어디 | 무엇 |
|---|---|
| [`api/views.js`](api/views.js) | 세고 읽는 함수 (Vercel) |
| [`src/pages/ranks.json.ts`](src/pages/ranks.json.ts) | 주소·제목·날짜·커버·저자를 담은 표 |
| [`src/layouts/PostLayout.astro`](src/layouts/PostLayout.astro) | 글을 열 때 하나 올리는 script |
| [`src/pages/index.astro`](src/pages/index.astro) | 카드 넷을 인기순으로 갈아 끼우는 script |
| [`admin-src/views.js`](admin-src/views.js) | `/admin` 의 조회수 대시보드 (탭) |

### 저장소는 Upstash Redis 입니다

해시 **둘**입니다 — `views` 에 `slug → 횟수`, `views:day` 에
`YYYY-MM-DD → 그날 전체 횟수`. **SDK 를 안 넣었습니다** — REST 로 명령
배열(`["HINCRBY","views","<slug>",1]`)을 보내면 끝이라 `fetch` 로 충분합니다.
글을 한 번 열 때 올리는 것이 둘이라 `/pipeline` 로 한 번에 보냅니다.

⚠ **날짜별은 글 × 날짜로 안 셉니다.** 사이트 전체 합계 하나뿐입니다 —
대시보드가 그리는 것은 "요즘 얼마나 읽히나" 한 줄이고, 글마다 나누면 열쇠가
글 수 × 날짜 수로 불어납니다.

⚠ **날짜는 한국 시각(UTC+9)입니다.** Vercel 함수는 UTC 로 도니까 9시간을
더해서 자정에 날을 가릅니다 — `api/views.js` 의 `today()` 와 `admin-src/views.js`
의 `todayKST()` 가 **같은 규칙이어야** 「오늘」 칸이 안 어긋납니다.

⚠ **날짜별은 2026-09-08 부터 쌓입니다.** 그전에 센 것은 날짜가 없습니다 —
막대의 합과 「모두」가 다른 것이 정상입니다.

Vercel 환경변수 (Production · Preview · Development):

| 이름 | 무엇 |
|---|---|
| `KV_REST_API_URL` · `KV_REST_API_TOKEN` | Vercel 마켓플레이스에서 Upstash 를 붙이면 이 이름으로 들어옵니다 |
| `UPSTASH_REDIS_REST_URL` · `UPSTASH_REDIS_REST_TOKEN` | Upstash 콘솔에서 직접 만들었을 때. **둘 중 아무 쪽이나** 받습니다 |
| `VIEWS_TOKEN` | 숫자를 보는 열쇠. `/admin` 에서 한 번 넣습니다 (**무작위 12자 이상** — 아래) |

⚠ **`VIEWS_TOKEN` 은 길이보다 무작위인가가 중요합니다.** 지키는 것이 전체
표 하나뿐이라 `openssl rand -hex 8` 정도면 충분합니다 — 폰에서 한 번 치는
값이니 길 이유가 없습니다. **말이 되는 값(`lim2026` 같은 것)만 피하세요.**
`sameToken` 이 길이가 다르면 먼저 false 를 돌려주기 때문에 **열쇠 길이는
밖에서 알 수 있습니다** — 무작위가 아니면 그만큼 좁혀집니다.

⚠ **바꾸면 다시 배포해야 합니다** (환경변수는 새 빌드부터). 브라우저마다
한 번씩 다시 넣는데, 옛 열쇠는 403 을 받고 그 자리에서 다시 묻습니다 —
따로 지울 필요는 없습니다.

⚠ **없어도 사이트는 굴러갑니다.** GET 은 빈 목록을 주고(홈이 최신순 그대로),
POST 는 501 입니다. 세는 곳이 아직 없는 것이 사실이라 그렇게 뒀습니다 —
500 을 내면 홈 콘솔에 빨간 줄만 남습니다.

⚠ **버린 것: 사진처럼 저장소만 있으면 되던 성질.** Upstash 계정이 막히거나
한도를 넘으면 그동안 센 것이 사라집니다. 글은 여전히 `.md` 파일이고 조회수만
거기 있으니, **없어져도 지면이 안 깨지게** 만들어 뒀습니다 (위의 폴백).

### 숫자를 아무에게나 주지 않습니다

- **공개 GET 은 순서만 돌려줍니다.** 홈이 필요한 것은 순서뿐입니다.
- **`?slug=` 는 그 글 하나의 숫자만.** 글 머리에 찍는 데 필요한 만큼입니다 — 없는 글이면 0 입니다 (함수는 글 목록을 모릅니다).
- **숫자는 `full=1` + `Authorization: Bearer <VIEWS_TOKEN>`.** `/admin` 은 정적 페이지라 서버에서 로그인 여부를 물어볼 데가 없어서, 열쇠를 이 브라우저(`localStorage`)에 남깁니다 — 비밀글 비밀번호와 같은 방식입니다 (§6-3).
- ⚠ **`full=1` 응답에 `store` 가 같이 옵니다** (저장소가 붙어 있는가). "아직 아무도 안 읽었다" 와 "셀 데가 없다" 는 둘 다 빈 표로 오는데, 뒤엣것을 앞엣것으로 적으면 **홈이 왜 「최신 글」에 머물러 있는지 알 방법이 없습니다.** `/admin` 의 조회수 판이 이 값으로 갈라 말합니다.
- ⚠ **순위 자체는 어차피 공개입니다.** 홈에 인기 글 여덟 줄이 있으니까요. 가리는 것은 **숫자**입니다.

### 세는 규칙

- **같은 기기는 하루에 한 번.** `localStorage` 의 `lim.view.<주소>` 를 봅니다. 오탈자를 고치느라 같은 글을 열 번 열어도 제 손이 순위를 만들면 안 됩니다.
- **로컬(localhost)에서는 안 셉니다.** 개발하면서 새로고침한 것이 섞입니다.
- ⚠ **정확한 숫자가 아닙니다.** 광고 차단기와 봇은 못 거릅니다. "몇 명이 읽었나"가 아니라 "대충 이 정도"입니다. **어딘가에 정확한 것처럼 적지 마세요.**
- ⚠ **주소를 정규식으로 거릅니다** (`config.yml` 의 「주소」와 같은 규칙). 아무 글자나 받으면 해시에 쓰레기 열쇠가 쌓입니다. 다만 **없는 글의 주소는 못 거릅니다** — 함수는 글 목록을 모릅니다. 그래서 홈과 `/admin` 이 `/ranks.json` 에 없는 주소를 버립니다.
- **비밀글도 셉니다.** 제목은 원래 목록에 보이고(§6-3), 인기 글에 올라가도 본문은 여전히 잠겨 있습니다.

### 왜 이렇게 했나

- **홈에 순위를 미리 못 넣습니다.** 빌드할 때 굳는 지면이라 브라우저가 갈아 끼워야 합니다.
- **그러려면 제목표가 필요합니다.** 어느 글이 올라올지 모르니까요. `/search.json`(92KB)은 본문 앞부분까지 들고 있어서 무거워, 주소·제목·날짜만 담은 `/ranks.json`(10KB)을 따로 냈습니다.
- ⚠ **`/api/views` 는 5분 캐시입니다** (`s-maxage=300`). 홈을 열 때마다 Redis 를 깨울 이유가 없고, 순위가 5분 늦게 움직이는 것은 아무 문제가 없습니다.
- ⚠ **로컬에서는 숫자가 안 나옵니다.** `/api` 가 없어서인데, Vite 는 `/api/views` 를 물으면 **함수 소스를 그대로 내줍니다** — 404 로 안 걸립니다. 그래서 주소(localhost)를 보고 먼저 끊습니다.

### `/admin` 의 대시보드 — 글·에디터·미디어 옆 탭

2026-09-08 에 목록 화면의 「131개」 옆 작은 단추 + 모달에서 **탭 + 지면**으로
옮겼습니다. 460px 짜리 판에는 순위·저자·날짜·막대가 안 들어갑니다.

```
[ 모두 · 오늘 · 읽힌 글 · 글 하나 평균 ]   숫자 칸 넷
[ 최근 14일 ]         날짜별 막대 (없으면 그렇게 적습니다)
[ 저자별 ]            합계와 몫
[ 글별 ]              순위 + 찾기 + 정렬(많이 읽힌 순 · 최신순)
[ 아직 안 읽힌 글 ]    카드 안에서만 흐릅니다 (백 편이 넘습니다)
```

- **제목을 누르면 그 글 편집 화면으로 갑니다.** 컬렉션 이름은 머리띠의 **「글」 탭**에서 뽑습니다 — 못 찾으면 링크 없이 글자로만 나옵니다. ⚠ 아무 컬렉션 링크나 잡으면 차례가 바뀌었을 때 조용히 **에디터 화면으로 가는 링크**가 됩니다 (2026-09-09).
- ⚠ **Decap 의 라우터에 길을 내지 않았습니다.** `#/views` 는 Decap 이 모르는 주소라 뒤에서 404 를 그립니다. 탭은 그냥 단추이고 지면은 우리가 화면 위에 덮습니다 — **그래서 새로고침하면 목록으로 돌아옵니다.**
- ⚠ **닫는 것은 "화면을 옮기는 클릭"만입니다** (탭 줄 · 「새 글」 · 글 제목 · Esc). 머리띠 전체를 보면 해/달을 눌러 테마만 바꿔도 지면이 닫힙니다 (재현해서 고쳤습니다).
- ⚠ **Decap 이 붙이는 눌림 표시(`header-link-active`)는 못 뗍니다** — React 가 주소를 보고 다시 붙입니다. 대시보드가 떠 있는 동안 `html[data-lim-views]` 로 흐리게 덮어서 눌린 탭이 둘로 보이지 않게 합니다.
- ⚠ **저자마다 색을 주지 않았습니다.** 블로그의 `--who-*` 는 여기 없고, 옮겨 적으면 저자를 늘릴 때 고칠 곳이 하나 더 생깁니다 (§3 은 세 군데). 막대는 전부 강조색입니다.
- ⚠ **`/ranks.json` 에 없는 주소도 보여 줍니다** ("목록에 없는 주소"). 홈은 이런 것을 버리는데(위), 지웠거나 주소를 바꾼 글이 해시에 남은 것을 알아볼 자리가 여기밖에 없습니다.
- ⚠ **폰(375px)에서는 탭 줄이 둘째 줄로 내려갑니다** (2026-09-09). 탭이 넷이 되면서 한 줄에는 못 들어갑니다 — 재 보면 390px 이고, 글자와 여백을 걷어도 402px 이라 계산이 안 맞습니다. 그래서 접었습니다(§6-2). **여기에 탭을 더 달아도 이제는 안 넘칩니다 — 대신 머리띠가 그만큼 길어집니다.**
- ⚠ **지면 위쪽(`top`)은 머리띠 높이를 재서 씁니다** (`--lim-head` · skin.js 의 `headHeight`). 폰에서 두 줄이 되면서 60px 이 아니라 81px 입니다 — CSS 에 숫자를 또 적지 마세요.

⚠ **Vercel Analytics 는 이것과 다릅니다.** 이미 붙어 있지만(`BaseLayout.astro`)
그쪽 숫자는 대시보드 안에만 있어서 화면에 꺼내 쓸 수가 없습니다. 둘은 서로
모르는 사이입니다.

---

## 6-7. 검색엔진

정적 사이트라 손댈 곳이 `<head>` 와 사이트맵뿐입니다. 붙어 있는 것들:

| 무엇 | 어디 |
|---|---|
| `robots.txt` · 사이트맵 위치 | [`public/robots.txt`](public/robots.txt) — 막는 건 `/admin/` 뿐 |
| 사이트맵 (144개) | `@astrojs/sitemap` — [`astro.config.mjs`](astro.config.mjs) |
| canonical · OG · JSON-LD · meta description | [`BaseHead.astro`](src/components/BaseHead.astro) |
| RSS | [`src/pages/rss.xml.js`](src/pages/rss.xml.js) — 네이버가 사이트맵과 따로 받습니다 |
| 소유확인 코드 | `consts.ts` 의 `SITE_VERIFICATION` |

⚠ **소유확인 태그를 지우지 마세요.** 구글·네이버·빙 셋 다 확인이 끝난 뒤에도
주기적으로 다시 봅니다 — 사라져 있으면 속성이 해제되어 색인 현황과 검색어
데이터를 못 봅니다. `vercel.app` 서브도메인이라 DNS 방식을 못 써서 이 태그가
유일한 끈입니다. **커스텀 도메인으로 옮기면** 그때 DNS 로 갈아타고 빼세요
(그때 고칠 곳은 넷입니다 — `astro.config.mjs` 의 `site`, `consts.ts` 의
`SITE_URL`, `robots.txt` 의 Sitemap 줄, `config.yml` 의 `base_url`).

⚠ **빈 값은 태그를 아예 안 답니다.** `content=""` 인 태그는 확인 실패로
잡힙니다 — 코드를 안 받았으면 빈 문자열로 두세요.

⚠ **`/search` 는 색인에 안 넣습니다.** 결과가 브라우저에서 채워지는 지면이라
검색엔진이 받아 가는 HTML 에는 빈 입력칸뿐입니다. **noindex 와 사이트맵
제외가 한 쌍**이고 둘 다 있어야 합니다 — 사이트맵에서 빼는 것은 "긁지 말라"가
아니라 "권하지 않는다"일 뿐입니다. 지면을 새로 만들 때 내용이 script 로
채워지는 것이면 `BaseLayout` 에 `noindex` 를 넘기고 filter 에도 더하세요.

⚠ **사이트맵의 `lastmod` 는 파일에 적힌 날짜만 씁니다** (`updatedDate ?? pubDate`).
빌드 시각을 넣지 마세요 — 배포할 때마다 131편이 전부 "오늘 바뀜"이 되고,
구글은 늘 거짓말하는 lastmod 를 통째로 무시합니다.
⚠ 이 날짜를 모으는 함수(`postLastmod`)가 **주소 모양(`/posts/<slug>/`)을 한 번 더
알고 있습니다** — 설정 파일은 `astro:content` 를 못 불러서(설정이 먼저 로드됩니다)
frontmatter 를 직접 읽는 것 말고 길이 없었습니다. §3 의 slug 규칙을 고치면
여기도 보세요. 못 찾은 글은 lastmod 만 빠지고 사이트맵에는 남습니다.

⚠ **비밀글에서 본문을 뽑는 코드를 새로 넣지 마세요** (§6-3). meta description ·
검색 색인 · 커버 셋 다 이미 `secret` 을 봅니다.

### 검색에 안 나올 때 보는 순서

기술적인 것은 위로 끝입니다. 그래도 안 나오면 대개 **사이트 밖**이 원인입니다.

1. **GSC → 색인 생성 > 페이지.** `중복, 사용자가 표준 페이지를 지정하지 않음` 이면 아래 2번입니다.
2. **티스토리 원본과의 중복.** 글 131편 중 116편이 `hyuk-todayfeelsogood.tistory.com` 에 같은 내용으로 있고 그쪽이 몇 년 먼저 색인됐습니다 (§5-2). 구글은 둘 중 오래되고 권위 있는 쪽을 띄웁니다 — 원본을 비공개로 돌리거나 canonical 을 이쪽으로 걸어야 합니다.
3. **국내 검색엔진 등록.** 네이버(searchadvisor.naver.com)·다음(register.search.daum.net)은 등록하지 않으면 거의 안 긁어갑니다. 사이트맵과 RSS 를 같이 냅니다.
4. **`vercel.app` 서브도메인.** 도메인에 쌓인 신뢰가 0이고 테스트·스팸 사이트가 많아 보수적으로 취급됩니다. 제일 크게 듣지만 제일 나중입니다.

⚠ **검색어를 가려서 보세요.** `lim's World` 같은 고유명으로도 안 나오면 색인이
안 된 것이고, `flutter setState` 같은 일반 검색어로 안 나오는 것은 정상입니다 —
그건 경쟁 문제라 시간이 걸립니다.

---

## 7. 명령어

```bash
npm run dev       # http://localhost:4321
npm run build     # 빌드 (frontmatter 스키마 검증 포함)
npm run preview   # 빌드 결과 확인

npm run post                     # 빌드 검증 → 커밋 → push
npm run post -- "넉백 버그 수정"    # 커밋 메시지 지정

npm run cms       # Decap 로컬 서버 (GitHub 없이 /admin 테스트)

npm run admin     # /admin 편집기 다시 묶기 (admin-src → public/admin/editor.js + editor.css)
npm run authors   # 에디터 목록을 /admin 쪽에 옮겨 적기 (admin 이 먼저 부릅니다)
node scripts/check-md-roundtrip.mjs   # 마크다운 왕복 검사

npm run icons     # limSystem 아이콘 다시 긁어오기

node scripts/import-tistory.mjs --dry    # 티스토리 이관 (한 번 쓰고 끝났습니다)
```

`npm run authors` 는 `src/data/authors.json` 을 읽어 `public/admin/config.yml` 의 「저자」 목록(`<lim:authors>` 표시 사이)과 `admin-src/authors.gen.js` 를 다시 씁니다. **`npm run admin` 이 먼저 부르니 따로 돌릴 일은 거의 없습니다** — `/admin` 에서 에디터를 고친 뒤 그 이름이 글의 저자 목록에 안 보일 때만 쓰세요 (§3).

`npm run icons` 는 `~/Develop/React/limSystem/src/assets/icons` 를 `src/assets/icons` 로 통째로 덮어쓰고 `src/lib/icon-names.ts` 를 다시 만듭니다. `src/assets/icons-extra/` 는 건드리지 않습니다. 경로가 다르면 인자로 넘기세요 — `node scripts/sync-icons.mjs <경로>`.

`draft: true` 인 글은 `dev` 에서는 보이고 `build` 에서는 빠집니다.

`npm run post` 는 **빌드가 깨지면 push 하지 않습니다.** frontmatter 오타가 배포된 뒤에 발견되는 걸 막기 위해서입니다.

---

## 8. 고친 것 기록

새 항목은 **위에** 붙입니다. 한 작업에 서너 줄이면 충분합니다 — 자세한 건 커밋에 있습니다.

### 2026-09-09 · 검색에 나오게 — 소유확인 자리·`/search` 색인 제외·사이트맵 날짜

"검색했을 때 나오게 하려면 어떻게 해야 하나" 로 시작해서 훑어봤습니다.
**기술적인 준비는 이미 다 되어 있었습니다** — robots·사이트맵·canonical·
OG·JSON-LD·meta description 전부. 그래서 코드로 할 것은 셋뿐이었고, 진짜
원인 셋은 사이트 밖에 있습니다 (§6-7 에 순서로 적어 뒀습니다).

- **소유확인 코드를 `consts.ts` 한 곳으로 모았습니다** (`SITE_VERIFICATION`). 구글 것만 `BaseHead` 에 박혀 있었고 네이버·빙은 아예 없었습니다 — 한국어 블로그인데 국내 검색엔진에 등록이 안 되어 있는 것이 제일 큰 구멍이었습니다. ⚠ **naver·bing 은 빈 문자열입니다** — 각 사이트에서 코드를 받아야 채울 수 있어서 자리만 냈습니다. 빈 값이면 태그를 아예 안 답니다 (`content=""` 는 확인 실패로 잡힙니다).
- **`/search` 를 색인에서 뺐습니다** (noindex + 사이트맵 filter). 결과가 브라우저에서 채워지는 지면이라 검색엔진이 받아 가는 HTML 에는 빈 입력칸뿐입니다 — 그대로 두면 내용 없는 지면이 검색 결과에서 글 대신 뜹니다. ⚠ 둘이 한 쌍입니다 (§6-7).
- **사이트맵에 `lastmod` 를 붙였습니다** (131편 전부). 없으면 검색엔진이 언제 다시 긁을지 판단할 근거가 없습니다. ⚠ **빌드 시각이 아니라 파일에 적힌 날짜**(`updatedDate ?? pubDate`)입니다 — 배포마다 전부 "오늘 바뀜"이 되면 구글이 그 값을 통째로 무시합니다.
- ⚠ **설정 파일이 주소 모양을 한 번 더 압니다.** `astro.config.mjs` 는 `astro:content` 를 못 불러서(설정이 먼저 로드됩니다) frontmatter 를 직접 읽습니다. 대신 못 찾은 글은 lastmod 만 빠지고 빌드는 안 깨집니다.
- **안 한 것 둘.** ① **커스텀 도메인** — 나중에 보기로 하셨습니다. 옮길 때 고칠 네 곳을 §6-7 에 적어 뒀습니다. ② **티스토리 원본 정리** — 직접 하시기로 했습니다. 이게 지금 제일 크게 막고 있는 것입니다 (116편 중복).
- 빌드해서 확인했습니다 — 사이트맵 145 → 144(`/search` 빠짐), `lastmod` 131개가 실제 pubDate 와 맞는 것, `/search` 에만 `noindex, follow` 가 붙고 홈·글에는 robots 태그가 없는 것, 구글 태그가 그대로 나가고 빈 naver·bing 태그는 안 나가는 것. 빌드 144쪽 통과.

### 2026-09-09 · `/admin` 을 시안 넉 장대로 다시 앉혔습니다

보내주신 시안(글 목록 · 에디터 목록 · 에디터 고치기 · 조회수)을 옮겼습니다.
앞 세션이 「에디터」 두 화면을 그리는 `admin-src/editors.js` 까지 짜 놓고
끊겨 있었습니다 — **불러 주는 곳도 CSS 도 없어서 화면에는 아무것도 안
나오고 있었습니다.** 그것부터 잇고, 시안대로 껍데기를 바꿨습니다. 규약은
§3 · §6-2 · §6-6 에 나눠 적었습니다.

- **왼쪽 사이드바를 걷어냈습니다.** 컬렉션이 둘뿐인데 화면 왼쪽 300px 을 통째로 먹고 있었습니다 — 시안에는 없고, 대신 「글」·「에디터」가 **머리띠 탭**으로 올라갑니다. ⚠ Decap 의 「콘텐츠」 링크는 감추기만 합니다(React 것입니다). 그 링크의 눌림 표시는 컬렉션이 어느 쪽이든 켜져 있어서 못 씁니다 — 우리가 답니다.
- ⚠ **찾기 칸은 사이드바 안에 있습니다.** 사이드바를 감추면 검색이 같이 사라지는데, 글이 131편이라 검색 없이는 고칠 글을 못 찾습니다. 그래서 **노드를 제목 줄로 옮깁니다**(`moveSearch`) — 폼 필드를 「발행 설정」 모달로 옮기는 것과 같은 방법이고, 목록↔에디터↔글을 오가며 React 오류가 없는 것을 봤습니다.
- ⚠ **찾기 칸 폭은 340px 고정입니다.** 처음에는 오른쪽 칸을 `1fr` 로 두고 이 칸이 줄어들게 했는데, 900px 짜리 창에서 **돋보기만 남을 때까지 쪼그라들었습니다.** 줄어드는 것은 제목 쪽입니다 — 제목은 「글」 두 글자고, 좁은 화면일수록 찾을 일이 많습니다. 그러려면 Decap 이 제목 덩어리에 박아 둔 **반쪽 폭**(1360px 에서 682px)도 같이 풀어야 합니다. 폰에서는 한 줄을 통째로 씁니다.
- **표를 판 하나로 둘렀습니다** (머리 줄 + 줄들). 감싸개가 클래스 없는 div 라 `skin.js` 가 이름을 붙입니다(`.lim-table`). ⚠ 올린 줄을 **떠오르게** 만들었다가 걷어냈습니다(radius + 그림자) — 줄이 판 좌우 끝까지 닿아 있어서 둥근 모서리가 판 테두리와 어긋납니다. 바탕색만 바뀝니다.
- ⚠ **폰에서 머리띠가 두 줄이 됐습니다.** 탭이 넷이 되면서 375px 에서 390px 로 넘칩니다(재서 확인). 글자를 줄이고 여백을 걷어도 402px 이라 계산이 안 맞아서 접었습니다 — 옆으로 흘려 감추는 쪽은 넷 중 둘이 화면 밖이면 있는 줄도 모릅니다. 그래서 조회수 지면이 기대던 `top: 60px` 이 틀어졌고, **머리띠를 재서 CSS 에 넘깁니다**(`--lim-head`).
- **에디터 목록은 사람마다 카드**(얼굴·이름·소개·글 수·편집·삭제), **고치는 화면은 줄**입니다. ⚠ 값을 카드에서 고치지 않고 전부 고치는 화면의 진짜 단추를 누릅니다. 글이 딸린 사람은 지우지 못하게 막았습니다 — 그대로 지우면 빌드가 멈추는데, 폰에서는 배포하고 나서야 압니다.
- ⚠ **잡은 것 다섯.** ① 검색결과에는 `h2` 가 **둘**이라(앞엣것이 컬렉션 이름) 줄마다 제목 자리에 「글」만 적히고 있었습니다 — 이번에 옮긴 것이 아니라 원래 그랬습니다. ② 「위젯 상자」 규칙이 `:not()` 여섯 개라 특이도 (0,7,0) 이어서, 에디터 폼의 바깥 판을 벗기는 규칙이 조용히 지고 있었습니다. ③ `.lim-ehead span` 이 아바타(도 span)까지 잡아 머리 줄이 가운데로 몰렸습니다. ④ 두 칸으로 세우는 이름이 **펼치는 순간** 사라졌습니다 — Decap 이 접힐 때와 펼칠 때 감싸는 div 를 갈아 끼웁니다. ⑤ Decap 이 제목 덩어리 폭을 반쪽(1360 → 682px)으로 박아 둬서 「에디터 추가」가 화면 가운데에 섰습니다.
- ⚠ **조회수 지면은 숫자가 든 채로 못 봤습니다** — 로컬에는 `/api` 가 없습니다. 이 작업에서 그쪽에 손댄 것은 제목 크기·여백·`top` 셋뿐이고, 빈 화면에서 자리(머리띠 아래 81px 에 딱 붙는 것)를 재서 확인했습니다.
- 로컬 Decap 으로 라이트·다크 양쪽에서 봤습니다 — 네 화면, 목록↔에디터↔조회수↔글 오가기, 찾기(제안 목록·검색결과 줄), 카드의 「편집」이 그 사람 줄을 펼치는 것, 「새 글」, 글 편집 화면이 그대로인 것. 375px 에서 가로 넘침이 없는 것(`scrollWidth == clientWidth`)과 아바타가 화면 안(365px)인 것도 재서 봤습니다. 왕복 검사 그대로(131편 중 119 / 76), 빌드 144쪽 통과.

### 2026-09-09 · 에디터 이름을 바꾸고, 에디터를 `/admin` 에서 늘립니다

"에디터 이름들을 바꾸고 에디터를 추가하려 한다, 어드민에서 수정할 수 있게
해 달라"고 하셔서 둘을 같이 했습니다. **study Lim → 학생 · 임데브 → 개발자**
(화면 이름만 — `id` 는 그대로라 주소도 글 131편의 frontmatter 도 안 움직입니다).

그리고 **저자를 늘리는 일을 폰에서 할 수 있게** 옮겼습니다. 그전에는
`consts.ts`·`global.css`·`config.yml` 세 군데를 손으로 고쳐야 했습니다 — 글은
폰에서 쓰는데 쓰는 사람을 늘리는 것만 노트북을 열어야 하는 것이 이상했습니다.
규약은 §3 · §4 · §6-2 에 적었습니다.

- **값을 `src/data/authors.json` 으로 내렸습니다.** `consts.ts` 는 그 파일을 읽어 화면이 쓰는 모양으로 바꾸기만 합니다. ⚠ **모르는 색·빈 칸에 빌드를 깨지 않습니다** — 폰에서 저장한 것 때문에 사이트가 통째로 안 나가는 쪽이 더 나쁩니다 (§3 의 blankAsUndefined 와 같은 판단). **이름 오타는 그대로 걸립니다.**
- ⚠ **색을 저자 이름에서 팔레트로 바꿨습니다** (`--who-student` → `--who-blue`). 저자마다 CSS 두 줄을 손으로 넣는 방식이면 `/admin` 에서 늘려도 색이 없어서 이름이 안 보입니다. 다섯 색(파랑·빨강·초록·주황·청록)을 미리 깔고 대비를 계산했습니다 — 라이트는 카드(#fff)와 지면(#fafafa) 양쪽에서 4.5:1 을 넘깁니다.
- ⚠ **`config.yml` 의 「저자」 목록은 스크립트가 채웁니다** (`npm run authors` · `<lim:authors>` 표시 사이). Decap 의 select 는 정적이라 다른 파일을 못 읽습니다. `relation` 위젯이면 되는데 **그러면 저자 알약이 통째로 안 그려집니다** (skin.js 가 select 위젯을 갈아끼운 것이라). 목록 화면의 「작성자」 칸도 같은 이유로 `authors.gen.js` 를 만들어 씁니다 — 거기는 위젯이 아예 없는 자리입니다.
- ⚠ **그래서 폰에서 더한 에디터는 다음 배포부터 글에 붙일 수 있습니다.** `/authors/<id>` 지면은 그 배포에서 바로 나갑니다. Vercel 이 `npm run build` 를 부르고, 그 앞에 `npm run admin` → `npm run authors` 가 붙어 있습니다.
- ⚠ **잡은 버그 하나: 글 목록의 원래 요약 글자가 제목 위에 다시 보였습니다.** 에디터 줄(네 칸으로 안 가르는 줄)을 위해 `[class*='ListCard']:not(.lim-row)` 규칙을 더했는데, 그 이름이 줄뿐 아니라 **안쪽 링크(`ListCardLink`)까지** 걸려서 `:not()` 이 참이 됐습니다. `li` 를 앞에 붙여 고쳤습니다.
- ⚠ **폰에서 컬렉션 목록을 다시 켰습니다.** 「글」 하나뿐일 때 숨겼던 것인데, 이제 「에디터」로 갈 길이 거기밖에 없습니다 — 머리띠에 탭을 하나 더 달면 아바타가 화면 밖으로 나갑니다 (§6-6). 세로로 쌓지 않고 알약 둘로 눕혔습니다 (375px 에서 12~150px).
- **`말투`는 목록이 아니라 여러 줄 칸입니다.** 목록으로 뒀더니 접힌 줄에 「한 줄」이라는 필드 이름만 보여서, 무엇이 적혀 있는지 하나씩 펼쳐 봐야 했습니다 (재현해서 바꿨습니다).
- 로컬 Decap 으로 **실제로 에디터를 하나 더해 저장까지 해 봤습니다** — 「에디터 추가」 → 이름·아이디·소개·색(초록)·아바타 글자 → 저장 → `authors.json` 에 써지고, `npm run authors` 가 목록 둘을 갱신하고, 빌드가 145쪽(+1)이 되면서 `/authors/tester` 가 초록 이름으로 나오는 것까지. 그리고 **지웠습니다.** 글 폼(알약·「발행 설정」 모달), 목록 네 칸, 폰 375px 가로 넘침 없음(`scrollWidth == clientWidth`), 라이트·다크도 봤습니다. 왕복 검사 그대로(131편 중 119 / 76), 빌드 144쪽 통과.
- ⚠ **`npm run admin` 을 `| head` 로 자르지 마세요.** 파이프가 먼저 닫히면서 sync 가 죽어 목록이 옛것으로 남습니다 (그것 때문에 한 번 속았습니다).

### 2026-09-08 · 조회수를 대시보드로 옮기고 날짜별을 세기 시작했습니다

「조회수」를 목록 화면의 작은 단추 + 모달에서 **콘텐츠·미디어 옆 탭**으로
옮겼습니다. 주소와 숫자를 한 줄씩 늘어놓기만 하던 판이라, 어느 저자가
읽히는지도 요즘 얼마나 오는지도 알 수 없었습니다. 규약은 §6-6 에 적었습니다.

- **날짜별 합계를 세기 시작했습니다** (`views:day` 해시 · 사이트 전체 하나). 세기 시작한 것이 오늘이라 지금 넣는 것이 제일 싼 때였습니다 — 하루라도 지나면 그만큼이 날짜 없는 숫자로 남습니다. ⚠ **그전 조회수는 날짜가 없습니다** — 막대의 합과 「모두」가 다른 것이 정상이고, 아직 하루치도 없으면 막대 대신 그렇게 적습니다. 글 × 날짜로는 안 셉니다 (열쇠가 글 수 × 날짜 수로 불어납니다).
- **글을 열 때 올리는 것이 둘이 되면서 `/pipeline` 을 씁니다.** 따로 부르면 Vercel 함수가 Upstash 를 두 번 기다립니다.
- ⚠ **Decap 라우터에 `#/views` 를 내지 않았습니다.** 모르는 주소라 뒤에서 404 를 그립니다 — 탭은 단추이고 지면은 우리가 덮습니다. 대신 **새로고침하면 목록으로 돌아옵니다.** 알고 고른 것입니다.
- ⚠ **잡은 버그 하나: 테마를 바꾸면 지면이 닫혔습니다.** 머리띠 안의 클릭을 전부 "다른 데로 간다"로 봤는데, 해/달과 「쓰다 만 글」이 거기 있습니다. 탭 줄과 「새 글」만 봅니다.
- ⚠ **폰에서 머리띠가 15px 넘쳤습니다** — 탭이 하나 늘어 아바타가 화면 밖으로 나갔습니다(재서 확인). 탭 여백·탭 사이·이름 크기에서 걷어 와 375px 에서 373px 로 들어옵니다. **여기에 단추를 더 달면 다시 넘칩니다.**
- **「아직 안 읽힌 글」과 「목록에 없는 주소」를 같이 보여 줍니다.** 앞엣것은 `/ranks.json` 에만 있고, 뒤엣것은 해시에만 있는 주소입니다 — 지웠거나 주소를 바꾼 글이 남아 있는지 알아볼 자리가 여기밖에 없습니다.
- 로컬 Decap 으로 확인했습니다 — 가짜 응답을 물려 숫자 칸·막대·저자별·순위·찾기·정렬(최신순)·제목을 눌러 글로 건너가는 것, Esc·「콘텐츠」로 닫히는 것(주소가 같을 때도), 열쇠 없음·틀린 열쇠·셀 데 없음·아무도 안 읽음·날짜별 없음 다섯 상태, 라이트와 다크, 375px 에서 가로 넘침이 없는 것(`scrollWidth == clientWidth`). 함수는 가짜 Upstash 를 물려 POST 가 `HINCRBY` 둘을 한 번에 보내는 것과 `full=1` 이 `days`·`store` 를 같이 주는 것, 틀린 열쇠가 403 인 것을 봤습니다. 왕복 검사 그대로(131편 중 119 / 76), 빌드 144쪽 통과.
- ⚠ **실제 Upstash 로는 못 봤습니다** — 로컬에는 `/api` 가 없습니다. 숫자는 이미 세지고 있으니(아래 항목) 배포하면 바로 나오는데, **「최근 14일」 막대는 하루가 지나야** 두 칸이 됩니다.
- ⚠ **이 작업의 코드가 다른 세션의 커밋(`168ca31`)에 딸려 들어갔습니다.** 그쪽이 문서를 커밋하면서 제 작업 파일까지 같이 담았습니다 — 커밋 메시지가 내용과 다릅니다. **같은 저장소에서 두 세션을 함께 돌리지 마세요** (2026-09-04 에도 같은 일이 있었습니다).

### 2026-09-08 · 홈이 「인기 글」로 안 바뀌던 것 — 셀 데가 안 붙어 있었습니다

"메인에 최신 글이 아니라 인기 글이어야 한다" 고 하셔서 배포본을 봤더니
`POST /api/views` 가 **501** 이었습니다 — `Upstash 환경변수가 없습니다`.
아무것도 안 세지니 `GET /api/views` 가 빈 목록을 주고, 홈은 규칙대로
「최신 글」에 머물러 있었습니다 (§4). **코드가 아니라 환경변수 문제입니다.**

- **홈 script 는 멀쩡합니다.** 로컬에서 `/api/views` 응답을 가짜로 물려 보고 확인했습니다 — 제목이 「인기 글」로 바뀌고, 카드 넷이 준 차례대로 갈리고, 커버·저자 이름까지 따라 바뀌고, 「더 읽을 글」이 그만큼 밀려 다시 채워집니다.
- ⚠ **`/admin` 이 "아직 아무도 안 읽었습니다" 라고 거짓말을 하고 있었습니다.** 저장소가 없을 때도 빈 표가 와서 같은 문구가 떴습니다. `full=1` 응답에 `store` 를 넣어 갈라 말하게 했습니다 (§6-6) — 다음에 같은 일이 나면 거기서 바로 보입니다.
- **같은 날 Upstash 를 붙이고 재배포해서 살아났습니다.** ⚠ **통합을 설치한 것만으로는 안 됩니다** — 환경변수는 그 값이 있는 상태로 빌드된 배포에만 들어갑니다. 설치 직후 재 봤을 때 여전히 501 이었고, **재배포하고 나서** 200 이 됐습니다. 다음에 변수를 건드리면 이것부터 하세요.
- 살아난 뒤 확인했습니다 — 글을 열면 머리에 숫자가 뜨고(POST → `{"views":1}`), `GET /api/views` 가 인기순 주소를 주고, 홈 제목이 「인기 글」로 바뀌면서 카드가 그 차례로 갈립니다. **헤드라인에 이미 있는 글은 빠지고 모자란 자리는 최신순으로 채워지는 것**까지 실제 데이터로 봤습니다.

### 2026-09-08 · 시안 열두 장으로 지면을 전부 갈아 앉혔습니다

보내주신 시안(라이트·다크 여섯 화면씩 · 홈 · 메뉴 · 글 · 저자 · 서비스 · 서고)을
옮겼습니다. **색부터 지면 구조까지 한 번에 바뀌어서** 자세한 규약은 §4 · §6-2 ·
§6-6 에 나눠 적었습니다. 여기에는 갈라진 판단만 남깁니다.

- **색을 limSystem 밖에서 가져왔습니다.** 시안의 다크 배경(#111214)이 limSystem 의 제일 어두운 회색보다 어둡고, 파랑도 BLUE 계열 어느 단계와도 안 맞습니다. `tokens.css` 에 `--ui-*` 로 따로 두고 **시안에서 픽셀을 뽑아 적었습니다** — ⚠ `npm run icons` 처럼 원본에서 다시 긁어올 수 있는 값이 아닙니다. **글자색은 limSystem 그대로** 뒀습니다 (대비를 계산해 둔 값입니다).
- ⚠ **강조색이 청록에서 파랑이 되면서 셋이 한 값으로 묶였습니다** — 강조 파랑 = study Lim, LIVE 빨강 = 임데브. 시안이 그렇게 그려져 있습니다. **LIVE 배지만 강조색에서 떼어 빨강으로** 옮겼습니다 — 링크·번호·주 버튼과 같은 파랑을 배지에까지 쓰면 "지금 되는 것" 이라는 표시가 안 읽힙니다.
- **서고에서 거르기를 걷어내고 쪽 나누기를 넣었습니다.** 시안에 쪽 단추가 생기면서 "거르기 × 쪽" 두 상태를 한 화면에서 들고 있어야 했는데, 저자별 목록은 이미 `/authors/<id>` 가 하고 있습니다. 알약은 이제 **거르는 것이 아니라 넘어가는 것**이고, 옛 `?author=` 주소는 그 자리로 넘겨 줍니다. 서고와 저자 지면이 같은 알약 줄·같은 줄 모양·같은 쪽 나누기를 씁니다.
- ⚠ **쪽마다 주소를 내지 않았습니다** (`/posts/page/2`). 줄을 다 그려 두고 브라우저가 스무 개씩 보여 줍니다 — 131편의 링크가 한 문서에 다 있어서 검색엔진이 한 번에 보고, 자바스크립트가 죽어도 전부 보이는 채로 남습니다.
- ⚠ **잡은 버그 하나: `.row` 이름이 겹쳤습니다.** 알약 줄의 감싸개도 `.row` 라, 쪽 나누기가 그걸 줄로 세어서 **두 번째 쪽부터 에디터 알약이 통째로 사라졌습니다.** 알약 쪽을 `.list` 로 바꾸고 찾는 쪽도 `.rows .row` 로 좁혔습니다.
- **`FeedLayout`(3열)과 `PostCard` 를 지웠습니다.** 마지막으로 쓰던 `/authors/<id>` 가 한 열이 되면서 아무도 안 부르게 됐습니다. 아래 띠는 이제 `BaseLayout` 이 **모든 지면**에 답니다 — ⚠ 지면에서 또 부르면 두 번 나옵니다.
- **글 머리에 조회수가 찍힙니다.** 시안이 그렇게 그려져 있어서 **한 편의 숫자만 공개로** 열었습니다 (§6-6). 전체 표는 여전히 열쇠가 있어야 합니다.
- **시안에 없는 지면도 같이 맞췄습니다** — `/projects/<id>/about` 의 버튼 색, 비밀글 단추의 글자색(`--pill-fg` 는 파랑 판 위에서 3.5:1), `/authors` 목록 제목 색, 그리고 **`/admin`**(판 네 겹 + 강조색). 안 맞추면 그 화면만 다른 사이트처럼 보입니다.
- ⚠ **인용문에 배경이 다시 생겼습니다.** 2026-09-07 에 "배경을 다시 깔지 마세요" 라고 적어 둔 것을 뒤집은 것입니다 — 글 본문이 카드에서 내려와 지면 위에 바로 놓이면서 `--card` 가 오히려 제일 옅은 층이 됐습니다. 편집기도 같이 고쳤는데 **판 색만 다릅니다** (편집기 바닥이 이미 `--a-card` 입니다).
- 로컬에서 여섯 화면을 라이트로, 다섯을 다크로 봤습니다 (글 지면의 다크는 화면이 안 잡혀서 색 값을 재서 확인했습니다 — 배경 #111214 · 링크 #6ea8ff · 인용문 판 #17181b). 375px 에서 가로 넘침이 없는 것(`scrollWidth == clientWidth`)과 눌린 알약이 화면 안으로 들어오는 것도 재서 봤습니다. 프로젝트 머리 사진이 1216×405(3:1)로 앉는 것, 쪽 단추가 `‹ 1 2 3 4 5 … 7 ›` 로 나오는 것도 봤습니다. 왕복 검사 그대로(131편 중 119 / 76), 빌드 144쪽 통과.
- ⚠ **작업 도중 앞 세션이 띄워 둔 `astro dev` 가 남아 있었습니다.** 그 서버가 옛 CSS 를 물고 있어서 고친 것이 화면에 안 나왔습니다 — 한참 헤맸습니다. Astro 는 한 저장소에 dev 서버를 둘 못 띄웁니다(`astro dev stop` 으로 정리). **화면이 코드와 다르면 서버부터 의심하세요.**

### 2026-09-08 · 조회수를 세고, 홈의 마지막 덩어리를 인기 글로 바꿨습니다

"조회수 기능을 추가할 수 있나요" 로 시작해서, **숫자는 저만 보고 홈은 그것으로
인기 글을 세우는** 쪽으로 정했습니다. 규약은 §6-6 에 있습니다.

- **Upstash Redis 해시 하나입니다.** SDK 를 안 넣었습니다 — REST 로 명령 배열 하나를 보내면 끝이라 `fetch` 로 충분합니다. 함수는 `api/views.js` 하나이고 `api/` 에는 이미 OAuth 둘이 돌고 있어서 자리를 새로 만들 것이 없었습니다.
- ⚠ **숫자를 공개 API 로 안 흘립니다.** 공개 GET 은 **순서만**, POST 는 아무것도 안 돌려줍니다. 숫자는 `full=1` + 열쇠(`VIEWS_TOKEN`)이고 보는 곳은 `/admin` 의 「조회수」 판 하나입니다. 순위 자체는 홈에 여덟 줄이 있으니 어차피 공개고, 가리는 것은 숫자입니다.
- ⚠ **「더 읽을 글」은 못 받아오면 그대로 남습니다.** 최신순 여덟 줄에 「인기 글」이라고 써 붙이는 순간 세지도 않은 것을 센 척하게 됩니다 — 번호를 순위로 안 쓰던 이유(§4)를 그대로 지킨 것입니다. 제목까지 script 가 갈아 끼웁니다.
- **제목표를 따로 냈습니다** (`/ranks.json` · 10KB). 어느 글이 올라올지 빌드할 때 모르는데, `/search.json` 은 본문 앞부분까지 있어 92KB 입니다. `/admin` 의 판도 이 파일로 제목을 찾습니다 — 함수는 주소만 알고 있습니다.
- ⚠ **판을 머리띠에 안 달았습니다.** 폰에서 머리띠는 이미 꽉 차 있습니다 (§6-2 의 「쓰다 만 글」과 같은 자리 문제). 목록 화면의 「131개」 옆에 달았고, 375px 에서 단추(310~363)와 판(20~355)이 화면 안에 들어오는 것을 재서 확인했습니다.
- ⚠ **알아낸 것: 로컬에서는 `/api/views` 가 404 가 아닙니다.** Vite 가 **함수 소스를 그대로** 내줍니다(200 · text/javascript). 그래서 "응답을 읽지 못했습니다" 가 떠서 열쇠가 틀린 것과 구분이 안 됐습니다 — 주소(localhost)를 보고 먼저 끊습니다.
- **같은 기기는 하루에 한 번만 세고 로컬에서는 안 셉니다.** 그래도 정확한 숫자는 아닙니다 (광고 차단기·봇). 어디에도 정확한 것처럼 적지 마세요.
- 홈은 미리보기로 확인했습니다 — 순서를 넣어 주면 「인기 글」로 바뀌고 제목·날짜·주소가 다 갈리는 것, 위에 이미 나온 여덟 편과 `/ranks.json` 에 없는 주소(`blog-rebuild-notes` — `slug` 는 `blog-modify` 입니다)가 빠지는 것, 안 주면 「더 읽을 글」 최신순 그대로인 것. 글 페이지는 localhost 라 POST 가 안 나가는 것까지 봤습니다. `/admin` 은 열쇠 묻기·틀린 열쇠·목록·Esc 를 눌러 봤습니다. 왕복 검사 그대로(131편 중 119 / 76), 빌드 144쪽 통과.
- ⚠ **환경변수를 넣기 전에는 아무것도 안 세집니다.** Vercel 에 `KV_REST_API_URL`·`KV_REST_API_TOKEN`(또는 `UPSTASH_REDIS_REST_*`)과 `VIEWS_TOKEN` 을 넣고 한 번 다시 배포해야 합니다. 그동안 홈은 「더 읽을 글」로 남습니다.

### 2026-09-08 · 홈 지면을 함부로 바꿨다가 되돌렸습니다

"최신글이라고 되어 있는데 과거 글이 맨 처음 나온다" 는 이야기에, 「최신 글」
제목을 헤드라인 위로 올려 한 덩어리로 묶었습니다. **지면을 바꾸지 말라고
하셨는데 바꾼 것입니다.** 그 자리에서 되돌렸습니다 (`277e22b` → revert).

- ⚠ **제목을 옮기는 것도 지면을 바꾸는 것입니다.** "배치는 그대로고 제목만 옮겼다" 고 적었는데, 화면에서 제일 먼저 보이는 줄이 달라졌으니 바뀐 것이 맞습니다. **홈은 손대지 마세요.**
- ⚠ **남은 문제는 그대로입니다** — 「최신 글」 카드 넷은 여전히 **다섯째 글부터**입니다 (헤드라인 1 + 옆줄 3 을 먼저 집어가기 때문). 그래서 새 글을 올려도 그 제목 아래에는 몇 주 지난 글이 놓입니다. **어떻게 고칠지는 정해진 것이 없습니다** — 고치려면 지면이 달라지는 일이라 먼저 물어보세요.
- 헤드라인·옆줄·카드·번호 줄 모두 되돌리기 전과 파일이 같은 것을 확인했습니다 (`git diff` 0줄). 빌드 144쪽 통과.

### 2026-09-08 · 글 쓰는 화면에도 해/달을 달았습니다

글을 쓰다가 테마를 바꾸려면 **목록으로 나갔다 들어와야** 했습니다. 글을 열면
머리띠(AppHeader) 자리를 위 띠(Toolbar)가 덮는데, 해/달은 머리띠에만 달려
있었습니다 — 정작 눈이 부신 자리는 글 쓰는 화면입니다.

- **머리띠 것과 같은 함수를 씁니다** (`skin.js` 의 `themeButton`). 따로 만들면 한쪽만 칠해지거나 한쪽만 안 눌리는 일이 생깁니다. `paintThemeButton` 도 화면에 있는 것을 **전부** 칠하게 고쳤습니다 (전에는 첫 번째 하나만).
- ⚠ **자리는 `order: -2` 로 못박습니다.** 상태 알약도 그 칸 맨 앞에 끼어드는데, 새 글은 초안 토글이 늦게 떠서 알약이 나중에 붙습니다 — DOM 차례에 기대면 해/달이 왼쪽에 있다 없다 합니다.
- **폰에서 자리를 재서 확인했습니다** — 375px 에서 오른쪽 끝이 362px 로 화면 안입니다 (상태 알약은 원래 폰에서 빠집니다). 해/달은 폰에서 30px 짜리로 이미 줄어 있었습니다.
- 로컬 Decap 으로 확인했습니다 — 글 화면에서 눌러 다크↔라이트가 바뀌고 두 단추의 그림이 같이 바뀌는 것, `localStorage.theme` 에 남는 것, 목록 화면에는 여전히 하나만 있는 것. 빌드 143쪽 통과.
- ⚠ **알아 둘 것: 글 화면에서도 머리띠는 DOM 에 남아 있습니다** (위 띠가 덮고 있을 뿐, 재 보고 확인). 그래서 그때는 `.lim-theme` 이 둘입니다 — 화면에 보이는 것은 하나입니다. `.lim-theme` 을 하나로 치고 코드를 쓰지 마세요.
- 곁다리로 §6-2 의 "OS 다크모드만 따라갑니다" 를 지웠습니다 — 2026-08-25 에 토글을 단 뒤로 틀린 말이었습니다.

### 2026-09-08 · 인용문을 `| ` 로 시작할 수 있습니다

인용문을 `| ` 로도 시작하게 해 달라고 하셔서 넣었습니다 (`editor.js` 의
`QuoteBar`). 줄 맨 앞에서 `|` 를 치고 스페이스를 누르면 인용문이 됩니다 —
화면의 인용문이 왼쪽 세로선 하나(§4)라 치는 글자와 모양이 맞습니다.

- **기본 `>` 는 안 뺐습니다.** StarterKit 의 Blockquote 가 매어 둔 것이라 빼려면 그 확장을 통째로 갈아끼워야 하고, 마크다운을 아는 사람은 그쪽을 먼저 칩니다.
- ⚠ **저장되는 것은 여전히 `> ` 입니다.** 치는 방법만 늘었고 파일에 `|` 가 남지 않습니다 — 왕복 검사 그대로(130편 중 119 / 76), 빌드 143쪽 통과.
- ⚠ **알아낸 것: 표 칸 안에서도 먹습니다.** 칸 맨 앞에서 `| ` 를 치면 그 칸에 인용문이 생깁니다 (재현해서 확인). 기본 `>` 도 원래 그런데, 표에서 `|` 를 치는 일이 `>` 보다는 있을 법합니다 — 되돌리기(⌘Z) 한 번으로 `|` 가 돌아오는 것까지 봤습니다.
- 로컬 Decap 으로 확인했습니다 — `| `→인용, `> `도 그대로, 줄 중간의 `가나| ` 는 안 걸리는 것, 원문 모드로 넘겨 저장될 마크다운이 `> 인용문 시험` 인 것, 표 칸 안, ⌘Z 로 `|` 가 돌아오는 것.
- ⚠ **재는 방법 하나.** 브라우저에 글자를 한꺼번에 밀어 넣으면(`"| 시험"`) 입력 규칙이 **안 걸립니다** — 규칙은 마지막 한 글자를 보고 판단합니다. 한 글자씩 넣어야 합니다. 기본 `>` 도 똑같이 안 걸리니, 이걸로 "안 된다" 고 판단하지 마세요.

### 2026-09-07 · 쓰다 만 글 목록 — 머리띠에 단추를 달았습니다

임시저장을 넣고 나니 **되찾을 길이 반쪽이었습니다.** 되살리기 줄은 그 글을
열어야 뜨는데, 옛 글에 남긴 것은 그 글을 다시 열기 전에는 있는지조차 알 수가
없었습니다. 머리띠에 목록을 달았습니다 — 남은 것이 없으면 단추가 숨습니다.

- **고른 글로 건너가기만 하고, 되살릴지는 원래 줄이 묻습니다.** 되살리기는 폼을 덮어쓰는 일이라 확인하는 자리를 둘로 늘리지 않았습니다.
- ⚠ **알아낸 것: 글 → 글은 주소만 바꿔서는 안 넘어갑니다.** Decap 이 주소는 바꿔 놓고 폼은 옛 글 그대로 둡니다 — **고칠 것이 없는 깨끗한 글에서도** 그렇습니다(재현해서 확인). 그러면 엉뚱한 글 위에 "쓰다 만 글이 있습니다" 가 떠서 되살리면 **남의 글을 덮어씁니다.** 목록을 한 번 거치면(60ms) 넘어갑니다. 그래도 안 바뀌면 「주소」 칸을 보고 통째로 다시 엽니다 — 조용히 틀리는 쪽이 제일 나쁩니다.
- ⚠ **폰에서 머리띠가 44px 밀렸습니다.** 「게시」와 아바타가 화면 밖으로 나가서, 글자와 숫자를 다른 span 에 담고 375px 에서는 숫자만 남깁니다 (재서 확인 — 이제 371px 로 들어옵니다).
- 목록·개수·판 열고 닫기(닫기·Esc·바깥 누르기), 줄마다 「버리기」, 지운 뒤 다시 안 살아나는 것, 새 글 ↔ 옛 글 건너뛰기, 건너간 뒤 되살리기까지 확인했습니다. 왕복 검사 그대로(130편 중 119 / 76), 빌드 143쪽 통과.
- ⚠ **한 가지 알아 두세요.** 고치던 글을 저장 안 한 채로 다른 글로 건너뛰면 Decap 이 들고 있던 값이 잠깐 섞여 보입니다(제목만 옛 것으로 남는 식). 되살리면 여섯 칸이 다 덮여서 바로잡힙니다. **Decap 이 원래 그런 것이고** 이 단추와 상관없습니다.

### 2026-09-07 · 커버 사진을 「이미지 선택」으로 넣으면 터지던 것

"업로드 누르고 사진 클릭하고 insert 하면 에러가 난다" 고 하셔서 봤습니다.
**앞의 CORS 이야기와 다른, 진짜 고장이었습니다.**

Decap 3.9 의 `insertHandler` 가 `asset.derived[0]` 을 읽는데, `derived` 가
**빈 배열**이면 `[]` 가 참이라 `undefined` 를 읽고 터집니다 — `derived` 가
아예 **없을** 때는 원본으로 잘 떨어지게 해 놨으면서 빈 배열은 안 걸렀습니다.
**방금 올린 사진**은 변환본이 아직 없어서 여기 걸리고, 그래서 이미 있던
사진을 고르면 되고 새로 올린 것만 안 됐습니다.

- **재현을 실제로 했습니다.** 로그인 없이는 창 안을 못 건드려서, `index.html` 에 잠깐 probe 를 넣어 **Decap 이 Cloudinary 에 건네는 진짜 `insertHandler` 를 잡아채서** 세 가지 모양으로 불러 봤습니다 — `derived` 있음/없음은 통과, **빈 배열만 `TypeError: Cannot read properties of undefined (reading 'secure_url')`**. probe 는 지웠습니다.
- **고친 자리는 `admin-src/upload.js` 의 `guardMediaLibrary()`** 입니다. Decap 안은 못 고칩니다(handler 가 모듈 안에 닫혀 있습니다) — 위젯을 만들 때 handler 를 감싸서 자산을 먼저 손봅니다. 자세한 건 §6-5.
- ⚠ **빈 배열일 때 그냥 원본으로 떨어뜨리지 않았습니다.** 터지지는 않지만 변환이 안 붙어서 폰 원본 3~5MB 가 본문에 박힙니다 (§6-5 가 막으려던 바로 그것). `default_transformations` 를 우리가 주소에 붙여 채웁니다. **`derived` 가 없을 때도** 같은 이유로 채웁니다.
- **덤으로 주소 모양이 하나로 모였습니다** — 「사진」 단추·끌어다 놓기·커버 사진이 전부 `…/c_limit,f_auto,q_auto,w_1600/…` 입니다.
- ⚠ **`window.cloudinary` 는 getter 로 감싸야 합니다.** 스크립트가 `{}` 를 먼저 놓고 `createMediaLibrary` 를 나중에 얹어서, set 만 지켜보면 놓칩니다 — 처음에 그렇게 짰다가 못 잡았습니다.
- 번들에 들어간 진짜 guard 에 Decap 의 표현식을 그대로 물려서 세 모양을 다시 돌렸습니다 — 셋 다 같은 변환 주소가 나오고 터지지 않습니다. 사진 창이 그대로 열리는 것, 콘솔에 경고가 없는 것도 봤습니다. 왕복 검사 그대로(129편 중 119 / 76), 빌드 142쪽 통과.
- ⚠ **실제 Cloudinary 업로드로는 아직 못 봤습니다** — 로그인이 필요해서입니다. 위 오류 메시지가 보신 것과 같은지 확인해 주세요.

### 2026-09-07 · 임시저장 · 글쓰기 화면 스크롤 막대 · Cloudinary 콘솔 오류

셋을 같이 봤습니다.

**스크롤 막대가 화면 가운데에 서던 것.** 1600px 짜리 화면에서 막대가 오른쪽
끝이 아니라 80px 안쪽에 있었습니다. `ControlPaneContainer` 라는 이름이 **둘**
인데(바깥은 `PreviewPaneContainer-ControlPaneContainer`) 폭을 재우는 규칙이
`[class*=…]` 로 **둘 다** 잡고 있었고, 스크롤은 바깥이 합니다 — 바깥에
`max-width: 1440px; margin: 0 auto` 가 걸리니 막대까지 같이 가운데로
들어왔습니다. 바깥은 화면 폭을 그대로 쓰게 두고 안쪽만 재웁니다.

- 1600px 에서 스크롤 판이 0~1600, 글은 80~1520 인 것을 재서 확인했습니다. 폰(375px)에서는 판이 0~375, 여백 24px 입니다.
- ⚠ **바깥 판에 폭이나 여백을 주지 마세요.** 좁은 화면 여백도 안쪽에서 한 번에 줍니다 — 두 판에 나눠 주면 그 규칙이 서로를 못 이깁니다.

**임시저장을 넣었습니다** (`admin-src/drafts.js` · 규약은 §6-2). 「저장」이 곧
커밋이라 그 사이에 탭이 죽으면 쓰던 글이 통째로 없어졌습니다. 손을 멈추고
1.5초 뒤에 이 브라우저에 한 벌 남기고, 다음에 열면 폼 맨 위에서 되살릴지
묻습니다.

- **값은 위젯이 직접 등록합니다.** 커버 사진과 저자에는 input 이 없어서 DOM 으로는 값을 넣을 수가 없습니다 (커버 칸이 「발행 설정」으로 떨어졌던 것과 같은 자리 — §6-2).
- ⚠ **잡은 함정 둘.** ① 타이머를 다시 거는 조건을 "남겨 둔 것과 다른가" 로 뒀더니, 손질 한 바퀴가 글자마다 도는 자리라 1.5초가 매번 되감겨 **영영 안 터졌습니다** — 10초 천장에 걸릴 때만 남아서 모든 저장이 10초씩 늦었습니다. "지난 바퀴에 본 것과 다른가" 로 바꿨습니다. ② `new` 에서 나가면 무조건 버리게 뒀더니, **쓰다 만 새 글을 두고 옛 글을 한 번 열어 보는 것만으로 남겨 둔 것이 사라졌습니다** — 도착한 주소가 그 글의 `slug` 와 같을 때만 버립니다.
- ⚠ **본문의 `get()` 은 tiptap 을 안 훑습니다.** 글자마다 마크다운으로 옮기면 긴 글에서 눌립니다 — 마지막으로 내보낸 것을 돌려주고, 남기기 직전에만 `flush()` 로 맞춥니다.
- ⚠ **실패하면 안 버립니다.** 성공·실패는 Decap 의 알림 클래스로 가르고, 클래스가 없으면 실패로 봅니다 — 남는 쪽이 안전합니다.
- 로컬 Decap 으로 확인했습니다 — 여섯 칸이 다 남는 것, 되살리기·버리기, 서식 모드와 **원문 모드(react-32)** 양쪽 복원, 손으로 누르는 「임시저장」, `pagehide` 로 즉시 남는 것, 실제로 저장해서 파일이 써지고 남긴 것이 사라지는 것, 제목을 비워 **실패시켰을 때는 남아 있는 것**, 새 글을 두고 딴 글로 갔다 와도 남아 있는 것. 왕복 검사 그대로 (129편 중 119 / 76), 빌드 142쪽 통과.
- ⚠ **시험용으로 만든 `zz-drafts-test.md` 는 지웠습니다.**

**Cloudinary 콘솔 오류는 우리 것이 아니었습니다.** 보내주신 CORS·404·Rollbar
는 전부 **사진 고르는 창(iframe) 안에서 Cloudinary 가 자기 서버에** 낸
것입니다 — MediaFlows 라는 안 쓰는 제품을 찾아보다 404 가 나고, 그 404 응답에만
CORS 헤더가 빠져 있습니다 (로그인 없이 부르면 401 + 헤더 정상인 것을 재서
확인했습니다). **우리 쪽에서 붙일 자리가 없고 아무것도 안 망가집니다** —
§6-5 에 적어 뒀으니 다음에 또 파헤치지 마세요.

### 2026-09-07 · 저장이 아예 안 나가던 것

"글 자체가 그냥 저장이 안 된다" 고 하셔서, 보내주신 콘솔에서 바로 나왔습니다 —
제가 넣어 둔 경고가 세 번 찍혀 있었습니다.

```
[lim admin skin] 저장 메뉴를 못 찾았습니다 — 직접 골라 주세요
```

Decap 의 「저장」은 드롭다운이라 [저장] → [지금 게시] 두 번입니다. 그 **두
번째를 코드가 대신 눌러 주는데**, 메뉴를 못 찾으면 저장이 시작조차 안 됩니다.
저장소를 봤더니 `/admin` 이 만든 커밋이 **09-04 이후로 하나도 없었습니다.**

- **기다리는 시간이 360ms 였습니다** (30ms × 12). 빈 글로 재 보면 드롭다운이 **6ms** 만에 떠서 그 숫자를 믿고 잡았는데, 글이 길거나 기기가 느리면 React 가 그 안에 못 그립니다. **MutationObserver 로 기다리고 6초**로 바꿨습니다 — 뜨는 순간 바로 누릅니다.
- ⚠ **조용히 넘어가던 것도 고쳤습니다.** 예전에는 `console.warn` 한 줄이 전부라 화면에는 아무 표시가 없었습니다 — 그래서 "아무 일도 안 일어난다" 였습니다. 이제 화면에 알리고, **다음 「저장」 한 번은 가로채지 않아서** Decap 원래 두 단계로 저장할 수 있게 둡니다.
- ⚠ **선택자를 느슨하게 풀지 않았습니다.** `[role='menuitem']` 만으로 찾으면 오른쪽 위 아바타 메뉴의 **「로그아웃」이 걸립니다.** `[class*='DropdownList']` 안에서만 찾습니다.
- **로컬은 계속 잘 됐습니다** — 그래서 못 잡고 있었습니다. 6ms 대 360ms 라 로컬에서는 어떻게 해도 재현이 안 됩니다. 재현은 `document.querySelector` 를 잠깐 갈아끼워 "메뉴가 안 뜨는 상황"을 만들어서 했습니다.
- 정상 저장(파일이 실제로 써지는 것), 실패했을 때 알림이 뜨는 것, 그 다음 한 번이 Decap 으로 통과하고 그 다음부터는 다시 우리 모달이 뜨는 것까지 확인했습니다. 왕복 검사 그대로 (129편 중 119 / 76), 빌드 142쪽 통과.

### 2026-09-07 · 저장했다는 것을 알려 주고, 커버 사진도 끌어다 놓습니다

"저장을 누르면 됐다는 알림이 떠야 하는데 아무 일도 안 일어난다" 고 하셔서
로컬에서 끝까지 눌러 봤습니다. **저장은 되고 있었습니다** — 파일이 그대로
써졌고, Decap 도 "항목 저장됨" 알림을 띄우고 있었습니다.

빈 자리는 **누른 뒤와 결과 사이**였습니다. 모달의 「저장」을 누르면 그
자리에서 모달을 닫아 버렸는데, 배포의 GitHub 백엔드는 저장이 곧 커밋이라
몇 초가 걸립니다. 그동안 화면에 아무 표시가 없으니 눌린 건지 아닌지조차
알 수가 없었습니다.

- **결과가 올 때까지 「저장하는 중…」 으로 둡니다.** 끝난 것은 Decap 의 알림을 보고 압니다 — 저장이 됐는지 아는 것은 Decap 뿐이라, **판정은 그대로 두고 보이는 자리만** 손봤습니다.
- ⚠ **감시자는 "기다리는 중"일 때만 반응합니다.** 알림은 저장 말고도 뜨는데, 아무 때나 모달을 닫으면 `closeSaveModal()` 이 초점을 위 「저장」 단추로 옮겨서 **글 쓰던 중에 커서가 튑니다.** 실제로 그렇게 만들었다가 가짜 알림을 띄워 재현하고 막았습니다.
- ⚠ **20초 뒤에는 풀어 줍니다** (네트워크가 멈춘 경우). 대신 **됐다고 알리지는 않습니다** — 안 된 것을 됐다고 하는 쪽이 훨씬 나쁩니다.
- **알림 색을 이 화면 것으로 맞췄습니다.** 기본값은 흰 글자에 초록/빨강이라 다른 사이트에서 온 것처럼 보였습니다. 성공은 왼쪽에 강조색 선, 실패는 붉은 선.
- ⚠ **알아낸 것: 재던 값이 계측 탓이었습니다.** 알림이 x=3948(화면 밖)에 있길래 한참 팠는데, `document.hidden` 이라 **CSS 애니메이션이 안 돌아서** 등장 애니메이션(`bounceInRight` 은 3000px 에서 시작합니다)의 첫 프레임에 멈춰 있던 것이었습니다. 확인용 애니메이션을 하나 태워 보고 알았습니다 — 숨은 창에서 좌표를 재면 이런 것에 속습니다.

**커버 사진.** 칸에는 "끌어다 놓거나 아래에서 고르세요" 라고 **적혀 있었는데
실제로는 안 됐습니다.** Decap 의 image 위젯이 떨어뜨리기를 안 받습니다.

- **위젯을 감싸서 붙였습니다** (`LimImageControl`). 저자 알약·주소 칸과 같은 방식입니다.
- ⚠ **본문과 같은 길로 올립니다** (`upload.js` 의 `uploadImage`). 저장소로 올리는 `onPersistMedia` 를 쓰면 base64 커밋이라 §6-5 와 부딪히고, 주소 모양도 단추로 고른 것과 갈라집니다. 실제로 떨어뜨려 보고 `…/c_limit,f_auto,q_auto,w_1600/…` 로 같은 변환이 붙는 것을 확인했습니다.
- ⚠ **`dragover` 와 `drop` 을 둘 다 막습니다.** 안 막으면 브라우저가 그 파일로 페이지를 넘겨서 쓰던 글이 날아갑니다.
- **`upload_preset: lim-it-blog` 이 실제로 만들어져 있는 것을 확인했습니다** (전에는 "아직 없는 이름"이라고 적어 뒀습니다).
- 로컬 Decap 으로 확인했습니다 — 끌어다 놓으면 테두리가 강조색으로 변하고 「사진 올리는 중…」이 뜬 뒤 커버가 채워지는 것, 저장이 실제로 파일로 나가는 것, 실패했을 때 Cloudinary 가 준 이유가 그대로 뜨는 것. 왕복 검사 그대로 (129편 중 119 / 76), 빌드 142쪽 통과.
- ⚠ **감싸는 div 를 하나 넣는 것이 커버 칸 디자인을 깼습니다.** 커버 판 CSS 가 `[data-field='heroImage'] > *` 로 **바로 밑 자식**을 겨냥하고 있어서, 점선 판이 감싼 div 로 가고 진짜 위젯은 카드 배경 규칙에서 빠져 **Decap 기본 흰 상자**가 됐습니다. 감싼 판은 아무것도 안 그리게 하고 두 규칙에 `> .lim-cover-drop >` 를 한 칸 더 뚫었습니다. ⚠ 「사진 올리는 중…」 줄(`.lim-cover-note`)은 그 규칙들에서 **빼야 합니다** — 안 빼면 그 줄에도 점선 상자와 안내 문구가 붙습니다.
- ⚠ **끌고 있는 동안의 강조는 `outline` 입니다. `border-color` 는 이 자리에서 안 먹습니다** — `!important` 에 특이도를 (0,8,0)까지 올려도 점선 색이 그대로였습니다 (같은 규칙 안의 `background-color` 는 먹습니다). 원인은 못 밝혔고, `outline` 은 특이도를 낮춰도 그대로 먹는 것을 재서 확인했습니다.
- 곁다리로 `--a-text-dim` 을 `--a-dim` 으로 고쳤습니다 — **없는 이름**이라 글자색이 조용히 본문색으로 떨어지고 있었습니다 (표 손잡이·커버 안내 줄).
- ⚠ **확인하느라 Cloudinary 에 1×1 짜리 시험 사진 두 장이 올라갔습니다** (`dot.png`·`cover-test.png`). unsigned 업로드라 지울 수가 없으니 콘솔에서 지워 주세요.

### 2026-09-07 · 표를 노션처럼 — 손잡이를 붙이고 「합치기」를 뺐습니다

"표를 넣을 때 매우 불편하다, 행·열을 추가하는 방법도 모르겠고 삭제도 잘
안 된다" 고 하셔서 봤습니다. 어제 넣은 표 줄이 도구 띠 **셋째 줄**에
`행+ 행↑ 행− 열+ 열← 열−` 이라는 작은 글자 여섯 개였습니다. 표에서 멀리
떨어져 있고, 어느 단추가 **어느 줄**을 가리키는지 화면만 봐서는 알 수가
없습니다 — 커서가 있는 줄인데 그게 안 보입니다.

- **표에 직접 손잡이를 붙였습니다** (`admin-src/table.js`). 열은 위, 행은 왼쪽. 누르면 그 줄이 통째로 골라지고 「왼쪽에 넣기 / 오른쪽에 넣기 / 지우기」가 뜹니다. 오른쪽 끝과 아래의 **＋** 는 끝에 한 줄 더. 노션과 같은 배치입니다.
- ⚠ **문서를 하나도 안 건드립니다.** 손잡이는 표 위에 겹쳐 놓는 판이라 저장되는 마크다운에 흔적이 없습니다 — 왕복 검사도 그대로입니다 (129편 중 119 / 76).
- ⚠ **알아낸 것: 「합치기」는 저장하면 사라집니다.** GFM 표에 `colspan` 을 적을 자리가 없어서 편집기에서만 합쳐집니다 — 재현했더니 `<th colspan="2">` 가 `| 가 |  |` 로 나갔습니다. **어제 넣은 단추가 처음부터 거짓말이었습니다.** 뺐고, 다시 넣지 마세요. 다행히 실제로 쓴 글은 0편입니다 (`colspan` 으로 찾아봤습니다).
- ⚠ **잡은 함정 셋.** ① 메뉴 노드를 들고 있었더니, 손잡이를 누를 때 바뀌는 칸 선택이 곧 다시 그리기라 메뉴가 그 자리에서 지워졌습니다 — "어느 손잡이가 열렸나"만 남기고 메뉴도 그리기 안에서 같이 그립니다. ② 명령을 `chain().focus()` 로 불렀더니 포커스가 먼저 가면서 칸 선택이 풀려서 「이 열 지우기」가 칸 하나만 지웠습니다 — prosemirror-tables 명령을 직접 부릅니다. ③ 판에 `pointer-events: none` 을 안 주면 표를 덮어서 칸을 눌러도 커서가 안 들어갑니다.
- **폰을 따로 챙겼습니다.** hover 가 없으니 **고른 자리가 든 표**에도 그립니다 — 칸을 한 번 탭하면 뜹니다. 375px 에서 손잡이와 메뉴가 편집기 안에 들어오는 것을 재서 확인했습니다. 그러느라 `.ProseMirror` 좌우·위 여백을 14 → 22px 로 넓혔습니다 (손잡이가 설 자리).
- **안 한 것: 끌어서 열 순서 바꾸기.** 노션은 되는데 칸 내용을 통째로 옮기는 일이라 따로 짜야 합니다. 지금 불편의 원인은 순서가 아니라 "넣고 지우는 길이 안 보이는 것"이라 거기까지 갔습니다.
- 로컬 Decap 으로 확인했습니다 — 새 글에서 2×3 → 열 넣기 → ＋행 → ＋열 로 4×4 를 만들고, `A B C D` 를 넣고 2번째 열을 지워 `A C D` 가 되는 것(엉뚱한 줄이 안 지워지는 것), 저장되는 마크다운이 `| A | C | D |` 인 것, 원문 모드에서는 손잡이가 안 그려지는 것. **옛 글(`redirect-rewrite`, 6×3)** 에서는 손잡이 자리가 칸과 **픽셀까지 일치**하는 것(줄 높이가 61~174px 로 제각각인 표입니다)과, 가운데 열만 지워지고 나머지 문단 25개가 그대로인 것을 봤습니다. 빌드 142쪽 통과.

### 2026-09-07 · 정렬 · 글쓰기 화면 폭 · 표 조작

셋을 같이 손봤습니다.

**정렬.** §6-2 에 "마크다운에 담을 자리가 없어서 안 가져왔다"고 적어 뒀던
것인데, 이 블로그는 글자 색·형광펜·사진 크기를 이미 HTML 로 내보내고 있어서
같은 길이 있었습니다.

- ⚠ **문단에 속성만 붙이는 방식은 막혔습니다.** `@tiptap/markdown` 이 `paragraph` 같은 기본 토큰의 렌더러를 먼저 잡아 두고 그것만 씁니다 — `parseMarkdown` 을 같이 줘도 안 이깁니다 (번들에서 확인하고 실제로 가로채 보려다 실패). 그래서 **새 이름의 블록으로 감쌉니다**: `<div class="ta-center">…</div>`.
- ⚠ **안쪽까지 HTML 로 내보냅니다.** CommonMark 는 HTML 블록 안의 마크다운을 안 읽어서, 안 그랬으면 블로그에서 `**굵게**` 가 별표째 보였습니다. `generateHTML` 로 바꿔 넣고, 되읽을 때 마크·제목·사진이 다 살아 돌아오는 것을 재서 확인했습니다.
- 왕복 검사는 그대로입니다 (129편 중 119 / 76) — 정렬을 준 덩어리만 HTML 이고 나머지 문단은 하나도 안 건드립니다.

**글쓰기 화면 폭.** "화면은 큰데 가운데만 조금 쓴다"고 하셔서 봤더니
머리띠와 목록은 1440px 인데 글쓰기 화면만 바깥 1240 · 안쪽 **760px** 로
못박혀 있었습니다. 바깥을 1440 으로 맞추고 안쪽은 1200px·1600px 에서
넓힙니다 (760 → 940 → 1120).

- ⚠ **화면 폭을 다 쓰지는 않았습니다.** 블로그 글 지면이 56rem(896px)이라, 편집기를 그보다 훨씬 넓히면 줄바꿈 위치가 올린 글과 달라져서 "보이는 대로 나온다"가 깨집니다. **더 넓히고 싶으면 그 대가를 알고 넓히세요.**

**표.** 넣고 나면 행·열을 더하거나 지울 방법이 아예 없었습니다 (표 자체를
지우기도 어려웠습니다). 표 안에 커서가 있을 때만 도구 띠 아래에 아홉 개가
나옵니다 — 사진 크기·코드 언어와 같은 자리입니다.

- ⚠ **도구 띠에 상시로 두지 않았습니다.** 단추가 이미 스물여섯이라 아홉 개를 더하면 표를 안 쓰는 동안에도 계속 보입니다.
- 로컬 Decap 으로 행·열을 더하고 지우고, 머리행을 켜고 끄고, 표를 지운 뒤 그 줄이 사라지는 것까지 확인했습니다. 표가 마크다운으로 그대로 나가는 것도 봤습니다.
- 정렬은 가운데→오른쪽→풀기와 눌림 표시, 화면에서 실제로 `text-align: center` 가 먹는 것, 저장되는 마크다운, 그걸 되읽는 것까지 확인했습니다. 블로그 쪽은 임시 글을 하나 빌드해서 `<div class="ta-center">` 가 그대로 나오는 것을 보고 지웠습니다. 빌드 142쪽 통과.

### 2026-09-07 · 편집기에 사진을 떨어뜨리거나 붙여넣을 수 있습니다

Cloudinary 로 옮기고 나서도 사진을 넣는 길은 「사진」 단추 하나뿐이었습니다.
끌어다 놓으면 브라우저가 그 파일로 페이지를 넘겨서 **쓰던 글이 날아갔습니다.**

- **올릴 자리를 고르는 데서 걸렸습니다.** Decap 위젯이 주는 `onPersistMedia` 는 **저장소로** 올립니다 — base64 커밋이라 이틀 전에 Cloudinary 로 옮긴 이유와 정면으로 부딪힙니다. Decap 이 쓰는 Cloudinary 미디어 라이브러리 위젯은 `show()`/`hide()` 뿐이라 손에 든 File 을 건넬 수도 없습니다 (번들에서 확인). 남는 길은 브라우저에서 바로 올리는 것뿐이고, 그래서 **unsigned 업로드 프리셋**을 씁니다.
- ⚠ **프리셋 이름은 공개됩니다** (저장소가 공개입니다). 막는 자리는 코드가 아니라 Cloudinary 콘솔의 프리셋 설정입니다 — 폴더 고정 · 최대 용량 · 허용 형식. **서명 방식**(api_secret 을 든 서버리스 함수)도 놓고 견줬는데, 로컬 `npm run cms` 에는 `/api` 가 없어서 떨어뜨리기를 배포에서만 확인하게 됩니다. 알고 unsigned 로 골랐습니다.
- ⚠ **단추로 넣은 사진과 주소 모양을 맞춰야 했습니다.** 단추 쪽은 Cloudinary 가 만들어 둔 derived 주소라 변환이 붙어 있는데, 직접 올리면 원본 주소만 돌아옵니다 — 안 맞췄으면 **떨어뜨린 사진만 폰 원본 5MB** 로 걸렸습니다. `default_transformations` 를 읽어서 같은 조각을 끼웁니다.
- ⚠ **「올리는 중」을 본문에 넣지 않았습니다.** blob: 주소를 가진 임시 사진을 끼우면, 올리는 동안 글을 치는 순간 그게 파일에 저장됩니다. decoration 으로 뒀습니다 — 화면에만 있어서 저장될 수가 없고 자리는 알아서 따라 움직입니다.
- ⚠ **원문 모드에도 달았습니다.** 거기는 그냥 textarea 라 안 막으면 글이 날아가는데, **옛 글은 늘 원문으로 열립니다** (129편 중 119편).
- **잡은 버그 하나**: 여러 장을 한꺼번에 떨어뜨리면 순서가 뒤집혔습니다. 넣은 뒤 자리를 `selection.from` 으로 잡았는데 그 값이 방금 넣은 사진 **앞**이었습니다 — `to` 로 고쳤습니다.
- **안 고친 것 하나**: 사진이 사라지는 것처럼 보여서 한참 팠는데, **숨겨진 브라우저 창에서 타이머가 늦춰져서** 제가 세는 시점이 업로드보다 빨랐던 것이었습니다. 그것 때문에 넣었던 방어 코드(`componentDidUpdate` 의 메아리 걸러내기)는 **되돌렸습니다** — 실제로 일어나는 것을 한 번도 못 봤습니다.
- 로컬 Decap 으로 확인했습니다 — 떨어뜨리기·붙여넣기·세 장 순서·점선 표시·「올리는 중」이 사라지는 것, 저장되는 마크다운이 단추 때와 똑같은 `![설명](…c_limit,f_auto,q_auto,w_1600/…)` 인 것, 실패하면 Cloudinary 가 준 이유가 그대로 뜨고 사진이 안 들어가는 것, 10MB 넘는 사진과 사진 아닌 파일이 막히는 것, 옛 글(`react-32`)을 원문으로 열어 떨어뜨린 것까지. 왕복 검사는 그대로입니다 (129편 중 119 / 76). **실제 Cloudinary 업로드만 못 해 봤습니다** — 프리셋을 아직 안 만들었습니다 (아래).
- ⚠ **`upload_preset: lim-it-blog` 은 그때는 없는 이름이었습니다.** 2026-09-07 에 실제로 올려 보고 **만들어져 있는 것을 확인했습니다** — 떨어뜨리기가 그대로 됩니다.

### 2026-09-07 · 사진을 Cloudinary 로 올립니다

"사진 올리는 게 매우 불편하다" 고 하셔서 봤습니다. 겹친 것이 둘이었습니다 —
GitHub 백엔드가 사진을 **base64 커밋**으로 올려서 폰 원본(3~5MB)이 느리고
`.git` 이 계속 불어나는 것(지금 68MB), 그리고 `media_folder` 가
`{{fields.slug}}` 라 **주소를 먼저 적어야** 사진 단추가 열리는 것.

- **Neon 이야기가 나왔는데 그건 답이 아닙니다.** Neon 은 Postgres 지 파일 저장소가 아닙니다. bytea 로 넣으면 정적 사이트에 사진을 내주는 함수를 하나 더 세워야 하고, CDN 이 안 붙고, 요청마다 compute 가 깨어납니다. **DB 가 필요한 이유가 없습니다** — 글은 계속 `.md` 파일이고 필요한 건 사진 주소 한 줄입니다.
- **Cloudinary 로 정했습니다.** 새로 짤 코드가 거의 없는 게 컸습니다 — `decap-cms@3.9.0` 번들에 미디어 라이브러리가 **이미 들어 있고 등록까지 돼 있습니다** (번들에서 `registerMediaLibrary(cloudinary)` 확인). `<script>` 를 더 걸 필요가 없습니다.
- ⚠ **`cover.ts` 를 같이 고쳐야 했습니다.** 커버는 본문 첫 사진에서 뽑는데(§4) 거기가 `startsWith('/')` 로 바깥 주소를 전부 막고 있었습니다 — 티스토리 CDN 이 만료돼서 만든 규칙입니다(§5-2). 안 고쳤으면 **새 글은 커버가 전부 저자 색 판**이 됐습니다. `ALLOWED_HOSTS` 로 **Cloudinary 하나만** 뚫었습니다. `startsWith('http')` 로 넓히지 마세요 — 그러면 티스토리 주소 116편이 도로 걸립니다.
- **원본을 그대로 안 겁니다.** `default_transformations` 로 긴 변 1600px · 화질과 형식 자동입니다. 안 걸면 폰 원본 5MB 가 그대로 본문에 붙습니다.
- **`openMedia()` 의 "주소를 먼저 적어 주세요" 가드를 걷어냈습니다.** 저장소를 안 거치니 주소가 필요 없습니다. ⚠ Cloudinary 를 되돌리면 이 가드도 같이 되살리세요 — 안 그러면 사진이 `public/images` 바닥에 쌓입니다. 컬렉션의 `media_folder` 두 줄은 지우지 않고 주석만 달아 뒀습니다.
- **옛 사진 429장은 안 옮겼습니다.** 옮겨도 `.git` 히스토리에 남아서 68MB 가 안 줄고(`filter-repo` 를 돌려야 합니다), 만료 없는 주소를 굳이 만료될 수 있는 곳으로 보내는 셈입니다. ⚠ 대신 **고르는 창에서 옛 사진을 못 봅니다** — 전역 `media_library` 가 Decap 내장 라이브러리를 대신합니다. 이미 본문에 박힌 주소라 화면에는 그대로 뜹니다.
- ⚠ **버린 것: 사진이 저장소에 있어서 안 죽던 성질.** 계정이 막히거나 한도를 넘으면 새 사진이 404 입니다 — 티스토리 때와 같은 자리입니다. 알고 고른 것입니다.
- `cover.ts` 는 다섯 경우를 돌려 확인했습니다 (내부 경로 · Cloudinary 마크다운 · Cloudinary `<img>` · **티스토리 주소는 여전히 거절** · 코드블록 안은 무시). `config.yml` 은 파싱해서 `default_transformations` 가 배열의 배열로, `api_key` 가 문자열로 들어가는 것까지 봤습니다. 빌드 142쪽 통과.
- ⚠ **`cloud_name`·`api_key` 는 아직 자리표시자입니다** (`CLOUD_NAME_HERE`·`API_KEY_HERE`). 채우기 전에는 사진 단추가 안 열립니다.

### 2026-09-07 · 한글 치는 중에는 단축키가 안 듣던 것 · 사진 크기 · 코드 블록

"단축키가 안 되는 것도 매우 불편합니다" 를 재현하려고 새 글에서 하나씩
눌러 봤는데 **영문으로는 전부 멀쩡했습니다.** 조합(IME)을 켜 놓고 다시 재
보니 그때만 아무 일도 안 일어났습니다.

- **ProseMirror 가 조합 중에 들어온 키를 통째로 버립니다** (`inOrNearComposition` 이 keydown 을 그냥 반환합니다). 한국어는 마지막 글자가 **거의 항상 조합 중**이라, 사실상 "가끔 되는" 게 아니라 "거의 안 되는" 상태였습니다. ⌘·Ctrl 이 눌린 키에 한해 조합을 먼저 끝내고 넘깁니다 (`ImeShortcuts`). ⚠ Enter·Backspace 까지 손대면 한글 입력이 망가집니다 — 그 둘은 IME 가 직접 써야 합니다.
- ⚠ **⌘⌥C(코드블록)만은 여전히 안 됩니다.** 크롬·사파리가 개발자도구를 여는 자리라 페이지에서 못 막습니다. 키를 옮기는 대신 그대로 두고 적어 뒀습니다 — 단추가 있습니다.
- **사진 크기를 넣었습니다.** 사진을 고르면 도구 띠 아래에 슬라이더가 나옵니다. `![설명](주소)` 에 크기를 적을 자리가 없어서 **크기를 준 사진만 `<img … width>` 로** 내보냅니다. ⚠ 100% 는 크기를 지우는 것이고 그래야 `![...]` 로 돌아갑니다. 읽는 쪽은 원래 됐고(재 보고 확인), 없던 것은 내보내기뿐이라 거기만 얹었습니다.
- **코드 블록을 노션처럼 했습니다.** 판 위에 언어 이름과 「복사」가 붙습니다(`rehypeCodeChrome`). ⚠ 머리 줄을 판 **안**에 넣지 마세요 — Shiki 가 배경을 `!important` 로 박아 색을 못 맞춥니다. 판 색은 `--surface` 로 덮었는데(라이트에서 `#fff` 라 카드와 구분이 안 됐습니다), 그랬더니 **토막마다 깔린 흰 배경이 줄 앞머리에 네모로 남아서** 그것도 투명으로 덮었습니다.
- 편집기 쪽에도 코드 언어 고르개를 달고, 코드 블록을 **줄바꿈 없이 가로 스크롤**로 바꿨습니다 — 블로그가 `wrap: false` 라 여기서만 접히면 몇 줄인지가 달라집니다. 색칠은 안 넣었습니다 (하이라이터를 하나 더 얹어야 하고 번들만 커집니다).
- 로컬 Decap 으로 확인했습니다 — 조합을 켜 놓고 ⌘B 가 듣는 것, 슬라이더 50% 가 `width="364"` 로 저장되는 것, 언어를 바꾸면 ```javascript 로 나가는 것, 왕복 검사가 그대로인 것(129편 중 119 / 76). 블로그 쪽은 라이트·다크에서 판 색과 언어 라벨을 재서 봤습니다. **「복사」 단추만 이 환경에서 못 눌러 봤습니다** — 미리보기 창이 숨어 있어 클립보드 권한이 막힙니다(`denied`). 눌리는 것과 실패 시 글자가 바뀌는 것까지는 확인했습니다.

### 2026-09-07 · 편집기 위쪽 빈 자리와 인용문 모양

"에디터 위쪽에 빈 공간은 왜 있는지 모르겠다"고 하셔서 봤더니, 글의 첫
덩어리가 `##` 제목일 때 그 제목의 위 여백(2.5em)이 도구 띠 바로 아래에
그대로 남는 것이었습니다. 위에는 아무것도 없으니 띄울 이유가 없어서 첫
덩어리의 위 여백만 0 으로 만들었습니다.

- **블로그(`.prose`)도 같이 고쳤습니다.** 편집기 안쪽은 `.prose` 를 옮긴 것이라 한쪽만 고치면 갈라집니다 (§4). 글 첫 줄이 제목인 글에서 제목 아래 여백이 그만큼 줄어듭니다.
- **인용문을 노션 모양으로 바꿨습니다** — 왼쪽 선 하나, 배경도 모서리도 없앴습니다. 판을 두르고 있으면 인용문이 코드블록·표와 같은 무게로 읽혀서 문단 사이에 덩어리가 하나 더 있는 것처럼 보입니다. ⚠ 배경을 다시 깔지 마세요 (§4 에 적어 뒀습니다).
- 인용문이 `--accent`·`--surface-2` 를 쓰던 유일한 자리가 아니었는지 확인하고 §4 의 토큰 표를 지금 것으로 고쳤습니다 (LIVE 배지·읽기 막대·포커스 테두리 / 표 머리·비밀글 자물쇠 판).
- 로컬 Decap 으로 새 글에 제목·문단·인용문을 넣어 보고, 첫 제목의 위 여백이 0 이 된 것과 인용문이 선 하나로 나오는 것을 재서 확인했습니다. 블로그 쪽은 `beeptimer-92` 에서 배경 없음·모서리 0·선 3px 을 확인했습니다.

### 2026-09-04 · `/admin` 에서 작성일 칸을 뺐습니다

"작성일이 있는 이유가 뭐냐, 예약 발송이 되냐"고 물으셔서 코드를 봤습니다 —
**안 됩니다.** 미래 날짜를 걸러내는 곳이 어디에도 없어서, 그렇게 적으면 그
자리에서 바로 나가고 맨 위로 정렬되기만 합니다. 그러면 손으로 고를 이유가
없어서 화면에서 뺐습니다.

- **하지만 "저장할 때마다 지금 시각"은 안 했습니다.** 옛 글의 오타 하나를 고쳐도 날짜가 오늘로 바뀌어 서고·홈·RSS 맨 위로 올라옵니다 — 티스토리에서 옮겨온 116편이 2022~2026 의 원본 날짜입니다. **새 글에만 지금 날짜가 들어가고 옛 글은 그대로 둡니다** (`slug` 를 채우는 것과 같은 규칙 — 값이 있는 칸은 안 건드립니다).
- ⚠ **칸을 안 그리는 것이 아니라 감추기만 합니다.** `{{now}}` 를 진짜 시각으로 바꾸는 일은 Decap 의 DateTime 위젯이 **mount 될 때** 합니다 (번들에서 확인). 안 그리면 새 글의 작성일이 `{{now}}` 라는 글자 그대로 저장되고 빌드가 깨집니다. `widget: hidden` 도 같은 이유로 안 됩니다.
- 새 글에 오늘 날짜가 들어가는 것과, 옛 글(`react-32` · 2022-04-18)을 열어 고치고 저장해도 날짜가 그대로인 것을 파일에서 확인하고 되돌렸습니다.
- **§3 에 예약 발송이 안 된다는 것을 적어 뒀습니다.** 안 내보내려면 `draft: true` 입니다.

### 2026-09-04 · 「발행 설정」을 저장할 때 묻고, 프로젝트를 라디오로 바꿨습니다

작성일·프로젝트·초안·비밀글 넷이 본문 아래에 늘 펼쳐져 있었습니다. 글을 쓰는
동안에는 볼 일이 없는데 폰에서 스크롤만 길어지고, 정작 저장할 때는 위 띠만
보고 눌러서 초안 토글을 그냥 지나쳤습니다. 「저장」을 누르면 묻게 했습니다.

- **Decap 의 「저장」이 드롭다운이라는 게 실마리였습니다.** 원래도 [저장] → [지금 게시] 두 번이었습니다. 그 첫 번째 누름을 가로채 모달을 열고, 모달의 「저장」이 원래 순서를 대신 밟습니다 — **누르는 횟수는 그대로**입니다.
- ⚠ **`PublishButton` 일 때만 가로챕니다.** 고칠 것이 없는 글에서는 같은 자리가 `PublishedToolbarButton`(메뉴에 「복제」뿐)이라, 거기까지 가로채면 복제를 못 합니다. 두 이름이 서로 부분문자열이 아닌 것을 재 보고 확인했습니다.
- ⚠ **모달을 `pane` 밖으로 옮기지 마세요.** 안에 든 것이 React 가 그린 필드 칸이라, 글 화면이 닫힐 때 유령으로 남습니다. `position: fixed` 로 자리만 띄웁니다.
- ⚠ **버린 것: 드롭다운의 나머지 둘** (「게시하고 새로 만들기」·「게시하고 복제」). 혼자 쓰는 저장소에서 쓸 일이 없다고 봤습니다. 되살리려면 모달 아래에 줄로 다세요.
- **프로젝트를 라디오로 바꿨습니다.** 저자를 알약으로 바꾼 것과 같은 이유입니다 — 셋뿐인데 react-select 는 매번 세 동작입니다. ⚠ **맨 아래 「없음」은 `config.yml` 에 없는 칸입니다.** 라디오는 눌러서 끄는 것이 없어서, 그게 없으면 한 번 고른 뒤로 영영 못 비웁니다 (`project` 는 `required: false` 입니다).
- 로컬 Decap 으로 새 글·옛 글에서 모달을 열고 취소·Esc·바깥 누르기·저장까지 돌려 봤습니다. `react-32` 에 실제로 `project: mineapp` 이 쓰이는 것을 파일에서 확인하고 되돌렸습니다. 폰 폭(375px)에서도 판이 화면 안에 들어옵니다.
- ⚠ **알아낸 것: /admin 으로 저장하면 frontmatter 의 주석이 사라집니다.** 티스토리에서 옮겨온 글의 `# 티스토리에서 옮겨왔습니다:` 줄입니다 — Decap 이 frontmatter 를 객체로 읽었다 다시 쓰기 때문이고, 이번 작업과 무관한 예전부터의 성질입니다. 그 줄을 지키려면 그 글은 손으로 고쳐야 합니다.

### 2026-09-04 · 사진 폴더가 없는 글이 `/admin` 에서 안 열리던 것

`status-effects-redefined` 를 열면 화면이 하얗게 남고 "항목 불러오기 실패:
Unknown error" 만 떴습니다. §1 이 "새 글을 쓰기 전에 읽으세요"로 가리키는
바로 그 글입니다.

- **사진 폴더가 없어서였습니다.** `media_folder` 가 글마다 갈라져 있어서 (`/public/images/{{fields.slug}}`) Decap 은 **글을 열 때** 그 폴더를 훑는데, 사진을 한 장도 안 쓴 글은 폴더가 없습니다 — 128편 중 34편입니다. `getMedia` 가 튕기면 글 읽기 전체가 같이 튕깁니다.
- **로컬(`npm run cms`)에서만 그렇습니다.** decap-server 가 없는 경로에 `fs.realpath` 를 걸어서 500 을 냅니다 (`middlewares/utils/path.ts`). **GitHub 백엔드는 멀쩡합니다** — 404 를 받고 `listFiles` 가 이미 빈 목록으로 넘깁니다 (3.9 번들에서 확인). 그래서 폰에서는 안 겪으셨을 것입니다.
- **`.gitkeep` 은 두지 않았습니다.** 사진 없는 글이 계속 나오는 한 폴더를 미리 만들어 두는 일이 끝나지 않습니다. 대신 **없는 폴더 = 사진 없음** 으로 넘깁니다 (`admin-src/media.js`) — Decap 자신도 404 를 그렇게 처리합니다.
- ⚠ **`proxy` 백엔드만 감쌉니다.** GitHub 쪽까지 감싸면 진짜 실패(토큰 만료·네트워크)도 "사진 없음"으로 삼켜서, 사진이 있는데 없는 것처럼 보이고 같은 파일을 또 올리게 됩니다.
- **곁다리 둘.** ① `flush()` 에 원문 모드 가드를 넣었습니다 — 원문 모드에서 tiptap 이 들고 있는 것은 파일의 글이 아니라 그것을 다시 쓴 것이라, 나갈 때 그걸 흘려보내면 손으로 고친 원문이 덮입니다 (지금은 Decap 이 초안을 버려서 안 덮이지만, 기댈 자리가 아닙니다). ② `TextColor` 에 Toast 시절 `wysiwygPlugins` 한 줄이 남아 있어서 지웠습니다 — `keymap`·`splitWithBlankLine` 둘 다 없는 이름입니다.
- **글자 수 줄도 같이 고쳤습니다.** 원문 모드에서 **안 보이는 tiptap 쪽**을 세고 있었고(문서 차례로 그게 먼저입니다), textarea 는 쳐도 DOM 이 안 바뀌어서 숫자가 멈춰 있었습니다. 옛 글은 원문으로 열리니 119편에서 늘 틀린 수였습니다.
- 로컬 Decap 으로 폴더 없는 글(`status-effects-redefined`·`blog-rebuild-notes`)과 있는 글(`react-32`)을 열어 확인했습니다. 서식 모드 글자 반영, 원문 모드 타이핑, 글자 수 실시간 갱신까지 봤습니다.
- **문서의 글 수를 지금 것으로 맞췄습니다** — 129·126·124 가 섞여 있던 것을 128 로. 사진도 425/420 → 429(전체)·404(티스토리에서 옮겨온 것)로 갈랐습니다. §8 의 옛 기록은 그때의 숫자라 그대로 뒀습니다.

### 2026-09-04 · `/admin` 편집기를 Toast 에서 tiptap 으로 되돌리고 하니 단축키를 옮겼습니다

Toast 가 쓰기 불편하다고 하셔서, 하니 매거진(`~/Develop/Company/hani/magazine`)의
편집기를 가져올 수 있는지 봤습니다. **그대로는 못 가져옵니다** — 저장 형식이
다릅니다. 하니는 `serializeBody = JSON.stringify(doc)` 로 **tiptap 문서 JSON** 을
저장하고 마크다운은 들어오는 쪽(`markdownToDoc`)만 있습니다. 문서 → 마크다운이
아예 없습니다. 여기는 글 129편이 `.md` 파일입니다. 게다가 `@tiptap/react` +
React 19 라 정적 `/admin` 에 얹을 수도 없습니다.

**대신 하니 편집기가 tiptap 이라, 단축키 층은 그대로 옮겨집니다.** 순수 tiptap
확장이라 React 가 필요 없습니다. 그래서 엔진을 tiptap 으로 되돌리고
`blocks.js`·`shortcuts.js` 를 옮겨 붙였습니다.

- **고른 근거는 셋이었습니다** (§6-2 에 정리): ① 단축키를 옮겨 쓸 수 있다 ② 도구 띠를 우리가 그려서 "..." 에 숨는 것을 없앨 수 있다 ③ 편집기 안쪽을 블로그 `.prose` 와 같게 맞출 수 있다.
- ⚠ **대가는 왕복입니다.** 글자가 달라지는 글이 **21편 → 76편**으로 늘었습니다. **알고 되돌아온 것입니다** — 옛 글은 어차피 원문 모드로 열리고(`isRoundTripSafe`), 매일 쓰는 것은 새 글이라 쓰는 맛을 택했습니다. 옛 글을 서식으로 열어 고칠 때만 조심하면 됩니다.
- **마크다운에 못 담는 것은 안 가져왔습니다** — 정렬·폰트·줄간격·콜아웃·토글·본문 크기. 넣으면 편집기에서는 보이는데 저장하면 사라집니다.
- **글자 색 자리(⌘⌥⇧1~5)는 하니가 본문 크기에 쓰던 자리입니다.** 본문 크기를 뺐으니 비었고, 제목이 ⌘⌥1~3 이라 ⇧ 하나만 더 누르면 되는 짝이 됩니다.
- **2026-08-28 의 Enter 우회(`splitWithBlankLine`)는 걷어냈습니다.** 그건 "원문 한 줄 = 블록 하나"인 Toast 때문에 필요했던 것이고, tiptap 은 문단을 가르면 그 자체로 빈 줄(`\n\n`)이 됩니다. 재 보고 확인했습니다 — Enter 는 `"첫 줄\n\n셋째 줄"`, Shift+Enter 는 `"첫 줄  \n이어 붙임"` 입니다. `prosemirror-keymap` 도 같이 지웠습니다.
- ⚠ **잡은 함정 하나.** Decap 이 `p` 에 글자색을 전역으로 까는 것을 잊고 있었습니다 — 본문이 `rgb(49,61,62)` 로 다크 배경에 묻혔습니다. `color: inherit` 을 도로 걸어 고쳤고, 고친 뒤 블로그 글 화면과 **문단 여백·줄높이를 재서 같은 값**인 것을 확인했습니다.
- **번들이 가벼워졌습니다** — 600KB + 185KB → 512KB + 6KB. Toast 의 CSS 가 아이콘을 base64 로 안고 있던 것이 빠졌습니다.
- 로컬 Decap 으로 새 글·옛 글을 열어 단축키(제목·형광펜·글자색)와 저장되는 마크다운(`## `, `<mark>`, `<span class="c-teal">`)까지 확인했습니다. 폰 폭에서 단추 23개가 네 줄로 다 보입니다.

### 2026-08-28 · `/admin` 에서 Enter 로 내린 줄이 결과물에서 한 줄로 붙던 것

글을 쓰면서 Enter 를 눌러 줄을 내렸는데 배포된 글은 한 줄이었습니다.
겹친 것이 둘이었습니다 — Toast 는 **원문 한 줄 = 블록 하나**로 잡아서 Enter
한 번을 줄바꿈 하나(`\n`)로 저장하고, 이 블로그는 `remark-breaks` 를 안 써서
(§2) 그 `\n` 은 화면에서 앞 줄에 그대로 이어 붙습니다.

- **편집기 쪽을 고쳤습니다.** 위지윅에서 문단을 가를 때 사이에 빈 문단을 하나 더 둡니다 — 빈 문단이 곧 마크다운의 빈 줄이라 본 대로 나옵니다. Toast 가 문서로 지원하는 `wysiwygPlugins` 에 `prosemirror-keymap` 하나를 얹었습니다.
- **`remark-breaks` 는 안 켰습니다.** 티스토리에서 옮겨온 116편이 "한 줄이 곧 한 문단"인 상태로 들어와 있어서, 켜면 그 글들의 줄 간격이 통째로 달라집니다.
- ⚠ **최상위 문단에서만 합니다.** Toast 는 플러그인 keymap 을 자기 keymap **앞에** 놓기 때문에(`WysiwygEditor.createPlugins`), 목록·인용문·표·코드블록·제목에서 `false` 를 안 돌려주면 거기 Enter 동작을 통째로 가로챕니다. 여섯 경우를 다 눌러 보고 확인했습니다.
- **Shift+Enter 는 그대로 뒀습니다** — 앞 줄에 이어 붙이는 유일한 방법이고, 티스토리에서 옮겨온 116편이 그 모양입니다. **원문 모드도 안 건드립니다** (거기서는 마크다운 소스를 직접 보는 것이라 Enter 가 줄바꿈 하나인 게 맞습니다).
- 왕복 검사는 그대로입니다 (129편 중 123 / 21) — 저장하는 방식은 안 바뀌었습니다. 로컬 Decap 으로 실제로 쳐 보고 `첫 줄` + Enter + `셋째 줄` 이 빈 줄을 사이에 두고 저장되는 것을 확인했습니다.

### 2026-09-04 · `/admin` 글쓰기 화면을 시안대로 한 열로 세웠습니다

보내주신 시안을 옮겼습니다. 왼쪽(쓰는 것)/오른쪽 카드(분류)로 갈라
놨던 것을 위에서 아래로 한 줄로 세웁니다 — 글을 쓰는 동안 눈이 좌우로
오갈 일이 없습니다.

```
저자(알약) → 제목 → 주소 → 한 줄 요약 → 커버 사진 → 본문
──────────────────────────────────────────────
발행 설정: 작성일 · 프로젝트 · 초안 · 비밀글
```

- **저자를 알약으로 바꿨습니다.** 둘뿐인데 react-select 라 "펴고 → 고르고 → 접힘" 이 매번 세 동작이었습니다. 고른 것을 다시 누르면 비웁니다 — `required: false` 라 "안 고름" 도 값인데 목록이 없으면 되돌릴 방법이 없습니다.
- **커버 사진 칸을 새로 넣었습니다** (시안의 썸네일). ⚠ 이걸 넣으려고 `heroImage` 를 **Astro 자산에서 공개 경로 문자열로** 바꿨습니다 — `/admin` 이 적어 주는 `/images/<주소>/파일명` 을 `image()` 가 못 읽고 `ImageNotFound` 로 빌드를 멈춥니다. 실제로 재현하고 확인했습니다. 쓰는 곳 넷(`cover.ts`·`PostLayout`·`BaseHead`·`BaseLayout`)을 같이 고쳤고, 커버가 글 맨 위·목록·공유 카드 세 곳에 다 붙는 것을 빌드 결과에서 확인했습니다.
- **본문 아래에 글자 수와 저장 여부를 답니다.** 긴 글에서는 위 띠가 화면 밖으로 나가서 저장이 됐는지 안 보입니다. 저장 여부는 위 띠의 글자를 그대로 읽어서 둘이 어긋날 일이 없습니다.
- 제목·한 줄 요약에 안내글을 넣고, 위 띠 이름을 새 글이면 "새 글 작성" 으로 가릅니다.
- ⚠ **필드 이름을 라벨의 `for` 에서 뽑게 고쳤습니다.** 입력칸 id 로 찾으면 사진 위젯을 놓칩니다 — 그건 input 을 안 그립니다. 그래서 커버 사진 칸이 「발행 설정」으로 떨어졌습니다.
- ⚠ **잡은 특이도 함정 둘.** ① 제목 24px 이 `> input` 규칙(태그를 하나 더 들고 있어 특이도가 높습니다)에 눌려 16px 로 나왔습니다. ② 커버 판의 점선이 「위젯 상자」 규칙의 `border` 한 줄에 덮였습니다. 둘 다 부모를 하나 붙여서 넘었습니다.
- **안 한 것: 검수 요청.** 시안 오른쪽 위가 [미리보기][임시 저장][검수 요청]인데, 혼자 쓰는 저장소라 검수도 승인도 없습니다 (`publish_mode: simple`). 글자는 「저장」 그대로 두고 색만 시안의 초록을 가져왔습니다. 칸 이름도 시안(타이틀·부제·썸네일) 대신 사이트가 쓰는 말(제목·한 줄 요약·커버 사진)을 그대로 뒀습니다 — 2026-08-25 에 정한 것과 같은 이유입니다.
- ⚠ **이 작업 도중 `admin-src/` 를 고치는 다른 세션이 같이 돌고 있었습니다.** 그쪽이 리베이스하면서 이 변경이 두 번 날아갔습니다. 같은 저장소에서 두 세션을 함께 돌리지 마세요.

### 2026-08-25 · `/admin` 목록과 글쓰기 화면을 시안대로 다시 앉혔습니다

보내주신 시안 두 장(목록·글 편집)을 옮겼습니다. Decap 기본 화면에 색만 입혀
둔 상태여서, 블로그를 고쳐 놓고 글 쓰러 들어오면 다른 사이트처럼 보였습니다.

```
목록      [컬렉션 카드] [글 · 128개 / 제목·작성자·날짜·상태 네 칸]
글 편집   [← 글 편집 · 변경사항 저장됨]        [게시됨] [삭제] [저장]
          [제목·주소·본문]              [작성일·요약·저자·프로젝트·토글]
```

- **CSS 만으로는 안 됐습니다.** 목록 한 줄이 글자 한 덩어리라 네 칸으로 가르려면 쪼개야 하고, 편집 화면은 감싸는 상자가 없으면 두 열이 안 됩니다. `admin-src/skin.js` 를 새로 만들었습니다 — 무엇을 왜 하는지는 §6-2.
- **필드는 이름으로 가릅니다.** Decap 이 입력칸마다 `<이름>-field-N` 으로 id 를 주는 것을 찾아서, emotion 해시(`css-83wr9v`)나 순서에 안 기대게 했습니다.
- **`config.yml` 의 `summary` 를 네 토막으로 바꿨습니다** — 목록의 네 칸이 여기서 나옵니다. ⚠ 구분자를 바꾸면 `skin.js` 의 `SEP` 도 같이.
- ⚠ **잡은 함정 셋.** ① Decap 을 겨냥한 규칙에 `!important` 를 안 붙이면 emotion 이 이깁니다 (그 `<style>` 이 뒤에 들어갑니다). ② MutationObserver 를 지역 변수에 두면 수거될 수 있습니다. ③ **화면이 안 보이는 동안 `requestAnimationFrame` 이 안 돌아서**, 손질이 처음 한 번만 되고 멈췄습니다 — 타이머로 바꿨습니다. 셋 다 한참 찾았습니다.
- **테마를 고를 수 있게 했습니다** (머리글의 해/달). 블로그와 같은 열쇠를 써서 한쪽에서 고른 것이 다른 쪽에도 갑니다. Toast 편집기는 다시 만들지 않고 클래스만 켰다 끕니다 — 다시 만들면 커서와 되돌리기 기록이 날아갑니다.
- **"새 글" 은 곧장 새 글로 갑니다.** 원래는 눌러서 메뉴가 열리고 컬렉션을 한 번 더 골라야 했는데, 컬렉션이 하나라 두 번 누르는 것이었습니다.
- **본문 툴바에서 "사진" 을 앞으로 당겼습니다.** 두 열이 되면서 본문 칸이 좁아졌고, Toast 는 폭이 모자라면 **뒤에서부터** "..." 안으로 밀어 넣습니다 — 제일 자주 쓰는 버튼이 숨었습니다. 자리를 만들려고 indent·outdent 를 뺐습니다 (목록 안에서 Tab 이 같은 일을 합니다).
- **안 한 것: 태그·대표 이미지 칸.** 시안 오른쪽에 있지만 태그는 2026-08-13 에 없앤 것이고(§3), heroImage 는 지금 쓰는 글이 0편입니다. 칸 이름도 시안(작성자·서비스·발행일) 대신 지금 것(저자·프로젝트·작성일)을 그대로 뒀습니다 — §3 과 사이트가 쓰는 말입니다.
- **폰에서 다른 것 둘**: 표는 제목·날짜 두 칸만 남기고(네 칸을 욱여넣으면 제목이 두세 글자만 보입니다), 위 띠의 상태 알약을 뺍니다(Decap 이 띠 높이를 66px 로 못박아 둬서 두 줄로 못 늘립니다 — 왼쪽 "변경사항 저장됨" 이 같은 자리에 있습니다).
- **뒤이어 고친 것 둘.** ① 글자를 갈아 끼운 단추(새 글·삭제·저장·탭)는 `font-size: 0` 때문에 **줄 높이가 0 이 되어** 글자가 위아래 가운데에 안 섰습니다 — flex 로 세웠습니다. ② **한 줄 요약을 textarea 로** 바꿨습니다(`widget: text`). 한 줄짜리 칸이라 써 둔 문장이 오른쪽으로 잘려서, 뭐라고 썼는지 보려면 커서를 끝까지 밀어야 했습니다. ⚠ Decap 이 이 칸 높이를 **인라인 `!important`** 로 박아서(146px) 스타일시트로는 못 이깁니다 — `max-height` 로 조여서 세 줄로 맞췄습니다.
- ⚠ **머리글 밑줄은 Decap 이 세 겹으로 깝니다** — 머리띠 말고 안쪽 내용(AppHeaderContent)과 오른쪽 묶음(AppHeaderActions)도 각자 `border-bottom` 을 들고 있습니다. 높이가 달라서 오른쪽 끝에만 짧은 선이 하나 더 그어져 보였습니다. 머리띠 것만 남깁니다.
- ⚠ **아바타 뒤 회색 원은 사진이 없을 때만 깝니다** (`:has(svg)`). GitHub 사진이 붙으면 그 사진이 곧 동그라미라, 뒤에 회색을 깔면 테두리가 한 겹 더 둘러쳐집니다.
- ⚠ **`[class*='AppHeader']` 는 머리띠만 잡는 이름이 아닙니다** — "새 글" 단추가 `AppHeaderQuickNewButton` 이라 같이 걸립니다. 머리띠에 높이를 줬더니 단추가 60px 짜리 동그라미가 됐습니다. `header[class*='AppHeader']` 로 태그까지 붙여서 잡습니다.
- ⚠ **머리띠는 56px, 안쪽 내용은 60px** 이라 내용이 밑줄 아래로 4px 삐져나왔습니다 — 머리띠 높이를 내용에 맞췄습니다.
- **본문 툴바 칸 전체를 툴바 색으로 깔았습니다.** 탭 줄(편집하기/미리보기)만 흰 바탕이면 안내문과 툴바 사이에 흰 띠가 하나 뜬 것처럼 보입니다.
- 저장·삭제·검색·사진 고르기·옛 글 원문 열기까지 로컬 Decap 으로 실제로 돌려 보고 확인했습니다. 왕복 검사는 그대로입니다 (128편 중 123 / 21).

### 2026-08-25 · `/admin` 편집기를 tiptap 에서 Toast UI 로 바꿨습니다

"tiptap 은 30일 뒤에 못 쓴다"는 이야기부터 확인했습니다 — **틀린 이야기입니다.**
설치돼 있던 `@tiptap/*` 일곱 개가 전부 MIT 이고 번들에 라이선스·만료 코드가
한 줄도 없었습니다. 30일은 Tiptap **Cloud/Pro** 체험 기간입니다.

그래도 다른 것을 써보고 싶다고 하셔서 `/preview/editors` 에 Tiptap ·
Toast UI · Milkdown 셋을 나란히 띄워 놓고 골랐습니다. **Toast 로 정했습니다.**

- **고른 이유는 왕복입니다.** 글 128편을 돌려 보면 글자가 달라지는 글이 **tiptap 76편 → Toast 21편**입니다. 편집기 셋 중에 옛 글을 제일 덜 망가뜨립니다. (Milkdown 은 **사진 설명(alt)을 통째로 날려서** 탈락했습니다 — 사진이 404장인 블로그에서는 못 씁니다.)
- **덤으로 얻은 것**: 원문 ↔ 위지윅 토글과 **미리보기**가 내장입니다. tiptap 때 "안 한 것"으로 남겨 뒀던 미리보기가 그냥 생겼습니다. 원문 모드도 맨 textarea 가 아니라 툴바가 살아 있는 편집기입니다. 표 칸 병합도 됩니다.
- **바꾸지 않은 것**: 저장 형식(마크다운 원문), 글자 색 `<span class="c-*">`, 형광펜 `<mark>`, 사진 올라가는 자리, 주소 자동 채우기. 실제 글에 `<mark>` 나 `c-*` 를 쓴 글이 **0편**이라 갈아탈 여지가 있었지만, 확인해 보니 Toast 도 두 태그를 그대로 왕복시켜서 규약을 안 건드렸습니다.
- ⚠ **`<span>`·`<mark>` 가 살아남는 건 `toHTMLRenderers.htmlInline` 에 등록했기 때문입니다.** 등록 안 하면 Toast 는 모르는 인라인 태그를 **글자만 남기고 지웁니다.**
- ⚠ **Toast 의 `color-syntax` 플러그인은 안 씁니다** — `<span style="color: #58dddf">` 를 본문에 박아서 §4 의 "색 값을 본문에 박지 않는다"와 정면으로 부딪힙니다. 같은 모양의 플러그인을 클래스로 직접 만들었습니다.
- **잡은 버그 셋** (전부 재 보고 고쳤습니다): ① 편집기를 폭 0 인 상태에서 만들면 툴바 버튼이 하나만 남고 전부 "..." 로 들어가 **되돌아오지 않습니다** → 폭이 생길 때까지 기다렸다 만듭니다. ② 툴바 칸 높이가 46px 로 못박혀 있어서 원문 모드의 탭 줄 + 버튼 줄이 본문 위로 흘러나왔습니다 → 높이를 풀었습니다. ③ 툴바 칸이 내용 크기로 줄어들면 Toast 가 "폭이 모자라다"고 보고 버튼을 더 숨기고, 그래서 더 줄어드는 되먹임이 생깁니다 → 폭을 100% 로 못박았습니다.
- ⚠ **툴바 sticky 는 걷어냈습니다.** tiptap 때는 붙여 뒀는데 Toast 는 툴바 칸과 버튼 줄이 따로 놀아서 바깥만 붙습니다.
- **왕복 검사 스크립트도 Toast 로 다시 썼습니다.** jsdom 에는 레이아웃이 없어서 ProseMirror 가 좌표를 물으면 터집니다 — 0 을 돌려주는 가짜를 끼워 넣었습니다.
- **정리한 것**: tiptap 일곱 개와 시안용으로 넣었던 다섯 개를 지웠습니다. 남은 건 `@toast-ui/editor` 와 표 병합 플러그인 둘뿐입니다. `/preview/editors` 와 `robots.txt` 의 `Disallow` 도 지웠습니다.

### 2026-08-25 · `/admin` 의 주소 칸을 미리 채웁니다

새 글을 열면 주소 칸이 비어 있어서, 폰에서 한글로 쓰다 말고 영문 자판으로
바꿔 쳐야 했습니다. 게다가 이 값은 저장할 때가 아니라 **사진을 넣는
순간** 필요합니다 (`media_folder` 가 `{{fields.slug}}`).

- **시각을 넣습니다** — `2026-08-25-0930`. 그대로 두면 되고, 고치고 싶으면 지우고 쓰면 됩니다.
- **제목에서 안 뽑습니다.** "블로그 재정비" 가 그대로 파일명이 되면 주소가 `%EB%B8%94…` 55자로 나갑니다. ASCII 만 남기는 설정은 한글 제목에서 남는 글자가 없어 빈 파일명이 됩니다.
- **번호(dev1·dev2…)도 안 씁니다.** 다음 번호를 알려면 저장소를 먼저 읽어야 하는데 새 글 화면에는 그 목록이 없을 수 있습니다. 티스토리에서 옮겨온 116편이 그 방식(`swiftui-68`)인데, 그건 티스토리 **서버가** 번호를 발급해 줬기 때문에 됐던 것입니다.
- **날짜만으로는 모자랍니다.** 128편이 71일에 몰려 있고 **31일은 하루에 두 편 이상**입니다. 그래서 분까지 넣습니다.
- ⚠ **알아낸 것: 중복 slug 를 Decap 이 덮어쓰지 않습니다.** 3.9 번들의 `generateUniqueSlug` 가 `entryExist` 로 저장소를 조회해서 `-1` 을 붙입니다. 다만 **파일명만** 바뀌고 사진 폴더는 칸의 값 그대로라 둘이 갈라집니다 — 겹치지 않게 만드는 편이 낫습니다.
- ⚠ **`config.yml` 의 `widget` 은 `string` 그대로입니다.** 새 위젯 이름을 적으면 `editor.js` 가 안 떴을 때 폼이 깨집니다. 등록된 `string` 위젯을 감싸서 이름이 `slug` 이고 값이 빈 칸일 때만 채웁니다.
- Decap 이 저장 안 된 초안에 물려서 UI 로 옛 글을 못 열길래, 위젯을 직접 태워서 다섯 경우를 확인했습니다 — 빈 값·빈 문자열은 채우고, **값이 있는 slug·제목·요약은 안 건드립니다.**
- 안 한 것: **저자 접두어(`dev-…`).** 저자는 `slug` 다음 칸이고 나중에 옮길 수도 있어서(§8 의 BeepTimer 3편) 주소가 거짓말이 됩니다.

### 2026-08-25 · 서비스 지면 셋을 시안대로 다시 짰습니다

보내주신 시안 두 장(목록·상세)을 옮겼습니다. `/projects` 가 아직 옛 3열
(`FeedLayout`)이라 홈·서고에서 넘어오면 화면이 통째로 바뀌었습니다.

```
/projects            가로 카드 셋 — 사진 | 이름·소개·스택·데브로그 수
/projects/<id>       머리(사진·버튼) + 숫자 띠 넷 + Devlog 목록
/projects/<id>/about 같은 껍데기로 맞추기만 (내용은 그대로)
```

- **`about` 은 시안에 없었지만 껍데기만 같이 옮겼습니다.** 상세에서 "소개 보기"로 바로 넘어가는 자리인데 혼자 3열로 남으면 거기서 지면이 튑니다. 안쪽 내용(소개문·할 수 있는 것·스택)은 손대지 않았습니다.
- ⚠ **시안의 문구를 그대로 옮기지 않았습니다.** 시안에는 MineApp 이 "Web / iOS / Android · TypeScript · Vite · Capacitor" 로 그려져 있는데 실제로는 iOS 전용 Swift 앱입니다. 시안은 **배치**로 보고 값은 `consts.ts` 것을 씁니다.
- **숫자 띠를 넣으면서 `PROJECTS[].stats` 를 만들었습니다.** 플랫폼과 최근 업데이트는 코드가 채우고 가운데만 손으로 씁니다. ⚠ 채운 값은 전부 `features`·`summary`·devlog 에 이미 있던 사실입니다 (GridBrawl 의 "15층"·"4단계"처럼).
- ⚠ **"최근 업데이트"는 마지막 devlog 날짜입니다.** 스토어 배포일을 알 방법이 없어서 셀 수 있는 것만 셉니다.
- **썸네일은 최신 devlog 의 커버를 끌어옵니다.** 프로젝트 대표 사진이 없어서인데, BeepTimer 는 devlog 3편에 사진이 하나도 없어서 프로젝트 색 판으로 떨어집니다 — 그것도 확인했습니다.
- ⚠ **처음에 사진 비율만 지웠다가 MineApp 카드만 593px 이 됐습니다.** 세로로 긴 폰 캡처가 커버라 사진이 거꾸로 카드 높이를 끌고 갔습니다. 틀을 띄워서 고쳤고, 셋 다 210px 로 맞은 것을 재서 확인했습니다.
- **`.live` 를 `global.css` 로 옮겼습니다.** 세 페이지가 각자 같은 규칙을 들고 있었습니다. 배경을 `--accent` 로 바꿔서 라이트에서도 4.6:1 나옵니다 (재서 확인).
- 곁다리로 `consts.ts` 의 `"너에게 닿기를"` 에서 꺾쇠를 뗐습니다 — §1 에서 금지한 표기가 화면에 나오고 있었습니다.
- 안 한 것: **프로젝트 대표 사진 필드.** 지금은 devlog 커버로 충분하고, 쓰지도 않을 필드를 미리 만들면 다음에 볼 때 뭔지 다시 알아봐야 합니다.

### 2026-08-24 · 서고(`/posts`)를 새로 짜고 홈에서 에디터 알약을 뺐습니다

홈에서 "최신 글 · 전체보기" 를 누르면 옛 3열 지면이 나와서, 방금 본 홈과
전혀 다른 화면으로 떨어졌습니다. 보내주신 시안대로 다시 만들었습니다.

```
[ 에디터 ]   [ 전체 글 · 연도로 묶은 줄 ]
```

- **`FeedLayout`(3열)을 안 씁니다.** 홈과 같은 폭·같은 여백을 쓰고, 오른쪽 열 대신 아래 띠(`SiteFooter`)를 붙였습니다. `/authors`·`/projects` 는 그대로 3열입니다.
- **거르기는 브라우저가 합니다.** 128편이 이미 다 그려져 있어서 서버를 다시 부를 이유가 없습니다. 대신 주소에 `?author=` 를 남겨서 그 상태로 주고받거나 뒤로가기로 돌아올 수 있게 했습니다 (거르기·뒤로가기 둘 다 확인했습니다).
- ⚠ **썸네일은 640px 아래에서 뺐습니다.** 128줄에 사진을 다 깔면 제목을 훑는 데 방해가 되고, 사진이 `public/` 원본 그대로 나가서 폰에서 무겁습니다.
- **날짜에서 점을 뗐습니다.** `ko-KR` 은 `08. 19.` 처럼 점과 공백을 붙여 주는데, 줄이 촘촘한 곳이라 `08.19` 로 줄였습니다 (`FormattedDate` 의 `short` 는 여기서만 씁니다).
- **홈의 에디터 알약을 없앴습니다.** 바로 아래 아래 띠에 같은 `Editors` 목록이 있어서 두 번 나왔습니다. 저자로 좁히는 건 서고 왼쪽 줄이 대신합니다.

### 2026-08-24 · 홈을 뉴스 지면으로 바꿨습니다

보내주신 매거진 시안을 옮겼습니다. 기사 자리는 글로 바꿨고 헤더는 손대지
않았습니다. 옛 홈과 나란히 놓고 보고 이쪽으로 정했습니다.

```
[ 헤드라인 ]   큰 커버 + 옆에 세 줄
[ 최신 글 ]    카드 넷 (폰 2열)
[ 더 읽을 글 ] 번호 붙은 줄 여덟
[ 에디터 ]     알약
[ 아래 띠 ]
```

- **옛 홈을 `/preview/home-now` 에 띄워 두고 나란히 보고 골랐습니다.** 새 지면으로 정해서 그 파일과 `robots.txt` 의 `Disallow: /preview/` 를 지웠습니다 — 되돌려야 하면 구현이 커밋에 통째로 남아 있습니다.
- ⚠ **"목록에 상자를 두르지 않는다"(§4)에 홈만 예외를 뒀습니다.** `/posts` 는 그대로 줄입니다.
- **원본의 "카테고리" 알약은 에디터로 바꿨습니다.** 카테고리는 되살리지 않기로 한 것입니다 (§3).
- **헤드라인 그늘을 두 번 만졌습니다.** 처음 값이 얕아서 GridBrawl 게임 화면 위에서 제목이 섞였습니다 — 띄워 놓고 보고 진하게 다시 깔았습니다.
- **원본의 "인기 기사"는 "더 읽을 글"입니다.** 조회수를 셀 데가 없어서(정적 사이트) 오른쪽은 날짜로 뒀습니다. **번호는 순위가 아니라 목록 번호입니다.**
- ⚠ **저자 묶음이 없어졌습니다.** 최신순으로만 잘라 쓰기 때문에, 지금 데이터로는 위에서 열두 칸이 전부 임데브입니다 (study Lim 은 "더 읽을 글" 다섯째 줄부터). **알고 버린 것입니다** — 저자로 나누는 지면과 최신순 지면 중에 최신순이 낫다고 봤습니다.
- 스크롤 효과(패럴랙스·표지 고정·떠오르기)는 넣지 않았습니다.

### 2026-08-24 · 아래 띠가 글 목록보다 32px 넓게 퍼져 있던 것

`EDITORS`·`SERVICE`·저작권 줄이 위쪽 카드보다 왼쪽으로 튀어나와 있었습니다.

- **가로 여백을 `.foot` 바깥이 아니라 안쪽 두 덩어리로 옮겼습니다.** 지면(`.band`)은 `max-width: 1280px` **안에** 여백을 두는데 아래 띠는 바깥에 두고 있어서, 안쪽 폭이 그만큼(2rem × 2) 넓었습니다.
- **여백을 넓히는 지점을 지면과 같은 900px 로 맞췄습니다.** 700px 이었어서 그 사이 구간에서 또 어긋났습니다.
- 구분선은 그대로 화면 끝까지 갑니다 — 선은 `.foot` 에 있고 여백만 안쪽으로 내려갔습니다.
- 1400 · 800 · 375px 에서 좌우 좌표를 재서 맞춘 것을 확인했습니다.

### 2026-08-24 · 홈에서 최근 글을 저자 묶음 위로 올렸습니다

첫 꼭지 바로 아래가 저자 묶음이라, 방금 올린 글을 보러 들어와도 한참
스크롤해야 나왔습니다. 최근 글 목록과 자리를 바꿨습니다.

- **고르는 순서도 같이 뒤집었습니다.** 화면에서 위에 있는 쪽이 먼저 집어갑니다 — 안 그러면 "최근 글" 이 그 아래 묶음보다 오래된 글을 늘어놓습니다.
- **최근 글을 10편에서 6편으로 줄였습니다.** 지금 최신 12편이 전부 임데브라, 10편을 먼저 집어가면 임데브 묶음에 한 편만 남습니다. 6편이면 셋 다 여섯 칸으로 찹니다.
- 안 한 것: **묶음 칸 수를 자동으로 맞추기.** 글이 늘면 이 균형은 저절로 풀리는데, 그때 다시 보는 편이 낫다고 봤습니다 (§4).

### 2026-08-24 · 홈 카드에 마우스를 올리면 테두리가 서던 것

저자 색 2px 테두리(`outline`)가 커버 밖으로 둘러쳤는데, 저자 표지가 화면 위에
붙어 있는 구간에서는 그 선이 `All` 옆까지 올라온 것처럼 보였습니다.

- **테두리를 뺐습니다.** 제목에 밑줄이 그어지는 것만으로 어느 칸을 가리키고 있는지 충분히 읽힙니다.
- ⚠ **다시 넣지 마세요.** 목록에 상자를 두르지 않기로 한 것(§4)과 같은 이유입니다 — hover 때만 생기는 테두리도 결국 카드 윤곽입니다.

### 2026-08-13 · 카테고리·태그를 없애고 저자로 합쳤습니다 (매거진화)

분류가 세 겹이었습니다 — 카테고리 트리 15개, 태그, 그리고 프로젝트. 그런데
태그는 124편 중 8편에만 붙어 있었고, 카테고리는 주제(React·SwiftUI)로 잘려
있어서 "이 글이 어떤 성격인가"는 어디에도 없었습니다.

- **저자 하나로 합쳤습니다.** 트리도 태그도 없습니다. `/categories/*` 15개와 `/tags/*` 11개 주소가 사라졌고 **되살리지 않습니다** — 색인이 막 시작된 참이라 잃을 게 적다고 보고 그냥 버렸습니다. Search Console 은 나중에 다시 붙입니다.
- **iOS 를 공부에 합쳤습니다.** 학습 기록 113편이 전부 `study Lim` 입니다. iOS·React·Flutter 를 저자로 쪼개면 "주제별 태그"를 이름만 바꾼 것이 됩니다 — 저자는 주제가 아니라 성격으로 나눕니다.
- **BeepTimer 글 3편만 `개발자` 로 옮겼습니다.** 만든 앱의 devlog 라 학습 기록과 성격이 다릅니다. 옮기면서 `project: beeptimer` 도 같이 붙였습니다.
- ⚠ **이름과 `id` 가 다릅니다.** 화면은 `study Lim`·`개발자` 인데 주소는 `/authors/student`·`/authors/developer` 입니다. 이름은 바꿀 수 있지만 `id` 를 바꾸면 글 124편의 frontmatter 와 주소가 같이 움직입니다 — 이름만 고치세요.
- ⚠ **저자가 한 사람이라는 것을 사이트 어디에도 쓰지 않기로 했습니다.** 기획 단계에서 밝히는 쪽을 권했지만 안 밝히는 것으로 정해졌습니다. 자동으로 문구를 넣지 마세요.
- **색은 새 계통(`--who-*`)을 만들었습니다.** 프로젝트 색(`--nj-*`)은 알약 **배경**인데 저자 색은 **글자색**이라, 400 단계를 그대로 쓰면 라이트 테마에서 2.8:1 로 떨어집니다. 강조색처럼 테마별로 다른 값을 씁니다.
- **목록에서 카드를 걷어냈습니다.** 큰 제목 + 구분선입니다. 안 한 것: **사진 표지** — `heroImage` 가 0장이라 지금 넣으면 124편 전부가 빈 자리가 됩니다.
- **Works 를 Live Service 로 바꿨습니다.** 이름만 바뀌었고 `PROJECTS` 는 그대로입니다.
- **`/authors` 에 얼굴을 넣었습니다.** 사진이 없어서 글자 아바타로 채웠습니다 — 자세한 건 §3. 머리글은 `Editors` 하나만 둡니다 (설명 문장을 붙였다 뺐습니다).
- **Editors 를 오른쪽으로 옮기고 왼쪽은 비웠습니다.** 저자 목록이 왼쪽 끝에 혼자 있으면 지면과 멀어서, 프로젝트·연락처와 한 열에 모았습니다.
- **Profile 카드를 뺐습니다.** 저자 이름으로 굴러가는 지면에서 오른쪽에 얼굴과 실명 소개가 따로 서 있으면 두 체계가 겹칩니다. `OWNER` 는 Contact 링크와 저작권 줄에만 남았고, `role`·`bio` 는 쓰는 데가 없어져서 지웠습니다. ⚠ `public/avatar.svg` 는 이제 아무 데서도 안 씁니다 — `avatar-512.png` 는 공유 카드 기본 이미지라 계속 필요합니다.

### 2026-08-13 · 글 상세에 사이드바를 붙였다가 뺐습니다

넓은 화면에서 글 양옆이 400px 넘게 비어서, 오른쪽에 Profile·Works·Contact 를
붙이고 글 폭을 832 → 912px 로 넓혀 봤습니다. **써 보고 예전이 낫다고 판단해
되돌렸습니다**(`cfeef0b` → `faa8ac9`).

⚠ **같은 제안을 다시 하지 마세요.** 글 상세는 `max-width: 56rem` 한 열입니다.
양옆이 비어 보이는 건 알고 남겨둔 것입니다 — 읽는 화면에는 글만 있는 편이 낫습니다.
정말 다시 손대야 하면 되돌린 커밋에 구현이 통째로 남아 있습니다.

### 2026-08-13 · `/admin` 본문에 툴바를 붙였습니다

본문 칸에 글자만 칠 수 있었습니다. 제목·굵게·색은 마크다운을 외워서 손으로
쳐야 했고, **사진은 아예 못 넣었습니다.**

- **Decap 위젯을 갈아끼웠습니다** (`public/admin/editor.js`). 리치 텍스트는 폰에서 한글이 깨져서 못 쓰고, 원문 모드는 툴바가 전부 `disabled` 라 못 씁니다 — 양쪽 다 막혀 있어서 직접 만들었습니다. 자세한 건 §6-2.
- **저장되는 건 여전히 마크다운 원문입니다.** 형식을 바꾸면 글 117개와 갈라지고, 티스토리에서 옮겨온 것들을 다시 손봐야 합니다. 툴바는 커서 자리에 마크다운을 꽂아 넣기만 합니다.
- **색은 값이 아니라 클래스로 넣습니다.** `#58dddf` 를 본문에 박으면 라이트 테마에서 안 읽힙니다. 다섯 색 모두 두 테마에서 4.5:1 이상 나오게 `global.css` 에서 짝을 맞춰 뒀습니다.
- **사진은 `public/images/<주소>/`** — 손으로 쓰던 규칙(§2)에 맞췄습니다. 그래서 주소를 안 적었으면 사진 버튼이 안 열립니다.
- 안 한 것: **미리보기**. 마크다운을 그려 보려면 파서를 하나 더 얹어야 하는데, 폰 화면에서 반을 미리보기에 내주는 게 남는 장사인지 모르겠습니다.

### 2026-08-13 · 탭 아이콘이 아직 Astro 기본값이었습니다

탭에 뜨던 건 Astro 스타터의 "A" 아이콘이었습니다. 예전 티스토리/morethan-log
쪽에서 쓰던 **Lim 원형 로고**(`~/Develop/React/morethan-log/public/favicon.ico`)로
되돌리고, 폰에서 홈 화면에 추가할 때도 같은 로고가 나오게 했습니다.

- **`public/favicon.svg` 를 지운 게 핵심입니다.** 브라우저는 SVG 아이콘이 걸려 있으면 `.ico` 보다 그쪽을 먼저 씁니다 — 남겨두면 `.ico` 만 바꿔도 탭은 그대로입니다. 대신할 SVG 로고 원본이 없어서 `.ico` + PNG 조합으로 갔습니다.
- **홈 화면 아이콘은 `.ico` 안의 48×48 을 키워서 만들었습니다** (`apple-touch-icon` 180 · `icon-192` · `icon-512`). 원본이 그것뿐이라 512 는 눈에 띄게 뭉갭니다. 벡터나 큰 원본이 나오면 그때 다시 뽑는 게 맞습니다.
- 로고 바깥은 투명인데 **PNG 는 흰 배경에 얹어 내보냈습니다.** iOS 는 투명한 아이콘 뒤를 검게 깔아서, 그대로 두면 검은 원이 배경에 묻힙니다.
- `site.webmanifest` 를 새로 넣었습니다. `theme_color` 는 다크 배경(`--lim-gray-990`)에 맞췄습니다 — 기본 테마가 다크라서 라이트 값으로 두면 상단 바만 하얗게 뜹니다.

### 2026-08-19 · `/admin` 에서 옛 글을 고치면 저장이 막히던 것

"필수 필드를 놓치셨습니다" 가 뜨고, **비밀글 토글을 켰다 꺼야만** 저장이
됐습니다. 걸리는 필드는 둘이었습니다.

- **비밀글로 두기** — Decap 의 boolean 은 값이 아예 없으면(frontmatter 에 키가 없으면) 필수 검사를 통과 못 합니다. 토글을 만지면 그제야 `false` 가 들어가서 풀린 것이었습니다. `required: false` 를 붙였습니다 — 없으면 false 라는 뜻이고 스키마도 그렇게 봅니다. `draft` 도 같이 걸릴 수 있어 같이 붙였습니다.
- **주소** — 손으로 쓴 글에는 `slug` 키가 없었습니다. 이건 필수를 풀면 안 됩니다(새 글의 파일명을 이 값으로 만듭니다). 그래서 **글 126편에 `slug` 를 채웠습니다** — 값은 파일명과 같아서 주소는 하나도 안 바뀝니다.
- ⚠ **알게 된 것: `slug` 는 버려지는 키가 아니라 주소 그 자체입니다.** §3 에 "Astro 스키마가 모르는 키라 버린다"고 적어 뒀던 것은 틀렸습니다 — glob 로더가 그 값을 글의 id 로 씁니다. `/admin` 에서 주소를 고친 글 두 편이 이미 옛 주소에서 404 입니다.
- 재현하고 고친 뒤 로컬 Decap 으로 실제 저장까지 확인했습니다("변경사항 저장됨", 경고 없음).

### 2026-08-14 · 이름·메뉴·푸터 다듬기

- **개발자 → 임데브.** 화면 이름만 바꿨습니다. `id` 는 `developer` 그대로입니다 — 바꾸면 글 124편의 frontmatter 와 주소가 같이 움직입니다 (§3).
- **Live Service → Service.** 화면에 보이는 이름만 바꿨습니다. `PROJECTS` 는 그대로입니다.
- **`/authors` 에서 오른쪽 열을 걷어냈습니다.** 저자를 소개하는 자리에 옆에서 같은 저자 목록이 또 나오면 무엇을 보라는 것인지 알 수 없습니다.
- **푸터에서 글 수와 LIVE 배지를 뺐습니다.** 맨 아래는 가는 곳을 알려주는 자리라 숫자가 필요 없습니다. 메뉴 판에는 남겨 뒀습니다 — 거기서는 고르는 데 도움이 됩니다.
- **메뉴 판이 미끄러져 들어옵니다.** ⚠ 여닫는 것을 `hidden` 이 아니라 클래스로 합니다 — `hidden` 은 `display:none` 이라 중간 상태가 없어서 움직임을 그릴 수가 없습니다. 닫힌 동안에는 `visibility` 로 감춥니다. 화면 밖에 두기만 하면 탭 키가 거기로 들어갑니다.

### 2026-08-14 · 홈을 새로 만들었습니다 (시안 D 적용)

시안 넷과 헤더 셋을 만들어 보고 **D + 헤더1** 로 정해 홈에 넣었습니다.
`/preview/*` 와 `Preview*` 컴포넌트는 전부 지웠습니다 — 고른 뒤에도 남겨
두면 다음에 볼 때 어느 것이 진짜인지 알 수 없습니다.

- 시안용이던 것 셋만 이름을 바꿔 남겼습니다 — `Cover.astro`(커버 그림), `SiteFooter.astro`(아래 띠), 그리고 헤더.
- **홈만 사이드바가 없습니다.** 나머지는 그대로 3열입니다 (§4).
- **옛 카테고리는 안 되살렸습니다.** 메뉴의 큰 항목은 저자·프로젝트입니다. 되살리려면 124편의 frontmatter 를 다시 손대야 하는데, 저자 하나로 합친 지 하루밖에 안 됐습니다.
- 남은 것: **사진을 크기별로 뽑기.** 커버가 `public/` 원본 그대로라 폰에서 무겁습니다.

### 2026-08-14 · 이름을 lim's World 로 · 검색 · 헤더 시안

- **사이트 이름을 바꿨습니다.** `SITE_TITLE` 한 곳이 화면 전부를 덮지만, 홈 화면에 추가할 때 뜨는 이름(`site.webmanifest`)과 `/admin` 탭 제목은 따로라 같이 고쳤습니다. **주소(`lim-it.vercel.app`)와 저장소 이름은 그대로입니다.**
- **검색을 붙였습니다.** 정적 사이트라 서버가 없어서, 빌드할 때 색인(`/search.json`)을 내보내고 브라우저가 받아서 걸러냅니다. 83KB 입니다.
  - ⚠ **본문 전체는 안 넣습니다.** 제목·소개문·본문 앞 240자까지입니다. 전체를 넣으면 몇 MB가 되고 검색창을 열 때마다 그걸 받게 됩니다.
  - ⚠ **비밀글은 본문에서 아무것도 뽑지 않습니다.** 색인에 실으면 주소만 열어도 다 읽힙니다 (§6-3).
  - 형태소 분석 없이 **글자 포함**으로 찾습니다. 한국어는 이걸로 충분합니다 — "지뢰"로 "지뢰찾기"가 걸립니다.
- **헤더 시안 셋을 만들었습니다** (`/preview/header-a·b·c`). 지면은 D 그대로 두고 헤더만 갈아 끼웁니다. 그러려고 D의 지면을 `PreviewHome.astro` 로 떼어냈고, `BaseLayout` 에 `hideHeader` 를 붙였습니다.

### 2026-08-14 · D 를 다듬었습니다 — 색·카드·스크롤

D 로 정하고 세 가지를 손봤습니다.

- **색은 안 늘렸습니다.** 저자 색(`--who-*`)이 놓이는 자리만 늘렸습니다 — 표지 밑줄, 카드 hover 테두리, 그리고 사진 없는 글의 커버. 색 체계가 이미 둘(저자·프로젝트)이라 셋째를 만들면 어느 색이 분류인지 안 읽힙니다.
- **사진 없는 커버를 저자 색 판으로 바꿨습니다.** 그림 파일을 지웠습니다 — 자세한 이유는 §4.
- **카드**: 제목을 두 줄에서 자르고(길이가 제각각이면 아래가 들쭉날쭉합니다), 사진은 **위쪽 기준으로 자릅니다**(코드·앱 캡처는 위에 정보가 몰려 있습니다).
- **스크롤 넷**: 커버 떠오르기 · 저자 표지 상단 고정 · 첫 꼭지 패럴랙스(큰 화면만) · 진행 막대. 라이브러리 없이 IntersectionObserver 로 했습니다.
- ⚠ **숨기는 클래스는 스크립트가 붙입니다.** CSS 로 처음부터 숨기면 자바스크립트가 죽었을 때 지면이 통째로 안 보입니다. `prefers-reduced-motion` 도 확인합니다.
- ⚠ **패럴랙스는 틀이 아니라 안쪽 사진을 움직입니다.** 틀을 밀면 아래에 빈 틈이 생깁니다 (한 번 그렇게 만들었다가 고쳤습니다).

### 2026-08-14 · 홈 디자인 시안 세 개 (임시)

홈이 밋밋하다는 이야기가 나와서 롱블랙·폴인·디에디트를 다시 봤습니다.
셋의 공통점은 **꼭지마다 사진 한 장**이었고, 그게 이 블로그에 없던 것입니다.

- **커버가 없는 게 아니었습니다.** `heroImage` 는 0편인데 본문에 사진이 있는 글이 91편이었습니다. 첫 사진을 커버로 쓰기로 했습니다 (§4).
- **시안을 말로 설명하지 않고 페이지로 만들었습니다** — `/preview/a·b·c`. 진짜 글 데이터로 그려서 폰에서 비교할 수 있습니다. ⚠ **고르고 나면 `src/pages/preview/`·`PreviewNav`·`PreviewCover`·robots.txt 의 Disallow 를 같이 지웁니다.**
- 참고 사이트는 눈대중하지 않고 폰 크기에서 재 봤습니다 — 디에디트는 커버가 화면을 꽉 채우고 제목 36px/800, 목록 사진은 2:3. 폴인은 4:3 카드 + 1:1 작은 썸네일. 롱블랙은 하루 한 편을 세우고 아래에 가로 스크롤 묶음.
- 알게 된 것: **작은 썸네일(88px)에서는 코드 캡처가 아무것도 안 알려줍니다.** 사진을 작게 여러 장 늘어놓는 구성(C)은 이 블로그 사진과 안 맞습니다.
- **D 를 더했습니다** — A의 첫 꼭지·세로 사진 격자 + B의 저자별 구분 + C의 최근 글 목록. 저자 이름을 지면을 나누는 표지로 쓰기 때문에 A보다 크게 씁니다.
- **D 로 정했습니다.** 이어서 색·카드·스크롤을 다듬었습니다 — 아래 항목 참고.

### 2026-08-13 · 카테고리·프로젝트 값의 첫 글자가 잘리던 것

"게임 개발"이 "베임 개발"로, "MineApp"이 "VlineApp"으로 보였습니다.

react-select 가 **보이지 않는 입력칸을 값 글자와 같은 칸에 겹쳐** 두는데
(둘 다 `grid-area: 1/1`), 다크모드용으로 입력칸에 배경을 칠한 규칙이 여기까지
닿아서 그 4px 이 첫 글자 위에 얹혔습니다. 잘린 게 아니라 덮인 것이었습니다.

- 눈으로 찾기 어려운 종류라 **화면을 재 보고 찾았습니다** — 글자 상자와 입력칸의 좌표가 똑같이 x=44 였습니다. 비슷한 증상이 또 나오면 위치부터 재 보세요.

### 2026-08-13 · `/admin` 편집기를 tiptap 으로 바꿨습니다

마크다운을 손으로 쳐야 하고, 쓴 것이 어떻게 보일지는 게시해 봐야 알았습니다.
쓰는 화면이 곧 결과가 되도록 tiptap 으로 갈아끼웠습니다.

- **저장 형식은 안 건드렸습니다.** 글 117개가 마크다운 파일이라, 편집기만 바꾸고 파일은 그대로 둡니다. 자세한 건 §6-2.
- **옛 글은 원문 모드로 엽니다.** 왕복시키면 목록·구분선 표기가 다시 쓰여서 한 글자만 고쳐도 파일 전체가 바뀝니다. 열 때 한 번 되돌려 보고 원본과 다르면 원문으로 엽니다 — 판정 기준은 검사 스크립트와 같은 함수를 씁니다(다르면 검사는 통과인데 폰에서는 원문으로 열리는 일이 생깁니다).
- **표가 사라지는 걸 먼저 잡았습니다.** StarterKit 에 표가 없어서, 표가 있는 글을 열었다 저장하면 표가 통째로 없어졌습니다. `scripts/check-md-roundtrip.mjs` 를 만들어 글 전체를 돌려 보고 찾았습니다 — **확장을 더할 때마다 이걸 돌리세요.**
- 안 한 것: **본문에 쓴 HTML 살리기.** `<label>` 처럼 글자로 쓴 태그는 왕복에서 없어집니다. 그런 글이 8개인데, 원문 모드로 열리니 실제로 부딪힐 일은 없다고 봤습니다.
- 링크하신 tiptap simple-editor 템플릿은 안 썼습니다 — React 컴포넌트 묶음이라 정적 `/admin` 에 얹으려면 React 를 따로 넣어야 합니다. 코어만 쓰고 툴바는 쓰던 것을 그대로 뒀습니다.

### 2026-08-13 · `/admin` 토글이 빈 상자를 하나씩 차지하던 것

"초안으로 두기"·"비밀글로 두기"가 입력칸과 똑같은 상자에 담겨 있어서,
20px 짜리 스위치 하나가 59px 짜리 빈 판을 차지하고 있었습니다. 폰에서는
그만큼 스크롤이 늘어납니다. 라벨 옆에 스위치만 놓고 힌트는 아래로 내렸습니다.

- 상자만 지우면 라벨이 붕 뜹니다 — 라벨은 상자 위에 얹히는 **탭 모양**(위쪽 모서리만 둥글고 상자와 잇는 이음매가 붙습니다)이라, 같이 풀어서 평범한 글자로 만들었습니다.
- 위젯 종류를 가릴 방법이 emotion 해시밖에 없어서 `:has([role='switch'])` 로 잡았습니다. `:has()` 를 모르는 브라우저에서는 규칙이 통째로 무시돼 예전 모양 그대로 보입니다 — 깨지진 않습니다.

### 2026-08-13 · 글쓴이 표시를 뺐습니다

글 117개 중 116개가 "직접 씀"이었습니다. 어차피 전부 제 글이라 날짜 옆에
붙는 그 한 줄이 아무것도 구분해 주지 못했고, `/admin` 폼에서도 매번 그냥
넘기는 칸이었습니다.

- 화면·스키마·`/admin` 폼·글 117개의 frontmatter 에서 전부 뺐습니다. "나중에 쓸지도 모르니 값만 남겨두기"는 안 했습니다 — 안 쓰는 키가 남아 있으면 다음에 볼 때 뭔지 다시 알아봐야 합니다.
- **AI가 초안을 썼다는 표시도 같이 없어졌습니다.** 대신 §5 절차에서 사람이 반드시 읽고 고친 뒤 올리도록 했습니다. 표시로 면피하는 것보다 초안을 그대로 안 올리는 쪽이 맞다고 봤습니다.
- `/admin` 폼을 고쳤으므로 `index.html` 의 `config.yml?v=` 를 올렸습니다 (§6-2).

### 2026-08-13 · 검색엔진에 글이 제대로 안 잡히던 것

글 117개 중 116개에 `description` 이 없어서, meta description 이 전부
`SITE_DESCRIPTION` 하나로 채워지고 있었습니다. 검색 결과에서 어느 글인지
구분이 안 되는 상태였습니다.

- **없는 소개문을 지어내지 않기로 했습니다.** 대신 본문 앞부분에서 뽑아 `<head>` 에만 씁니다 — 화면 카드에는 여전히 안 나옵니다. 소개문이 없는 글은 없는 채로 보이는 게 맞다고 봤습니다. RSS 설명도 같은 값을 씁니다.
- **공유 카드가 텅 비던 것** — `heroImage` 가 없는 글이 대부분이라 OG 이미지가 아예 안 붙었습니다. 없으면 프로필 이미지로 떨어지게 했습니다.
- **구조화 데이터(JSON-LD)를 넣었습니다.** 글이면 `BlogPosting`, 나머지는 `WebSite`. ⚠ 여기 넣는 값은 **화면에도 있는 것**만 씁니다. 안 보이는 정보를 구조화 데이터에만 넣으면 스팸으로 봅니다.
- `robots.txt` 를 넣고 sitemap 위치를 알렸습니다. 막은 건 `/admin/` 뿐입니다.

⚠ **`BaseHead.astro` 의 `google-site-verification` 태그를 지우지 마세요.** Search Console 소유권 확인용인데, 확인이 끝난 뒤에도 구글이 주기적으로 다시 봅니다 — 사라져 있으면 속성이 해제되고 색인 현황·검색어 데이터를 못 봅니다. `lim-it.vercel.app` 은 서브도메인이라 DNS 확인 방식을 못 써서 이 태그가 유일한 끈입니다. **커스텀 도메인으로 옮기면** 그때는 DNS 방식으로 갈아타고 이 태그를 빼도 됩니다.

⚠ **비밀글(§6-3)에는 요약을 뽑지 않습니다.** 본문을 잠가 놓고 meta description
으로 앞부분을 흘리면 잠근 의미가 없습니다. RSS 는 비밀글을 아예 빼고 있어서
괜찮지만, **본문에서 뭔가를 뽑아 쓰는 코드를 새로 넣을 때는 `secret` 을 같이
확인하세요.**
